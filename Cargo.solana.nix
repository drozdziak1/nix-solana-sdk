# This file was @generated by cargo2nix 0.9.0.
# It is not intended to be manually edited.

args@{
  release ? true,
  rootFeatures ? [
    "solana-accounts-cluster-bench/default"
    "solana-account-decoder/default"
    "solana-config-program/default"
    "solana-sdk/default"
    "solana-crate-features/default"
    "solana-frozen-abi/default"
    "solana-frozen-abi-macro/default"
    "solana-logger/default"
    "solana-program/default"
    "solana-sdk-macro/default"
    "solana-stake-program/default"
    "solana-metrics/default"
    "solana-vote-program/default"
    "solana-clap-utils/default"
    "solana-remote-wallet/default"
    "solana-client/default"
    "solana-faucet/default"
    "solana-cli-config/default"
    "solana-version/default"
    "solana-net-utils/default"
    "solana-transaction-status/default"
    "solana-runtime/default"
    "solana-measure/default"
    "solana-rayon-threadlimit/default"
    "solana-secp256k1-program/default"
    "solana-noop-program/default"
    "solana-core/default"
    "solana-banks-server/default"
    "solana-banks-interface/default"
    "solana-ledger/default"
    "solana-bpf-loader-program/default"
    "solana-merkle-tree/default"
    "solana-perf/default"
    "solana-budget-program/default"
    "solana-storage-bigtable/default"
    "solana-storage-proto/default"
    "solana-program-test/default"
    "solana-banks-client/default"
    "solana-streamer/default"
    "solana-sys-tuner/default"
    "solana-local-cluster/default"
    "solana-download-utils/default"
    "solana-exchange-program/default"
    "solana-vest-program/default"
    "solana-bench-exchange/default"
    "solana-genesis/default"
    "solana-bench-streamer/default"
    "solana-bench-tps/default"
    "solana-accounts-bench/default"
    "solana-banking-bench/default"
    "solana-cli-output/default"
    "solana-dos/default"
    "solana-validator/default"
    "solana-gossip/default"
    "solana-install/default"
    "solana-keygen/default"
    "solana-ledger-tool/default"
    "solana-log-analyzer/default"
    "solana-merkle-root-bench/default"
    "solana-stake-o-matic/default"
    "solana-notifier/default"
    "solana-net-shaper/default"
    "solana-poh-bench/default"
    "solana-failure-program/default"
    "solana-ownable/default"
    "solana-ramp-tps/default"
    "solana-store-tool/default"
    "solana-cargo-build-bpf/default"
    "solana-cargo-test-bpf/default"
    "solana-scripts/default"
    "solana-stake-accounts/default"
    "solana-stake-monitor/default"
    "solana-tokens/default"
    "solana-upload-perf/default"
    "solana-cli/default"
    "solana-watchtower/default"
  ],
  rustPackages,
  buildRustPackages,
  hostPlatform,
  hostPlatformCpu ? null,
  hostPlatformFeatures ? [],
  mkRustCrate,
  rustLib,
  lib,
  workspaceSrc,
}:
let
  workspaceSrc = if args.workspaceSrc == null then ./. else args.workspaceSrc;
in let
  inherit (rustLib) fetchCratesIo fetchCrateLocal fetchCrateGit fetchCrateAlternativeRegistry expandFeatures decideProfile genDrvsByProfile;
  profilesByName = {
  };
  rootFeatures' = expandFeatures rootFeatures;
  overridableMkRustCrate = f:
    let
      drvs = genDrvsByProfile profilesByName ({ profile, profileName }: mkRustCrate ({ inherit release profile hostPlatformCpu hostPlatformFeatures; } // (f profileName)));
    in { compileMode ? null, profileName ? decideProfile compileMode release }:
      let drv = drvs.${profileName}; in if compileMode == null then drv else drv.override { inherit compileMode; };
in
{
  cargo2nixVersion = "0.9.0";
  workspace = {
    solana-accounts-cluster-bench = rustPackages.unknown.solana-accounts-cluster-bench."1.6.6";
    solana-account-decoder = rustPackages.unknown.solana-account-decoder."1.6.6";
    solana-config-program = rustPackages.unknown.solana-config-program."1.6.6";
    solana-sdk = rustPackages.unknown.solana-sdk."1.6.6";
    solana-crate-features = rustPackages.unknown.solana-crate-features."1.6.6";
    solana-frozen-abi = rustPackages.unknown.solana-frozen-abi."1.6.6";
    solana-frozen-abi-macro = rustPackages.unknown.solana-frozen-abi-macro."1.6.6";
    solana-logger = rustPackages.unknown.solana-logger."1.6.6";
    solana-program = rustPackages.unknown.solana-program."1.6.6";
    solana-sdk-macro = rustPackages.unknown.solana-sdk-macro."1.6.6";
    solana-stake-program = rustPackages.unknown.solana-stake-program."1.6.6";
    solana-metrics = rustPackages.unknown.solana-metrics."1.6.6";
    solana-vote-program = rustPackages.unknown.solana-vote-program."1.6.6";
    solana-clap-utils = rustPackages.unknown.solana-clap-utils."1.6.6";
    solana-remote-wallet = rustPackages.unknown.solana-remote-wallet."1.6.6";
    solana-client = rustPackages.unknown.solana-client."1.6.6";
    solana-faucet = rustPackages.unknown.solana-faucet."1.6.6";
    solana-cli-config = rustPackages.unknown.solana-cli-config."1.6.6";
    solana-version = rustPackages.unknown.solana-version."1.6.6";
    solana-net-utils = rustPackages.unknown.solana-net-utils."1.6.6";
    solana-transaction-status = rustPackages.unknown.solana-transaction-status."1.6.6";
    solana-runtime = rustPackages.unknown.solana-runtime."1.6.6";
    solana-measure = rustPackages.unknown.solana-measure."1.6.6";
    solana-rayon-threadlimit = rustPackages.unknown.solana-rayon-threadlimit."1.6.6";
    solana-secp256k1-program = rustPackages.unknown.solana-secp256k1-program."1.6.6";
    solana-noop-program = rustPackages.unknown.solana-noop-program."1.6.6";
    solana-core = rustPackages.unknown.solana-core."1.6.6";
    solana-banks-server = rustPackages.unknown.solana-banks-server."1.6.6";
    solana-banks-interface = rustPackages.unknown.solana-banks-interface."1.6.6";
    solana-ledger = rustPackages.unknown.solana-ledger."1.6.6";
    solana-bpf-loader-program = rustPackages.unknown.solana-bpf-loader-program."1.6.6";
    solana-merkle-tree = rustPackages.unknown.solana-merkle-tree."1.6.6";
    solana-perf = rustPackages.unknown.solana-perf."1.6.6";
    solana-budget-program = rustPackages.unknown.solana-budget-program."1.6.6";
    solana-storage-bigtable = rustPackages.unknown.solana-storage-bigtable."1.6.6";
    solana-storage-proto = rustPackages.unknown.solana-storage-proto."1.6.6";
    solana-program-test = rustPackages.unknown.solana-program-test."1.6.6";
    solana-banks-client = rustPackages.unknown.solana-banks-client."1.6.6";
    solana-streamer = rustPackages.unknown.solana-streamer."1.6.6";
    solana-sys-tuner = rustPackages.unknown.solana-sys-tuner."1.6.6";
    solana-local-cluster = rustPackages.unknown.solana-local-cluster."1.6.6";
    solana-download-utils = rustPackages.unknown.solana-download-utils."1.6.6";
    solana-exchange-program = rustPackages.unknown.solana-exchange-program."1.6.6";
    solana-vest-program = rustPackages.unknown.solana-vest-program."1.6.6";
    solana-bench-exchange = rustPackages.unknown.solana-bench-exchange."1.6.6";
    solana-genesis = rustPackages.unknown.solana-genesis."1.6.6";
    solana-bench-streamer = rustPackages.unknown.solana-bench-streamer."1.6.6";
    solana-bench-tps = rustPackages.unknown.solana-bench-tps."1.6.6";
    solana-accounts-bench = rustPackages.unknown.solana-accounts-bench."1.6.6";
    solana-banking-bench = rustPackages.unknown.solana-banking-bench."1.6.6";
    solana-cli-output = rustPackages.unknown.solana-cli-output."1.6.6";
    solana-dos = rustPackages.unknown.solana-dos."1.6.6";
    solana-validator = rustPackages.unknown.solana-validator."1.6.6";
    solana-gossip = rustPackages.unknown.solana-gossip."1.6.6";
    solana-install = rustPackages.unknown.solana-install."1.6.6";
    solana-keygen = rustPackages.unknown.solana-keygen."1.6.6";
    solana-ledger-tool = rustPackages.unknown.solana-ledger-tool."1.6.6";
    solana-log-analyzer = rustPackages.unknown.solana-log-analyzer."1.6.6";
    solana-merkle-root-bench = rustPackages.unknown.solana-merkle-root-bench."1.6.6";
    solana-stake-o-matic = rustPackages.unknown.solana-stake-o-matic."1.6.6";
    solana-notifier = rustPackages.unknown.solana-notifier."1.6.6";
    solana-net-shaper = rustPackages.unknown.solana-net-shaper."1.6.6";
    solana-poh-bench = rustPackages.unknown.solana-poh-bench."1.6.6";
    solana-failure-program = rustPackages.unknown.solana-failure-program."1.6.6";
    solana-ownable = rustPackages.unknown.solana-ownable."1.6.6";
    solana-ramp-tps = rustPackages.unknown.solana-ramp-tps."1.6.6";
    solana-store-tool = rustPackages.unknown.solana-store-tool."1.6.6";
    solana-cargo-build-bpf = rustPackages.unknown.solana-cargo-build-bpf."1.6.6";
    solana-cargo-test-bpf = rustPackages.unknown.solana-cargo-test-bpf."1.6.6";
    solana-scripts = rustPackages.unknown.solana-scripts."1.6.6";
    solana-stake-accounts = rustPackages.unknown.solana-stake-accounts."1.6.6";
    solana-stake-monitor = rustPackages.unknown.solana-stake-monitor."1.6.6";
    solana-tokens = rustPackages.unknown.solana-tokens."1.6.6";
    solana-upload-perf = rustPackages.unknown.solana-upload-perf."1.6.6";
    solana-cli = rustPackages.unknown.solana-cli."1.6.6";
    solana-watchtower = rustPackages.unknown.solana-watchtower."1.6.6";
  };
  "registry+https://github.com/rust-lang/crates.io-index".Inflector."0.11.4" = overridableMkRustCrate (profileName: rec {
    name = "Inflector";
    version = "0.11.4";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "fe438c63458706e03479442743baae6c88256498e6431708f6dfc520a26515d3"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "heavyweight" ]
      [ "lazy_static" ]
      [ "regex" ]
    ];
    dependencies = {
      lazy_static = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
      regex = rustPackages."registry+https://github.com/rust-lang/crates.io-index".regex."1.3.9" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".addr2line."0.12.1" = overridableMkRustCrate (profileName: rec {
    name = "addr2line";
    version = "0.12.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "a49806b9dadc843c61e7c97e72490ad7f7220ae249012fbda9ad0609457c0543"; };
    dependencies = {
      gimli = rustPackages."registry+https://github.com/rust-lang/crates.io-index".gimli."0.21.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".adler32."1.0.4" = overridableMkRustCrate (profileName: rec {
    name = "adler32";
    version = "1.0.4";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "5d2e7343e7fc9de883d1b0341e0b13970f764c14101234857d2ddafa1cb1cac2"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".ahash."0.4.6" = overridableMkRustCrate (profileName: rec {
    name = "ahash";
    version = "0.4.6";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "f6789e291be47ace86a60303502173d84af8327e3627ecf334356ee0f87a164c"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".ahash."0.6.1" = overridableMkRustCrate (profileName: rec {
    name = "ahash";
    version = "0.6.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "865f8b0b3fced577b7df82e9b0eb7609595d7209c0b39e78d0646672e244b1b1"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "std" ]
    ];
    dependencies = {
      ${ if hostPlatform.parsed.kernel.name == "linux" || hostPlatform.parsed.kernel.name == "android" || hostPlatform.parsed.kernel.name == "windows" || hostPlatform.parsed.kernel.name == "darwin" || hostPlatform.parsed.kernel.name == "ios" || hostPlatform.parsed.kernel.name == "freebsd" || hostPlatform.parsed.kernel.name == "openbsd" || hostPlatform.parsed.kernel.name == "netbsd" || hostPlatform.parsed.kernel.name == "dragonfly" || hostPlatform.parsed.kernel.name == "solaris" || hostPlatform.parsed.kernel.name == "illumos" || hostPlatform.parsed.kernel.name == "fuchsia" || hostPlatform.parsed.kernel.name == "redox" || hostPlatform.parsed.kernel.name == "cloudabi" || hostPlatform.parsed.kernel.name == "haiku" || hostPlatform.parsed.kernel.name == "vxworks" || hostPlatform.parsed.kernel.name == "emscripten" || hostPlatform.parsed.kernel.name == "wasi" then "getrandom" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".getrandom."0.2.0" { inherit profileName; };
      ${ if hostPlatform.parsed.kernel.name == "linux" || hostPlatform.parsed.kernel.name == "android" || hostPlatform.parsed.kernel.name == "windows" || hostPlatform.parsed.kernel.name == "darwin" || hostPlatform.parsed.kernel.name == "ios" || hostPlatform.parsed.kernel.name == "freebsd" || hostPlatform.parsed.kernel.name == "openbsd" || hostPlatform.parsed.kernel.name == "netbsd" || hostPlatform.parsed.kernel.name == "dragonfly" || hostPlatform.parsed.kernel.name == "solaris" || hostPlatform.parsed.kernel.name == "illumos" || hostPlatform.parsed.kernel.name == "fuchsia" || hostPlatform.parsed.kernel.name == "redox" || hostPlatform.parsed.kernel.name == "cloudabi" || hostPlatform.parsed.kernel.name == "haiku" || hostPlatform.parsed.kernel.name == "vxworks" || hostPlatform.parsed.kernel.name == "emscripten" || hostPlatform.parsed.kernel.name == "wasi" then "lazy_static" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
    };
    buildDependencies = {
      version_check = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".version_check."0.9.2" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".aho-corasick."0.7.10" = overridableMkRustCrate (profileName: rec {
    name = "aho-corasick";
    version = "0.7.10";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "8716408b8bc624ed7f65d223ddb9ac2d044c0547b6fa4b0d554f3a9540496ada"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "std" ]
    ];
    dependencies = {
      memchr = rustPackages."registry+https://github.com/rust-lang/crates.io-index".memchr."2.3.3" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".ansi_term."0.11.0" = overridableMkRustCrate (profileName: rec {
    name = "ansi_term";
    version = "0.11.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "ee49baf6cb617b853aa8d93bf420db2383fab46d314482ca2803b40d5fde979b"; };
    dependencies = {
      ${ if hostPlatform.parsed.kernel.name == "windows" then "winapi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.3.8" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".anyhow."1.0.38" = overridableMkRustCrate (profileName: rec {
    name = "anyhow";
    version = "1.0.38";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "afddf7f520a80dbf76e6f50a35bca42a2331ef227a28b3b6dc5c2e2338d114b1"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "std" ]
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".arc-swap."0.4.8" = overridableMkRustCrate (profileName: rec {
    name = "arc-swap";
    version = "0.4.8";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "dabe5a181f83789739c194cbe5a897dde195078fac08568d09221fd6137a7ba8"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".arc-swap."1.2.0" = overridableMkRustCrate (profileName: rec {
    name = "arc-swap";
    version = "1.2.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "d4d7d63395147b81a9e570bcc6243aaf71c017bd666d4909cfef0085bdda8d73"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".arrayref."0.3.6" = overridableMkRustCrate (profileName: rec {
    name = "arrayref";
    version = "0.3.6";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "a4c527152e37cf757a3f78aae5a06fbeefdb07ccc535c980a3208ee3060dd544"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".arrayvec."0.5.1" = overridableMkRustCrate (profileName: rec {
    name = "arrayvec";
    version = "0.5.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "cff77d8686867eceff3105329d4698d96c2391c176d5d03adc90c7389162b5b8"; };
    features = builtins.concatLists [
      [ "array-sizes-33-128" ]
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".ascii."0.9.3" = overridableMkRustCrate (profileName: rec {
    name = "ascii";
    version = "0.9.3";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "eab1c04a571841102f5345a8fc0f6bb3d31c315dec879b5c6e42e40ce7ffa34e"; };
    features = builtins.concatLists [
      [ "std" ]
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".assert_cmd."1.0.2" = overridableMkRustCrate (profileName: rec {
    name = "assert_cmd";
    version = "1.0.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "3dc1679af9a1ab4bea16f228b05d18f8363f8327b1fa8db00d2760cfafc6b61e"; };
    dependencies = {
      doc_comment = rustPackages."registry+https://github.com/rust-lang/crates.io-index".doc-comment."0.3.3" { inherit profileName; };
      predicates = rustPackages."registry+https://github.com/rust-lang/crates.io-index".predicates."1.0.4" { inherit profileName; };
      predicates_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".predicates-core."1.0.0" { inherit profileName; };
      predicates_tree = rustPackages."registry+https://github.com/rust-lang/crates.io-index".predicates-tree."1.0.0" { inherit profileName; };
      wait_timeout = rustPackages."registry+https://github.com/rust-lang/crates.io-index".wait-timeout."0.2.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".assert_matches."1.3.0" = overridableMkRustCrate (profileName: rec {
    name = "assert_matches";
    version = "1.3.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "7deb0a829ca7bcfaf5da70b073a8d128619259a7be8216a355e23f00763059e5"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".async-stream."0.3.0" = overridableMkRustCrate (profileName: rec {
    name = "async-stream";
    version = "0.3.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "3670df70cbc01729f901f94c887814b3c68db038aad1329a418bae178bc5295c"; };
    dependencies = {
      async_stream_impl = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".async-stream-impl."0.3.0" { profileName = "__noProfile"; };
      futures_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-core."0.3.8" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".async-stream-impl."0.3.0" = overridableMkRustCrate (profileName: rec {
    name = "async-stream-impl";
    version = "0.3.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "a3548b8efc9f8e8a5a0a2808c5bd8451a9031b9e5b879a79590304ae928b0a70"; };
    dependencies = {
      proc_macro2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."1.0.24" { inherit profileName; };
      quote = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quote."1.0.6" { inherit profileName; };
      syn = rustPackages."registry+https://github.com/rust-lang/crates.io-index".syn."1.0.60" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".async-trait."0.1.42" = overridableMkRustCrate (profileName: rec {
    name = "async-trait";
    version = "0.1.42";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "8d3a45e77e34375a7923b1e8febb049bb011f064714a8e17a1a616fef01da13d"; };
    dependencies = {
      proc_macro2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."1.0.24" { inherit profileName; };
      quote = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quote."1.0.6" { inherit profileName; };
      syn = rustPackages."registry+https://github.com/rust-lang/crates.io-index".syn."1.0.60" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".atty."0.2.14" = overridableMkRustCrate (profileName: rec {
    name = "atty";
    version = "0.2.14";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "d9b39be18770d11421cdb1b9947a45dd3f37e93092cbf377614828a319d5fee8"; };
    dependencies = {
      ${ if hostPlatform.parsed.kernel.name == "hermit" then "hermit_abi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".hermit-abi."0.1.13" { inherit profileName; };
      ${ if hostPlatform.isUnix then "libc" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      ${ if hostPlatform.isWindows then "winapi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.3.8" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".autocfg."0.1.7" = overridableMkRustCrate (profileName: rec {
    name = "autocfg";
    version = "0.1.7";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "1d49d90015b3c36167a20fe2810c5cd875ad504b39cff3d4eae7977e6b7c1cb2"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".autocfg."1.0.0" = overridableMkRustCrate (profileName: rec {
    name = "autocfg";
    version = "1.0.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "f8aac770f1885fd7e387acedd76065302551364496e46b3dd00860b2f8359b9d"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".backoff."0.3.0" = overridableMkRustCrate (profileName: rec {
    name = "backoff";
    version = "0.3.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "9fe17f59a06fe8b87a6fc8bf53bb70b3aba76d7685f432487a68cd5552853625"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "futures" ]
      [ "futures-core" ]
      [ "pin-project" ]
      [ "tokio" ]
      [ "tokio_1" ]
    ];
    dependencies = {
      futures_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-core."0.3.8" { inherit profileName; };
      getrandom = rustPackages."registry+https://github.com/rust-lang/crates.io-index".getrandom."0.2.0" { inherit profileName; };
      instant = rustPackages."registry+https://github.com/rust-lang/crates.io-index".instant."0.1.6" { inherit profileName; };
      pin_project = rustPackages."registry+https://github.com/rust-lang/crates.io-index".pin-project."1.0.1" { inherit profileName; };
      rand = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand."0.8.3" { inherit profileName; };
      tokio_1 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio."1.1.1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".backtrace."0.3.48" = overridableMkRustCrate (profileName: rec {
    name = "backtrace";
    version = "0.3.48";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "0df2f85c8a2abbe3b7d7e748052fdd9b76a0458fdeb16ad4223f5eca78c7c130"; };
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "addr2line")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "default")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "gimli-symbolize")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "object")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "serde")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "serialize-serde")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "std")
    ];
    dependencies = {
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "addr2line" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".addr2line."0.12.1" { inherit profileName; };
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "cfg_if" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".cfg-if."0.1.10" { inherit profileName; };
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "libc" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "object" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".object."0.19.0" { inherit profileName; };
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "rustc_demangle" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rustc-demangle."0.1.16" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "serde" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".base-x."0.2.6" = overridableMkRustCrate (profileName: rec {
    name = "base-x";
    version = "0.2.6";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "1b20b618342cf9891c292c4f5ac2cde7287cc5c87e87e9c769d617793607dec1"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".base32."0.4.0" = overridableMkRustCrate (profileName: rec {
    name = "base32";
    version = "0.4.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "23ce669cd6c8588f79e15cf450314f9638f967fc5770ff1c7c1deb0925ea7cfa"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".base64."0.9.3" = overridableMkRustCrate (profileName: rec {
    name = "base64";
    version = "0.9.3";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "489d6c0ed21b11d038c31b6ceccca973e65d73ba3bd8ecb9a2babf5546164643"; };
    dependencies = {
      byteorder = rustPackages."registry+https://github.com/rust-lang/crates.io-index".byteorder."1.3.4" { inherit profileName; };
      safemem = rustPackages."registry+https://github.com/rust-lang/crates.io-index".safemem."0.3.3" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".base64."0.10.1" = overridableMkRustCrate (profileName: rec {
    name = "base64";
    version = "0.10.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "0b25d992356d2eb0ed82172f5248873db5560c4721f564b13cb5193bda5e668e"; };
    dependencies = {
      byteorder = rustPackages."registry+https://github.com/rust-lang/crates.io-index".byteorder."1.3.4" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".base64."0.11.0" = overridableMkRustCrate (profileName: rec {
    name = "base64";
    version = "0.11.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "b41b7ea54a0c9d92199de89e20e58d49f02f8e699814ef3fdf266f6f748d15c7"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "std" ]
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".base64."0.12.3" = overridableMkRustCrate (profileName: rec {
    name = "base64";
    version = "0.12.3";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "3441f0f7b02788e948e47f457ca01f1d7e6d92c693bc132c22b087d3141c03ff"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "std" ]
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".base64."0.13.0" = overridableMkRustCrate (profileName: rec {
    name = "base64";
    version = "0.13.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "904dfeac50f3cdaba28fc6f57fdcddb75f49ed61346676a78c4ffe55877802fd"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "std" ]
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".bincode."1.3.1" = overridableMkRustCrate (profileName: rec {
    name = "bincode";
    version = "1.3.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "f30d3a39baa26f9651f17b375061f3233dde33424a8b72b0dbe93a68a0bc896d"; };
    dependencies = {
      byteorder = rustPackages."registry+https://github.com/rust-lang/crates.io-index".byteorder."1.3.4" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".bindgen."0.54.0" = overridableMkRustCrate (profileName: rec {
    name = "bindgen";
    version = "0.54.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "66c0bb6167449588ff70803f4127f0684f9063097eca5016f37eb52b92c2cf36"; };
    features = builtins.concatLists [
      [ "clap" ]
      [ "default" ]
      [ "env_logger" ]
      [ "log" ]
      [ "logging" ]
      [ "runtime" ]
      [ "which" ]
      [ "which-rustfmt" ]
    ];
    dependencies = {
      bitflags = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bitflags."1.2.1" { inherit profileName; };
      cexpr = rustPackages."registry+https://github.com/rust-lang/crates.io-index".cexpr."0.4.0" { inherit profileName; };
      cfg_if = rustPackages."registry+https://github.com/rust-lang/crates.io-index".cfg-if."0.1.10" { inherit profileName; };
      clang_sys = rustPackages."registry+https://github.com/rust-lang/crates.io-index".clang-sys."0.29.3" { inherit profileName; };
      clap = rustPackages."registry+https://github.com/rust-lang/crates.io-index".clap."2.33.3" { inherit profileName; };
      env_logger = rustPackages."registry+https://github.com/rust-lang/crates.io-index".env_logger."0.7.1" { inherit profileName; };
      lazy_static = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
      lazycell = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazycell."1.2.1" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      peeking_take_while = rustPackages."registry+https://github.com/rust-lang/crates.io-index".peeking_take_while."0.1.2" { inherit profileName; };
      proc_macro2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."1.0.24" { inherit profileName; };
      quote = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quote."1.0.6" { inherit profileName; };
      regex = rustPackages."registry+https://github.com/rust-lang/crates.io-index".regex."1.3.9" { inherit profileName; };
      rustc_hash = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rustc-hash."1.1.0" { inherit profileName; };
      shlex = rustPackages."registry+https://github.com/rust-lang/crates.io-index".shlex."0.1.1" { inherit profileName; };
      which = rustPackages."registry+https://github.com/rust-lang/crates.io-index".which."3.1.1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".bitflags."1.2.1" = overridableMkRustCrate (profileName: rec {
    name = "bitflags";
    version = "1.2.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "cf1de2fe8c75bc145a2f577add951f8134889b4795d47466a54a5c846d691693"; };
    features = builtins.concatLists [
      [ "default" ]
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".blake2b_simd."0.5.10" = overridableMkRustCrate (profileName: rec {
    name = "blake2b_simd";
    version = "0.5.10";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "d8fb2d74254a3a0b5cac33ac9f8ed0e44aa50378d9dbb2e5d83bd21ed1dc2c8a"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "std" ]
    ];
    dependencies = {
      arrayref = rustPackages."registry+https://github.com/rust-lang/crates.io-index".arrayref."0.3.6" { inherit profileName; };
      arrayvec = rustPackages."registry+https://github.com/rust-lang/crates.io-index".arrayvec."0.5.1" { inherit profileName; };
      constant_time_eq = rustPackages."registry+https://github.com/rust-lang/crates.io-index".constant_time_eq."0.1.5" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".blake3."0.3.7" = overridableMkRustCrate (profileName: rec {
    name = "blake3";
    version = "0.3.7";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "e9ff35b701f3914bdb8fad3368d822c766ef2858b2583198e41639b936f09d3f"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "std" ]
    ];
    dependencies = {
      arrayref = rustPackages."registry+https://github.com/rust-lang/crates.io-index".arrayref."0.3.6" { inherit profileName; };
      arrayvec = rustPackages."registry+https://github.com/rust-lang/crates.io-index".arrayvec."0.5.1" { inherit profileName; };
      cfg_if = rustPackages."registry+https://github.com/rust-lang/crates.io-index".cfg-if."0.1.10" { inherit profileName; };
      constant_time_eq = rustPackages."registry+https://github.com/rust-lang/crates.io-index".constant_time_eq."0.1.5" { inherit profileName; };
      crypto_mac = rustPackages."registry+https://github.com/rust-lang/crates.io-index".crypto-mac."0.8.0" { inherit profileName; };
      digest = rustPackages."registry+https://github.com/rust-lang/crates.io-index".digest."0.9.0" { inherit profileName; };
    };
    buildDependencies = {
      cc = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".cc."1.0.49" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".block-buffer."0.7.3" = overridableMkRustCrate (profileName: rec {
    name = "block-buffer";
    version = "0.7.3";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "c0940dc441f31689269e10ac70eb1002a3a1d3ad1390e030043662eb7fe4688b"; };
    dependencies = {
      block_padding = rustPackages."registry+https://github.com/rust-lang/crates.io-index".block-padding."0.1.5" { inherit profileName; };
      byte_tools = rustPackages."registry+https://github.com/rust-lang/crates.io-index".byte-tools."0.3.1" { inherit profileName; };
      byteorder = rustPackages."registry+https://github.com/rust-lang/crates.io-index".byteorder."1.3.4" { inherit profileName; };
      generic_array = rustPackages."registry+https://github.com/rust-lang/crates.io-index".generic-array."0.12.3" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".block-buffer."0.9.0" = overridableMkRustCrate (profileName: rec {
    name = "block-buffer";
    version = "0.9.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "4152116fd6e9dadb291ae18fc1ec3575ed6d84c29642d97890f4b4a3417297e4"; };
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/sha3" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "block-padding")
    ];
    dependencies = {
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/sha3" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "block_padding" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".block-padding."0.2.1" { inherit profileName; };
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "generic_array" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".generic-array."0.14.3" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".block-padding."0.1.5" = overridableMkRustCrate (profileName: rec {
    name = "block-padding";
    version = "0.1.5";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "fa79dedbb091f449f1f39e53edf88d5dbe95f895dae6135a8d7b881fb5af73f5"; };
    dependencies = {
      byte_tools = rustPackages."registry+https://github.com/rust-lang/crates.io-index".byte-tools."0.3.1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".block-padding."0.2.1" = overridableMkRustCrate (profileName: rec {
    name = "block-padding";
    version = "0.2.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "8d696c370c750c948ada61c69a0ee2cbbb9c50b1019ddb86d9317157a99c2cae"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".borsh."0.8.1" = overridableMkRustCrate (profileName: rec {
    name = "borsh";
    version = "0.8.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "a5a26c53ddf60281f18e7a29b20db7ba3db82a9d81b9650bfaa02d646f50d364"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "std" ]
    ];
    dependencies = {
      borsh_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".borsh-derive."0.8.1" { profileName = "__noProfile"; };
      hashbrown = rustPackages."registry+https://github.com/rust-lang/crates.io-index".hashbrown."0.9.1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".borsh-derive."0.8.1" = overridableMkRustCrate (profileName: rec {
    name = "borsh-derive";
    version = "0.8.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "b637a47728b78a78cd7f4b85bf06d71ef4221840e059a38f048be2422bf673b2"; };
    dependencies = {
      borsh_derive_internal = rustPackages."registry+https://github.com/rust-lang/crates.io-index".borsh-derive-internal."0.8.1" { inherit profileName; };
      borsh_schema_derive_internal = rustPackages."registry+https://github.com/rust-lang/crates.io-index".borsh-schema-derive-internal."0.8.1" { inherit profileName; };
      proc_macro_crate = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro-crate."0.1.5" { inherit profileName; };
      proc_macro2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."1.0.24" { inherit profileName; };
      syn = rustPackages."registry+https://github.com/rust-lang/crates.io-index".syn."1.0.60" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".borsh-derive-internal."0.8.1" = overridableMkRustCrate (profileName: rec {
    name = "borsh-derive-internal";
    version = "0.8.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "d813fa25eb0bed78c36492cff4415f38c760d6de833d255ba9095bd8ebb7d725"; };
    dependencies = {
      proc_macro2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."1.0.24" { inherit profileName; };
      quote = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quote."1.0.6" { inherit profileName; };
      syn = rustPackages."registry+https://github.com/rust-lang/crates.io-index".syn."1.0.60" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".borsh-schema-derive-internal."0.8.1" = overridableMkRustCrate (profileName: rec {
    name = "borsh-schema-derive-internal";
    version = "0.8.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "dcf78ee4a98c8cb9eba1bac3d3e2a1ea3d7673c719ce691e67b5cbafc472d3b7"; };
    dependencies = {
      proc_macro2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."1.0.24" { inherit profileName; };
      quote = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quote."1.0.6" { inherit profileName; };
      syn = rustPackages."registry+https://github.com/rust-lang/crates.io-index".syn."1.0.60" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".bs58."0.3.1" = overridableMkRustCrate (profileName: rec {
    name = "bs58";
    version = "0.3.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "476e9cd489f9e121e02ffa6014a8ef220ecb15c05ed23fc34cca13925dc283fb"; };
    features = builtins.concatLists [
      [ "alloc" ]
      [ "default" ]
      [ "std" ]
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".bstr."0.2.13" = overridableMkRustCrate (profileName: rec {
    name = "bstr";
    version = "0.2.13";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "31accafdb70df7871592c058eca3985b71104e15ac32f64706022c58867da931"; };
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-scripts" || rootFeatures' ? "solana-tokens") "default")
      (lib.optional (rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-scripts" || rootFeatures' ? "solana-tokens") "lazy_static")
      (lib.optional (rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-scripts" || rootFeatures' ? "solana-tokens") "regex-automata")
      (lib.optional (rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-scripts" || rootFeatures' ? "solana-tokens") "serde")
      (lib.optional (rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-scripts" || rootFeatures' ? "solana-tokens") "serde1")
      (lib.optional (rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-scripts" || rootFeatures' ? "solana-tokens") "serde1-nostd")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-scripts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "std")
      (lib.optional (rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-scripts" || rootFeatures' ? "solana-tokens") "unicode")
    ];
    dependencies = {
      ${ if rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-scripts" || rootFeatures' ? "solana-tokens" then "lazy_static" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-scripts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" then "memchr" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".memchr."2.3.3" { inherit profileName; };
      ${ if rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-scripts" || rootFeatures' ? "solana-tokens" then "regex_automata" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".regex-automata."0.1.9" { inherit profileName; };
      ${ if rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-scripts" || rootFeatures' ? "solana-tokens" then "serde" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".bumpalo."3.3.0" = overridableMkRustCrate (profileName: rec {
    name = "bumpalo";
    version = "3.3.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "5356f1d23ee24a1f785a56d1d1a5f0fd5b0f6a0c0fb2412ce11da71649ab78f6"; };
    features = builtins.concatLists [
      [ "default" ]
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".bv."0.11.1" = overridableMkRustCrate (profileName: rec {
    name = "bv";
    version = "0.11.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "8834bb1d8ee5dc048ee3124f2c7c1afcc6bc9aed03f11e9dfd8c69470a5db340"; };
    features = builtins.concatLists [
      [ "serde" ]
    ];
    dependencies = {
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
    };
    buildDependencies = {
      feature_probe = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".feature-probe."0.1.1" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".byte-tools."0.3.1" = overridableMkRustCrate (profileName: rec {
    name = "byte-tools";
    version = "0.3.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "e3b5ca7a04898ad4bcd41c90c5285445ff5b791899bb1b0abdd2a2aa791211d7"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".byte-unit."4.0.9" = overridableMkRustCrate (profileName: rec {
    name = "byte-unit";
    version = "4.0.9";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "1c8758c32833faaae35b24a73d332e62d0528e89076ae841c63940e37008b153"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "std" ]
      [ "u128" ]
    ];
    dependencies = {
      utf8_width = rustPackages."registry+https://github.com/rust-lang/crates.io-index".utf8-width."0.1.3" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".bytecount."0.6.0" = overridableMkRustCrate (profileName: rec {
    name = "bytecount";
    version = "0.6.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "b0017894339f586ccb943b01b9555de56770c11cda818e7e3d8bd93f4ed7f46e"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".byteorder."1.3.4" = overridableMkRustCrate (profileName: rec {
    name = "byteorder";
    version = "1.3.4";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "08c48aae112d48ed9f069b33538ea9e3e90aa263cfa3d1c24309612b1f7472de"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "i128" ]
      [ "std" ]
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".bytes."0.4.12" = overridableMkRustCrate (profileName: rec {
    name = "bytes";
    version = "0.4.12";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "206fdffcfa2df7cbe15601ef46c813fce0965eb3286db6b56c583b814b51c81c"; };
    features = builtins.concatLists [
      [ "either" ]
    ];
    dependencies = {
      byteorder = rustPackages."registry+https://github.com/rust-lang/crates.io-index".byteorder."1.3.4" { inherit profileName; };
      either = rustPackages."registry+https://github.com/rust-lang/crates.io-index".either."1.5.3" { inherit profileName; };
      iovec = rustPackages."registry+https://github.com/rust-lang/crates.io-index".iovec."0.1.4" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".bytes."0.5.4" = overridableMkRustCrate (profileName: rec {
    name = "bytes";
    version = "0.5.4";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "130aac562c0dd69c56b3b1cc8ffd2e17be31d0b6c25b61c96b76231aa23e39e1"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "std" ]
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".bytes."1.0.1" = overridableMkRustCrate (profileName: rec {
    name = "bytes";
    version = "1.0.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "b700ce4376041dcd0a327fd0097c41095743c4c8af8887265942faf1100bd040"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "std" ]
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".bytesize."1.0.1" = overridableMkRustCrate (profileName: rec {
    name = "bytesize";
    version = "1.0.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "81a18687293a1546b67c246452202bbbf143d239cb43494cc163da14979082da"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".bzip2."0.3.3" = overridableMkRustCrate (profileName: rec {
    name = "bzip2";
    version = "0.3.3";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "42b7c3cbf0fa9c1b82308d57191728ca0256cb821220f4e2fd410a72ade26e3b"; };
    dependencies = {
      bzip2_sys = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bzip2-sys."0.1.9+1.0.8" { inherit profileName; };
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".bzip2-sys."0.1.9+1.0.8" = overridableMkRustCrate (profileName: rec {
    name = "bzip2-sys";
    version = "0.1.9+1.0.8";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "ad3b39a260062fca31f7b0b12f207e8f2590a67d32ec7d59c20484b07ea7285e"; };
    dependencies = {
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
    };
    buildDependencies = {
      cc = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".cc."1.0.49" { profileName = "__noProfile"; };
      pkg_config = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".pkg-config."0.3.17" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".cargo_metadata."0.12.0" = overridableMkRustCrate (profileName: rec {
    name = "cargo_metadata";
    version = "0.12.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "d5a5f7b42f606b7f23674f6f4d877628350682bc40687d3fae65679a58d55345"; };
    features = builtins.concatLists [
      [ "default" ]
    ];
    dependencies = {
      semver = rustPackages."registry+https://github.com/rust-lang/crates.io-index".semver."0.11.0" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      serde_json = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_json."1.0.62" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".cast."0.2.3" = overridableMkRustCrate (profileName: rec {
    name = "cast";
    version = "0.2.3";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "4b9434b9a5aa1450faa3f9cb14ea0e8c53bb5d2b3c1bfd1ab4fc03e9f33fbfb0"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "std" ]
    ];
    buildDependencies = {
      rustc_version = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".rustc_version."0.2.3" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".cc."1.0.49" = overridableMkRustCrate (profileName: rec {
    name = "cc";
    version = "1.0.49";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "e450b8da92aa6f274e7c6437692f9f2ce6d701fb73bacfcf87897b3f89a4c20e"; };
    features = builtins.concatLists [
      [ "jobserver" ]
      [ "num_cpus" ]
      [ "parallel" ]
    ];
    dependencies = {
      jobserver = rustPackages."registry+https://github.com/rust-lang/crates.io-index".jobserver."0.1.21" { inherit profileName; };
      num_cpus = rustPackages."registry+https://github.com/rust-lang/crates.io-index".num_cpus."1.13.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".cexpr."0.4.0" = overridableMkRustCrate (profileName: rec {
    name = "cexpr";
    version = "0.4.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "f4aedb84272dbe89af497cf81375129abda4fc0a9e7c5d317498c15cc30c0d27"; };
    dependencies = {
      nom = rustPackages."registry+https://github.com/rust-lang/crates.io-index".nom."5.1.1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".cfg-if."0.1.10" = overridableMkRustCrate (profileName: rec {
    name = "cfg-if";
    version = "0.1.10";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "4785bdd1c96b2a846b2bd7cc02e86b6b3dbf14e7e53446c4f54c92a361040822"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".cfg-if."1.0.0" = overridableMkRustCrate (profileName: rec {
    name = "cfg-if";
    version = "1.0.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "baf1de4339761588bc0619e3cbc0120ee582ebb74b53b4efbf79117bd2da40fd"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".chrono."0.4.19" = overridableMkRustCrate (profileName: rec {
    name = "chrono";
    version = "0.4.19";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "670ad68c9088c2a963aaa298cb369688cf3f9465ce5e2d4ca10e6e0098a1ce73"; };
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-sdk/chrono" || rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "clock")
      (lib.optional (rootFeatures' ? "solana-sdk/chrono" || rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "default")
      (lib.optional (rootFeatures' ? "solana-sdk/chrono" || rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "libc")
      (lib.optional (rootFeatures' ? "solana-sdk/chrono" || rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "oldtime")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-watchtower") "serde")
      (lib.optional (rootFeatures' ? "solana-sdk/chrono" || rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "std")
      (lib.optional (rootFeatures' ? "solana-sdk/chrono" || rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "time")
      (lib.optional (rootFeatures' ? "solana-sdk/chrono" || rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "winapi")
    ];
    dependencies = {
      ${ if rootFeatures' ? "solana-sdk/chrono" || rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "libc" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/chrono" || rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "num_integer" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".num-integer."0.1.42" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/chrono" || rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "num_traits" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".num-traits."0.2.11" { inherit profileName; };
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-watchtower" then "serde" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/chrono" || rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "time" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".time."0.1.43" { inherit profileName; };
      ${ if (rootFeatures' ? "solana-sdk/chrono" || rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") && hostPlatform.isWindows then "winapi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.3.8" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".chrono-humanize."0.1.1" = overridableMkRustCrate (profileName: rec {
    name = "chrono-humanize";
    version = "0.1.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "f0a4c32145b4db85fe1c4f2b125a4f9493769df424f5f84baf6b04ea8eaf33c9"; };
    dependencies = {
      chrono = rustPackages."registry+https://github.com/rust-lang/crates.io-index".chrono."0.4.19" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".clang-sys."0.29.3" = overridableMkRustCrate (profileName: rec {
    name = "clang-sys";
    version = "0.29.3";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "fe6837df1d5cba2397b835c8530f51723267e16abbf83892e9e5af4f0e5dd10a"; };
    features = builtins.concatLists [
      [ "clang_6_0" ]
      [ "gte_clang_3_6" ]
      [ "gte_clang_3_7" ]
      [ "gte_clang_3_8" ]
      [ "gte_clang_3_9" ]
      [ "gte_clang_4_0" ]
      [ "gte_clang_5_0" ]
      [ "gte_clang_6_0" ]
      [ "libloading" ]
      [ "runtime" ]
    ];
    dependencies = {
      glob = rustPackages."registry+https://github.com/rust-lang/crates.io-index".glob."0.3.0" { inherit profileName; };
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      libloading = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libloading."0.5.2" { inherit profileName; };
    };
    buildDependencies = {
      glob = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".glob."0.3.0" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".clap."2.33.3" = overridableMkRustCrate (profileName: rec {
    name = "clap";
    version = "2.33.3";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "37e58ac78573c40708d45522f0d80fa2f01cc4f9b4e2bf749807255454312002"; };
    features = builtins.concatLists [
      [ "ansi_term" ]
      [ "atty" ]
      [ "color" ]
      [ "default" ]
      [ "strsim" ]
      [ "suggestions" ]
      [ "vec_map" ]
    ];
    dependencies = {
      ${ if !hostPlatform.isWindows then "ansi_term" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".ansi_term."0.11.0" { inherit profileName; };
      atty = rustPackages."registry+https://github.com/rust-lang/crates.io-index".atty."0.2.14" { inherit profileName; };
      bitflags = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bitflags."1.2.1" { inherit profileName; };
      strsim = rustPackages."registry+https://github.com/rust-lang/crates.io-index".strsim."0.8.0" { inherit profileName; };
      textwrap = rustPackages."registry+https://github.com/rust-lang/crates.io-index".textwrap."0.11.0" { inherit profileName; };
      unicode_width = rustPackages."registry+https://github.com/rust-lang/crates.io-index".unicode-width."0.1.7" { inherit profileName; };
      vec_map = rustPackages."registry+https://github.com/rust-lang/crates.io-index".vec_map."0.8.2" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".cloudabi."0.0.3" = overridableMkRustCrate (profileName: rec {
    name = "cloudabi";
    version = "0.0.3";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "ddfc5b9aa5d4507acaf872de71051dfd0e309860e88966e1051e462a077aac4f"; };
    features = builtins.concatLists [
      [ "bitflags" ]
      [ "default" ]
    ];
    dependencies = {
      bitflags = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bitflags."1.2.1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".cloudabi."0.1.0" = overridableMkRustCrate (profileName: rec {
    name = "cloudabi";
    version = "0.1.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "4344512281c643ae7638bbabc3af17a11307803ec8f0fcad9fae512a8bf36467"; };
    features = builtins.concatLists [
      [ "bitflags" ]
      [ "default" ]
    ];
    dependencies = {
      bitflags = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bitflags."1.2.1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".combine."3.8.1" = overridableMkRustCrate (profileName: rec {
    name = "combine";
    version = "3.8.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "da3da6baa321ec19e1cc41d31bf599f00c783d0517095cdaf0332e3fe8d20680"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "std" ]
    ];
    dependencies = {
      ascii = rustPackages."registry+https://github.com/rust-lang/crates.io-index".ascii."0.9.3" { inherit profileName; };
      byteorder = rustPackages."registry+https://github.com/rust-lang/crates.io-index".byteorder."1.3.4" { inherit profileName; };
      either = rustPackages."registry+https://github.com/rust-lang/crates.io-index".either."1.5.3" { inherit profileName; };
      memchr = rustPackages."registry+https://github.com/rust-lang/crates.io-index".memchr."2.3.3" { inherit profileName; };
      unreachable = rustPackages."registry+https://github.com/rust-lang/crates.io-index".unreachable."1.0.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".console."0.11.3" = overridableMkRustCrate (profileName: rec {
    name = "console";
    version = "0.11.3";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "8c0994e656bba7b922d8dd1245db90672ffb701e684e45be58f20719d69abc5a"; };
    features = builtins.concatLists [
      [ "ansi-parsing" ]
      [ "default" ]
      [ "regex" ]
      [ "unicode-width" ]
      [ "winapi-util" ]
      [ "windows-console-colors" ]
    ];
    dependencies = {
      ${ if hostPlatform.isWindows then "encode_unicode" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".encode_unicode."0.3.6" { inherit profileName; };
      lazy_static = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      regex = rustPackages."registry+https://github.com/rust-lang/crates.io-index".regex."1.3.9" { inherit profileName; };
      terminal_size = rustPackages."registry+https://github.com/rust-lang/crates.io-index".terminal_size."0.1.12" { inherit profileName; };
      ${ if hostPlatform.isUnix then "termios" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".termios."0.3.2" { inherit profileName; };
      unicode_width = rustPackages."registry+https://github.com/rust-lang/crates.io-index".unicode-width."0.1.7" { inherit profileName; };
      ${ if hostPlatform.isWindows then "winapi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.3.8" { inherit profileName; };
      ${ if hostPlatform.isWindows then "winapi_util" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi-util."0.1.5" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".const_fn."0.4.5" = overridableMkRustCrate (profileName: rec {
    name = "const_fn";
    version = "0.4.5";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "28b9d6de7f49e22cf97ad17fc4036ece69300032f45f78f30b4a4482cdc3f4a6"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".constant_time_eq."0.1.5" = overridableMkRustCrate (profileName: rec {
    name = "constant_time_eq";
    version = "0.1.5";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "245097e9a4535ee1e3e3931fcfcd55a796a44c643e8596ff6566d68f09b87bbc"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".core-foundation."0.9.1" = overridableMkRustCrate (profileName: rec {
    name = "core-foundation";
    version = "0.9.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "0a89e2ae426ea83155dccf10c0fa6b1463ef6d5fcb44cee0b224a408fa640a62"; };
    dependencies = {
      core_foundation_sys = rustPackages."registry+https://github.com/rust-lang/crates.io-index".core-foundation-sys."0.8.2" { inherit profileName; };
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".core-foundation-sys."0.8.2" = overridableMkRustCrate (profileName: rec {
    name = "core-foundation-sys";
    version = "0.8.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "ea221b5284a47e40033bf9b66f35f984ec0ea2931eb03505246cd27a963f981b"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".core_affinity."0.5.10" = overridableMkRustCrate (profileName: rec {
    name = "core_affinity";
    version = "0.5.10";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "7f8a03115cc34fb0d7c321dd154a3914b3ca082ccc5c11d91bf7117dbbe7171f"; };
    dependencies = {
      ${ if hostPlatform.parsed.kernel.name == "windows" then "kernel32" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".kernel32-sys."0.2.2" { inherit profileName; };
      ${ if hostPlatform.parsed.kernel.name == "linux" || hostPlatform.parsed.kernel.name == "darwin" then "libc" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      num_cpus = rustPackages."registry+https://github.com/rust-lang/crates.io-index".num_cpus."1.13.0" { inherit profileName; };
      ${ if hostPlatform.parsed.kernel.name == "windows" then "winapi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.2.8" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".cpuid-bool."0.1.2" = overridableMkRustCrate (profileName: rec {
    name = "cpuid-bool";
    version = "0.1.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "8aebca1129a03dc6dc2b127edd729435bbc4a37e1d5f4d7513165089ceb02634"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".crc32fast."1.2.0" = overridableMkRustCrate (profileName: rec {
    name = "crc32fast";
    version = "1.2.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "ba125de2af0df55319f41944744ad91c71113bf74a4646efff39afe1f6842db1"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "std" ]
    ];
    dependencies = {
      cfg_if = rustPackages."registry+https://github.com/rust-lang/crates.io-index".cfg-if."0.1.10" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".criterion-stats."0.3.0" = overridableMkRustCrate (profileName: rec {
    name = "criterion-stats";
    version = "0.3.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "387df94cb74ada1b33e10ce034bb0d9360cc73edb5063e7d7d4120a40ee1c9d2"; };
    dependencies = {
      cast = rustPackages."registry+https://github.com/rust-lang/crates.io-index".cast."0.2.3" { inherit profileName; };
      num_traits = rustPackages."registry+https://github.com/rust-lang/crates.io-index".num-traits."0.2.11" { inherit profileName; };
      num_cpus = rustPackages."registry+https://github.com/rust-lang/crates.io-index".num_cpus."1.13.0" { inherit profileName; };
      rand = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand."0.4.6" { inherit profileName; };
      thread_scoped = rustPackages."registry+https://github.com/rust-lang/crates.io-index".thread-scoped."1.0.2" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".crossbeam-channel."0.4.4" = overridableMkRustCrate (profileName: rec {
    name = "crossbeam-channel";
    version = "0.4.4";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "b153fe7cbef478c567df0f972e02e6d736db11affe43dfc9c56a9374d1adfb87"; };
    dependencies = {
      crossbeam_utils = rustPackages."registry+https://github.com/rust-lang/crates.io-index".crossbeam-utils."0.7.2" { inherit profileName; };
      maybe_uninit = rustPackages."registry+https://github.com/rust-lang/crates.io-index".maybe-uninit."2.0.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".crossbeam-channel."0.5.0" = overridableMkRustCrate (profileName: rec {
    name = "crossbeam-channel";
    version = "0.5.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "dca26ee1f8d361640700bde38b2c37d8c22b3ce2d360e1fc1c74ea4b0aa7d775"; };
    features = builtins.concatLists [
      [ "crossbeam-utils" ]
      [ "default" ]
      [ "std" ]
    ];
    dependencies = {
      cfg_if = rustPackages."registry+https://github.com/rust-lang/crates.io-index".cfg-if."1.0.0" { inherit profileName; };
      crossbeam_utils = rustPackages."registry+https://github.com/rust-lang/crates.io-index".crossbeam-utils."0.8.1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".crossbeam-deque."0.7.3" = overridableMkRustCrate (profileName: rec {
    name = "crossbeam-deque";
    version = "0.7.3";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "9f02af974daeee82218205558e51ec8768b48cf524bd01d550abe5573a608285"; };
    dependencies = {
      crossbeam_epoch = rustPackages."registry+https://github.com/rust-lang/crates.io-index".crossbeam-epoch."0.8.2" { inherit profileName; };
      crossbeam_utils = rustPackages."registry+https://github.com/rust-lang/crates.io-index".crossbeam-utils."0.7.2" { inherit profileName; };
      maybe_uninit = rustPackages."registry+https://github.com/rust-lang/crates.io-index".maybe-uninit."2.0.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".crossbeam-deque."0.8.0" = overridableMkRustCrate (profileName: rec {
    name = "crossbeam-deque";
    version = "0.8.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "94af6efb46fef72616855b036a624cf27ba656ffc9be1b9a3c931cfc7749a9a9"; };
    features = builtins.concatLists [
      [ "crossbeam-epoch" ]
      [ "crossbeam-utils" ]
      [ "default" ]
      [ "std" ]
    ];
    dependencies = {
      cfg_if = rustPackages."registry+https://github.com/rust-lang/crates.io-index".cfg-if."1.0.0" { inherit profileName; };
      crossbeam_epoch = rustPackages."registry+https://github.com/rust-lang/crates.io-index".crossbeam-epoch."0.9.1" { inherit profileName; };
      crossbeam_utils = rustPackages."registry+https://github.com/rust-lang/crates.io-index".crossbeam-utils."0.8.1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".crossbeam-epoch."0.8.2" = overridableMkRustCrate (profileName: rec {
    name = "crossbeam-epoch";
    version = "0.8.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "058ed274caafc1f60c4997b5fc07bf7dc7cca454af7c6e81edffe5f33f70dace"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "lazy_static" ]
      [ "std" ]
    ];
    dependencies = {
      cfg_if = rustPackages."registry+https://github.com/rust-lang/crates.io-index".cfg-if."0.1.10" { inherit profileName; };
      crossbeam_utils = rustPackages."registry+https://github.com/rust-lang/crates.io-index".crossbeam-utils."0.7.2" { inherit profileName; };
      lazy_static = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
      maybe_uninit = rustPackages."registry+https://github.com/rust-lang/crates.io-index".maybe-uninit."2.0.0" { inherit profileName; };
      memoffset = rustPackages."registry+https://github.com/rust-lang/crates.io-index".memoffset."0.5.4" { inherit profileName; };
      scopeguard = rustPackages."registry+https://github.com/rust-lang/crates.io-index".scopeguard."1.1.0" { inherit profileName; };
    };
    buildDependencies = {
      autocfg = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".autocfg."1.0.0" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".crossbeam-epoch."0.9.1" = overridableMkRustCrate (profileName: rec {
    name = "crossbeam-epoch";
    version = "0.9.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "a1aaa739f95311c2c7887a76863f500026092fb1dce0161dab577e559ef3569d"; };
    features = builtins.concatLists [
      [ "alloc" ]
      [ "lazy_static" ]
      [ "std" ]
    ];
    dependencies = {
      cfg_if = rustPackages."registry+https://github.com/rust-lang/crates.io-index".cfg-if."1.0.0" { inherit profileName; };
      const_fn = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".const_fn."0.4.5" { profileName = "__noProfile"; };
      crossbeam_utils = rustPackages."registry+https://github.com/rust-lang/crates.io-index".crossbeam-utils."0.8.1" { inherit profileName; };
      lazy_static = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
      memoffset = rustPackages."registry+https://github.com/rust-lang/crates.io-index".memoffset."0.6.1" { inherit profileName; };
      scopeguard = rustPackages."registry+https://github.com/rust-lang/crates.io-index".scopeguard."1.1.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".crossbeam-queue."0.2.3" = overridableMkRustCrate (profileName: rec {
    name = "crossbeam-queue";
    version = "0.2.3";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "774ba60a54c213d409d5353bda12d49cd68d14e45036a285234c8d6f91f92570"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "std" ]
    ];
    dependencies = {
      cfg_if = rustPackages."registry+https://github.com/rust-lang/crates.io-index".cfg-if."0.1.10" { inherit profileName; };
      crossbeam_utils = rustPackages."registry+https://github.com/rust-lang/crates.io-index".crossbeam-utils."0.7.2" { inherit profileName; };
      maybe_uninit = rustPackages."registry+https://github.com/rust-lang/crates.io-index".maybe-uninit."2.0.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".crossbeam-utils."0.7.2" = overridableMkRustCrate (profileName: rec {
    name = "crossbeam-utils";
    version = "0.7.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "c3c7c73a2d1e9fc0886a08b93e98eb643461230d5f1925e4036204d5f2e261a8"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "lazy_static" ]
      [ "std" ]
    ];
    dependencies = {
      cfg_if = rustPackages."registry+https://github.com/rust-lang/crates.io-index".cfg-if."0.1.10" { inherit profileName; };
      lazy_static = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
    };
    buildDependencies = {
      autocfg = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".autocfg."1.0.0" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".crossbeam-utils."0.8.1" = overridableMkRustCrate (profileName: rec {
    name = "crossbeam-utils";
    version = "0.8.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "02d96d1e189ef58269ebe5b97953da3274d83a93af647c2ddd6f9dab28cedb8d"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "lazy_static" ]
      [ "std" ]
    ];
    dependencies = {
      cfg_if = rustPackages."registry+https://github.com/rust-lang/crates.io-index".cfg-if."1.0.0" { inherit profileName; };
      lazy_static = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
    };
    buildDependencies = {
      autocfg = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".autocfg."1.0.0" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".crunchy."0.2.2" = overridableMkRustCrate (profileName: rec {
    name = "crunchy";
    version = "0.2.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "7a81dae078cea95a014a339291cec439d2f232ebe854a9d672b796c6afafa9b7"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "limit_128" ]
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".crypto-mac."0.7.0" = overridableMkRustCrate (profileName: rec {
    name = "crypto-mac";
    version = "0.7.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "4434400df11d95d556bac068ddfedd482915eb18fe8bea89bc80b6e4b1c179e5"; };
    dependencies = {
      generic_array = rustPackages."registry+https://github.com/rust-lang/crates.io-index".generic-array."0.12.3" { inherit profileName; };
      subtle = rustPackages."registry+https://github.com/rust-lang/crates.io-index".subtle."1.0.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".crypto-mac."0.8.0" = overridableMkRustCrate (profileName: rec {
    name = "crypto-mac";
    version = "0.8.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "b584a330336237c1eecd3e94266efb216c56ed91225d634cb2991c5f3fd1aeab"; };
    dependencies = {
      generic_array = rustPackages."registry+https://github.com/rust-lang/crates.io-index".generic-array."0.14.3" { inherit profileName; };
      subtle = rustPackages."registry+https://github.com/rust-lang/crates.io-index".subtle."2.2.2" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".crypto-mac."0.10.0" = overridableMkRustCrate (profileName: rec {
    name = "crypto-mac";
    version = "0.10.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "4857fd85a0c34b3c3297875b747c1e02e06b6a0ea32dd892d8192b9ce0813ea6"; };
    dependencies = {
      generic_array = rustPackages."registry+https://github.com/rust-lang/crates.io-index".generic-array."0.14.3" { inherit profileName; };
      subtle = rustPackages."registry+https://github.com/rust-lang/crates.io-index".subtle."2.2.2" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".csv."1.1.3" = overridableMkRustCrate (profileName: rec {
    name = "csv";
    version = "1.1.3";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "00affe7f6ab566df61b4be3ce8cf16bc2576bca0963ceb0955e45d514bf9a279"; };
    dependencies = {
      bstr = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bstr."0.2.13" { inherit profileName; };
      csv_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".csv-core."0.1.10" { inherit profileName; };
      itoa = rustPackages."registry+https://github.com/rust-lang/crates.io-index".itoa."0.4.5" { inherit profileName; };
      ryu = rustPackages."registry+https://github.com/rust-lang/crates.io-index".ryu."1.0.4" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".csv-core."0.1.10" = overridableMkRustCrate (profileName: rec {
    name = "csv-core";
    version = "0.1.10";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "2b2466559f260f48ad25fe6317b3c8dac77b5bdb5763ac7d9d6103530663bc90"; };
    features = builtins.concatLists [
      [ "default" ]
    ];
    dependencies = {
      memchr = rustPackages."registry+https://github.com/rust-lang/crates.io-index".memchr."2.3.3" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".ctrlc."3.1.5" = overridableMkRustCrate (profileName: rec {
    name = "ctrlc";
    version = "3.1.5";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "54dedab740bc412d514cfbc4a1d9d5d16fed02c4b14a7be129003c07fdc33b9b"; };
    features = builtins.concatLists [
      [ "termination" ]
    ];
    dependencies = {
      ${ if hostPlatform.isUnix then "nix" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".nix."0.17.0" { inherit profileName; };
      ${ if hostPlatform.isWindows then "winapi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.3.8" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".curve25519-dalek."2.1.0" = overridableMkRustCrate (profileName: rec {
    name = "curve25519-dalek";
    version = "2.1.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "5d85653f070353a16313d0046f173f70d1aadd5b42600a14de626f0dfb3473a5"; };
    features = builtins.concatLists [
      [ "alloc" ]
      [ "default" ]
      [ "std" ]
      [ "u64_backend" ]
    ];
    dependencies = {
      byteorder = rustPackages."registry+https://github.com/rust-lang/crates.io-index".byteorder."1.3.4" { inherit profileName; };
      digest = rustPackages."registry+https://github.com/rust-lang/crates.io-index".digest."0.8.1" { inherit profileName; };
      rand_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand_core."0.5.1" { inherit profileName; };
      subtle = rustPackages."registry+https://github.com/rust-lang/crates.io-index".subtle."2.2.2" { inherit profileName; };
      zeroize = rustPackages."registry+https://github.com/rust-lang/crates.io-index".zeroize."1.2.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".curve25519-dalek."3.0.0" = overridableMkRustCrate (profileName: rec {
    name = "curve25519-dalek";
    version = "3.0.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "c8492de420e9e60bc9a1d66e2dbb91825390b738a388606600663fc529b4b307"; };
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/ed25519-dalek" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "alloc")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "default")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/ed25519-dalek" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "std")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/ed25519-dalek" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "u64_backend")
    ];
    dependencies = {
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/ed25519-dalek" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "byteorder" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".byteorder."1.3.4" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/ed25519-dalek" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "digest" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".digest."0.9.0" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/ed25519-dalek" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "rand_core" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand_core."0.5.1" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/ed25519-dalek" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "subtle" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".subtle."2.2.2" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/ed25519-dalek" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "zeroize" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".zeroize."1.2.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".dashmap."4.0.2" = overridableMkRustCrate (profileName: rec {
    name = "dashmap";
    version = "4.0.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "e77a43b28d0668df09411cb0bc9a8c2adc40f9a048afe863e05fd43251e8e39c"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "raw-api" ]
      [ "rayon" ]
    ];
    dependencies = {
      cfg_if = rustPackages."registry+https://github.com/rust-lang/crates.io-index".cfg-if."1.0.0" { inherit profileName; };
      num_cpus = rustPackages."registry+https://github.com/rust-lang/crates.io-index".num_cpus."1.13.0" { inherit profileName; };
      rayon = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rayon."1.5.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".derivative."2.1.1" = overridableMkRustCrate (profileName: rec {
    name = "derivative";
    version = "2.1.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "cb582b60359da160a9477ee80f15c8d784c477e69c217ef2cdd4169c24ea380f"; };
    features = builtins.concatLists [
      [ "use_core" ]
    ];
    dependencies = {
      proc_macro2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."1.0.24" { inherit profileName; };
      quote = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quote."1.0.6" { inherit profileName; };
      syn = rustPackages."registry+https://github.com/rust-lang/crates.io-index".syn."1.0.60" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".derive_more."0.99.11" = overridableMkRustCrate (profileName: rec {
    name = "derive_more";
    version = "0.99.11";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "41cb0e6161ad61ed084a36ba71fbba9e3ac5aee3606fb607fe08da6acbcf3d8c"; };
    features = builtins.concatLists [
      [ "add" ]
      [ "add_assign" ]
      [ "as_mut" ]
      [ "as_ref" ]
      [ "constructor" ]
      [ "default" ]
      [ "deref" ]
      [ "deref_mut" ]
      [ "display" ]
      [ "error" ]
      [ "from" ]
      [ "from_str" ]
      [ "index" ]
      [ "index_mut" ]
      [ "into" ]
      [ "into_iterator" ]
      [ "iterator" ]
      [ "mul" ]
      [ "mul_assign" ]
      [ "not" ]
      [ "sum" ]
      [ "try_into" ]
    ];
    dependencies = {
      proc_macro2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."1.0.24" { inherit profileName; };
      quote = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quote."1.0.6" { inherit profileName; };
      syn = rustPackages."registry+https://github.com/rust-lang/crates.io-index".syn."1.0.60" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".dialoguer."0.6.2" = overridableMkRustCrate (profileName: rec {
    name = "dialoguer";
    version = "0.6.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "f4aa86af7b19b40ef9cbef761ed411a49f0afa06b7b6dcd3dfe2f96a3c546138"; };
    dependencies = {
      console = rustPackages."registry+https://github.com/rust-lang/crates.io-index".console."0.11.3" { inherit profileName; };
      lazy_static = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
      tempfile = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tempfile."3.1.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".difference."2.0.0" = overridableMkRustCrate (profileName: rec {
    name = "difference";
    version = "2.0.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "524cbf6897b527295dff137cec09ecf3a05f4fddffd7dfcd1585403449e74198"; };
    features = builtins.concatLists [
      [ "default" ]
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".digest."0.8.1" = overridableMkRustCrate (profileName: rec {
    name = "digest";
    version = "0.8.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "f3d0c8c8752312f9713efd397ff63acb9f85585afbf179282e720e7704954dd5"; };
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "std")
    ];
    dependencies = {
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "generic_array" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".generic-array."0.12.3" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".digest."0.9.0" = overridableMkRustCrate (profileName: rec {
    name = "digest";
    version = "0.9.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "d3dd60d1080a57a05ab032377049e0591415d2b31afd7028356dbf3cc6dcb066"; };
    features = builtins.concatLists [
      [ "alloc" ]
      [ "std" ]
    ];
    dependencies = {
      generic_array = rustPackages."registry+https://github.com/rust-lang/crates.io-index".generic-array."0.14.3" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".dir-diff."0.3.2" = overridableMkRustCrate (profileName: rec {
    name = "dir-diff";
    version = "0.3.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "2860407d7d7e2e004bb2128510ad9e8d669e76fa005ccf567977b5d71b8b4a0b"; };
    dependencies = {
      walkdir = rustPackages."registry+https://github.com/rust-lang/crates.io-index".walkdir."2.3.1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".dirs-next."2.0.0" = overridableMkRustCrate (profileName: rec {
    name = "dirs-next";
    version = "2.0.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "b98cf8ebf19c3d1b223e151f99a4f9f0690dca41414773390fc824184ac833e1"; };
    dependencies = {
      cfg_if = rustPackages."registry+https://github.com/rust-lang/crates.io-index".cfg-if."1.0.0" { inherit profileName; };
      dirs_sys_next = rustPackages."registry+https://github.com/rust-lang/crates.io-index".dirs-sys-next."0.1.1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".dirs-sys-next."0.1.1" = overridableMkRustCrate (profileName: rec {
    name = "dirs-sys-next";
    version = "0.1.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "99de365f605554ae33f115102a02057d4fc18b01f3284d6870be0938743cfe7d"; };
    dependencies = {
      ${ if hostPlatform.isUnix then "libc" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      ${ if hostPlatform.parsed.kernel.name == "redox" then "redox_users" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".redox_users."0.3.4" { inherit profileName; };
      ${ if hostPlatform.isWindows then "winapi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.3.8" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".discard."1.0.4" = overridableMkRustCrate (profileName: rec {
    name = "discard";
    version = "1.0.4";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "212d0f5754cb6769937f4501cc0e67f4f4483c8d2c3e1e922ee9edbe4ab4c7c0"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".dlopen."0.1.8" = overridableMkRustCrate (profileName: rec {
    name = "dlopen";
    version = "0.1.8";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "71e80ad39f814a9abe68583cd50a2d45c8a67561c3361ab8da240587dda80937"; };
    dependencies = {
      dlopen_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".dlopen_derive."0.1.4" { profileName = "__noProfile"; };
      lazy_static = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
      ${ if hostPlatform.isUnix then "libc" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      ${ if hostPlatform.isWindows then "winapi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.3.8" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".dlopen_derive."0.1.4" = overridableMkRustCrate (profileName: rec {
    name = "dlopen_derive";
    version = "0.1.4";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "f236d9e1b1fbd81cea0f9cbdc8dcc7e8ebcd80e6659cd7cb2ad5f6c05946c581"; };
    dependencies = {
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      quote = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quote."0.6.13" { inherit profileName; };
      syn = rustPackages."registry+https://github.com/rust-lang/crates.io-index".syn."0.15.44" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".doc-comment."0.3.3" = overridableMkRustCrate (profileName: rec {
    name = "doc-comment";
    version = "0.3.3";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "fea41bba32d969b513997752735605054bc0dfa92b4c56bf1189f2e174be7a10"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".dtoa."0.4.5" = overridableMkRustCrate (profileName: rec {
    name = "dtoa";
    version = "0.4.5";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "4358a9e11b9a09cf52383b451b49a169e8d797b68aa02301ff586d70d9661ea3"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".ed25519."1.0.1" = overridableMkRustCrate (profileName: rec {
    name = "ed25519";
    version = "1.0.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "bf038a7b6fd7ef78ad3348b63f3a17550877b0e28f8d68bcc94894d1412158bc"; };
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "serde")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/ed25519-dalek" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "std")
    ];
    dependencies = {
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "serde" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/ed25519-dalek" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "signature" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".signature."1.1.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".ed25519-dalek."1.0.1" = overridableMkRustCrate (profileName: rec {
    name = "ed25519-dalek";
    version = "1.0.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "c762bae6dcaf24c4c84667b8579785430908723d5c889f469d76a41d59cc7a9d"; };
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/ed25519-dalek" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "default")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/ed25519-dalek" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "rand")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "serde")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "serde_bytes")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/ed25519-dalek" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "serde_crate")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/ed25519-dalek" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "std")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/ed25519-dalek" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "u64_backend")
    ];
    dependencies = {
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/ed25519-dalek" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "curve25519_dalek" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".curve25519-dalek."3.0.0" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/ed25519-dalek" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "ed25519" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".ed25519."1.0.1" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/ed25519-dalek" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "rand" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand."0.7.3" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/ed25519-dalek" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "serde_crate" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "serde_bytes" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_bytes."0.11.4" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/ed25519-dalek" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "sha2" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".sha2."0.9.2" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/ed25519-dalek" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "zeroize" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".zeroize."1.2.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".educe."0.4.13" = overridableMkRustCrate (profileName: rec {
    name = "educe";
    version = "0.4.13";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "7260c7e6e656fc7702a1aa8d5b498a1a69aa84ac4ffcd5501b7d26939f368a93"; };
    features = builtins.concatLists [
      [ "Debug" ]
      [ "Default" ]
    ];
    dependencies = {
      enum_ordinalize = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".enum-ordinalize."3.1.8" { profileName = "__noProfile"; };
      proc_macro2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."1.0.24" { inherit profileName; };
      quote = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quote."1.0.6" { inherit profileName; };
      syn = rustPackages."registry+https://github.com/rust-lang/crates.io-index".syn."1.0.60" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".either."1.5.3" = overridableMkRustCrate (profileName: rec {
    name = "either";
    version = "1.5.3";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "bb1f6b1ce1c140482ea30ddd3335fc0024ac7ee112895426e0a629a6c20adfe3"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "use_std" ]
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".encode_unicode."0.3.6" = overridableMkRustCrate (profileName: rec {
    name = "encode_unicode";
    version = "0.3.6";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "a357d28ed41a50f9c765dbfe56cbc04a64e53e5fc58ba79fbc34c10ef3df831f"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "std" ]
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".encoding_rs."0.8.23" = overridableMkRustCrate (profileName: rec {
    name = "encoding_rs";
    version = "0.8.23";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "e8ac63f94732332f44fe654443c46f6375d1939684c17b0afb6cb56b0456e171"; };
    dependencies = {
      cfg_if = rustPackages."registry+https://github.com/rust-lang/crates.io-index".cfg-if."0.1.10" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".enum-iterator."0.6.0" = overridableMkRustCrate (profileName: rec {
    name = "enum-iterator";
    version = "0.6.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "c79a6321a1197d7730510c7e3f6cb80432dfefecb32426de8cea0aa19b4bb8d7"; };
    dependencies = {
      enum_iterator_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".enum-iterator-derive."0.6.0" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".enum-iterator-derive."0.6.0" = overridableMkRustCrate (profileName: rec {
    name = "enum-iterator-derive";
    version = "0.6.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "1e94aa31f7c0dc764f57896dc615ddd76fc13b0d5dca7eb6cc5e018a5a09ec06"; };
    dependencies = {
      proc_macro2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."1.0.24" { inherit profileName; };
      quote = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quote."1.0.6" { inherit profileName; };
      syn = rustPackages."registry+https://github.com/rust-lang/crates.io-index".syn."1.0.60" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".enum-ordinalize."3.1.8" = overridableMkRustCrate (profileName: rec {
    name = "enum-ordinalize";
    version = "3.1.8";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "1676e1daadfd216bda88d3a6fedd1bf53b829a085f5cc4d81c6f3054f50ef983"; };
    dependencies = {
      num_bigint = rustPackages."registry+https://github.com/rust-lang/crates.io-index".num-bigint."0.3.1" { inherit profileName; };
      num_traits = rustPackages."registry+https://github.com/rust-lang/crates.io-index".num-traits."0.2.11" { inherit profileName; };
      proc_macro2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."1.0.24" { inherit profileName; };
      quote = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quote."1.0.6" { inherit profileName; };
      syn = rustPackages."registry+https://github.com/rust-lang/crates.io-index".syn."1.0.60" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".env_logger."0.7.1" = overridableMkRustCrate (profileName: rec {
    name = "env_logger";
    version = "0.7.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "44533bbbb3bb3c1fa17d9f2e4e38bbbaf8396ba82193c4cb1b6445d711445d36"; };
    features = builtins.concatLists [
      [ "atty" ]
      [ "default" ]
      [ "humantime" ]
      [ "regex" ]
      [ "termcolor" ]
    ];
    dependencies = {
      atty = rustPackages."registry+https://github.com/rust-lang/crates.io-index".atty."0.2.14" { inherit profileName; };
      humantime = rustPackages."registry+https://github.com/rust-lang/crates.io-index".humantime."1.3.0" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      regex = rustPackages."registry+https://github.com/rust-lang/crates.io-index".regex."1.3.9" { inherit profileName; };
      termcolor = rustPackages."registry+https://github.com/rust-lang/crates.io-index".termcolor."1.1.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".env_logger."0.8.3" = overridableMkRustCrate (profileName: rec {
    name = "env_logger";
    version = "0.8.3";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "17392a012ea30ef05a610aa97dfb49496e71c9f676b27879922ea5bdf60d9d3f"; };
    features = builtins.concatLists [
      [ "atty" ]
      [ "default" ]
      [ "humantime" ]
      [ "regex" ]
      [ "termcolor" ]
    ];
    dependencies = {
      atty = rustPackages."registry+https://github.com/rust-lang/crates.io-index".atty."0.2.14" { inherit profileName; };
      humantime = rustPackages."registry+https://github.com/rust-lang/crates.io-index".humantime."2.0.1" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      regex = rustPackages."registry+https://github.com/rust-lang/crates.io-index".regex."1.3.9" { inherit profileName; };
      termcolor = rustPackages."registry+https://github.com/rust-lang/crates.io-index".termcolor."1.1.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".failure."0.1.8" = overridableMkRustCrate (profileName: rec {
    name = "failure";
    version = "0.1.8";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "d32e9bd16cc02eae7db7ef620b392808b89f6a5e16bb3497d159c6b92a0f4f86"; };
    features = builtins.concatLists [
      [ "backtrace" ]
      [ "default" ]
      [ "derive" ]
      [ "failure_derive" ]
      [ "std" ]
    ];
    dependencies = {
      backtrace = rustPackages."registry+https://github.com/rust-lang/crates.io-index".backtrace."0.3.48" { inherit profileName; };
      failure_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".failure_derive."0.1.8" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".failure_derive."0.1.8" = overridableMkRustCrate (profileName: rec {
    name = "failure_derive";
    version = "0.1.8";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "aa4da3c766cd7a0db8242e326e9e4e081edd567072893ed320008189715366a4"; };
    dependencies = {
      proc_macro2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."1.0.24" { inherit profileName; };
      quote = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quote."1.0.6" { inherit profileName; };
      syn = rustPackages."registry+https://github.com/rust-lang/crates.io-index".syn."1.0.60" { inherit profileName; };
      synstructure = rustPackages."registry+https://github.com/rust-lang/crates.io-index".synstructure."0.12.3" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".fake-simd."0.1.2" = overridableMkRustCrate (profileName: rec {
    name = "fake-simd";
    version = "0.1.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "e88a8acf291dafb59c2d96e8f59828f3838bb1a70398823ade51a84de6a6deed"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".fast-math."0.1.1" = overridableMkRustCrate (profileName: rec {
    name = "fast-math";
    version = "0.1.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "2465292146cdfc2011350fe3b1c616ac83cf0faeedb33463ba1c332ed8948d66"; };
    dependencies = {
      ieee754 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".ieee754."0.2.6" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".fd-lock."2.0.0" = overridableMkRustCrate (profileName: rec {
    name = "fd-lock";
    version = "2.0.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "0010f02effd88c702318c5dde0463206be67495d0b4d906ba7c0a8f166cc7f06"; };
    dependencies = {
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      ${ if hostPlatform.isWindows then "winapi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.3.8" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".feature-probe."0.1.1" = overridableMkRustCrate (profileName: rec {
    name = "feature-probe";
    version = "0.1.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "835a3dc7d1ec9e75e2b5fb4ba75396837112d2060b03f7d43bc1897c7f7211da"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".filetime."0.2.10" = overridableMkRustCrate (profileName: rec {
    name = "filetime";
    version = "0.2.10";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "affc17579b132fc2461adf7c575cc6e8b134ebca52c51f5411388965227dc695"; };
    dependencies = {
      cfg_if = rustPackages."registry+https://github.com/rust-lang/crates.io-index".cfg-if."0.1.10" { inherit profileName; };
      ${ if hostPlatform.isUnix then "libc" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      ${ if hostPlatform.parsed.kernel.name == "redox" then "syscall" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".redox_syscall."0.1.56" { inherit profileName; };
      ${ if hostPlatform.isWindows then "winapi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.3.8" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".flate2."1.0.14" = overridableMkRustCrate (profileName: rec {
    name = "flate2";
    version = "1.0.14";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "2cfff41391129e0a856d6d822600b8d71179d46879e310417eb9c762eb178b42"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "miniz_oxide" ]
      [ "rust_backend" ]
    ];
    dependencies = {
      cfg_if = rustPackages."registry+https://github.com/rust-lang/crates.io-index".cfg-if."0.1.10" { inherit profileName; };
      crc32fast = rustPackages."registry+https://github.com/rust-lang/crates.io-index".crc32fast."1.2.0" { inherit profileName; };
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      miniz_oxide = rustPackages."registry+https://github.com/rust-lang/crates.io-index".miniz_oxide."0.3.6" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".fnv."1.0.7" = overridableMkRustCrate (profileName: rec {
    name = "fnv";
    version = "1.0.7";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "3f9eec918d3f24069decb9af1554cad7c880e2da24a9afd88aca000531ab82c1"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "std" ]
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".foreign-types."0.3.2" = overridableMkRustCrate (profileName: rec {
    name = "foreign-types";
    version = "0.3.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "f6f339eb8adc052cd2ca78910fda869aefa38d22d5cb648e6485e4d3fc06f3b1"; };
    dependencies = {
      foreign_types_shared = rustPackages."registry+https://github.com/rust-lang/crates.io-index".foreign-types-shared."0.1.1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".foreign-types-shared."0.1.1" = overridableMkRustCrate (profileName: rec {
    name = "foreign-types-shared";
    version = "0.1.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "00b0228411908ca8685dba7fc2cdd70ec9990a6e753e89b6ac91a84c40fbaf4b"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".form_urlencoded."1.0.0" = overridableMkRustCrate (profileName: rec {
    name = "form_urlencoded";
    version = "1.0.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "ece68d15c92e84fa4f19d3780f1294e5ca82a78a6d515f1efaabcc144688be00"; };
    dependencies = {
      matches = rustPackages."registry+https://github.com/rust-lang/crates.io-index".matches."0.1.8" { inherit profileName; };
      percent_encoding = rustPackages."registry+https://github.com/rust-lang/crates.io-index".percent-encoding."2.1.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".fs_extra."1.2.0" = overridableMkRustCrate (profileName: rec {
    name = "fs_extra";
    version = "1.2.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "2022715d62ab30faffd124d40b76f4134a550a87792276512b18d63272333394"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".fuchsia-cprng."0.1.1" = overridableMkRustCrate (profileName: rec {
    name = "fuchsia-cprng";
    version = "0.1.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "a06f77d526c1a601b7c4cdd98f54b5eaabffc14d5f2f0296febdc7f357c6d3ba"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".fuchsia-zircon."0.3.3" = overridableMkRustCrate (profileName: rec {
    name = "fuchsia-zircon";
    version = "0.3.3";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "2e9763c69ebaae630ba35f74888db465e49e259ba1bc0eda7d06f4a067615d82"; };
    dependencies = {
      bitflags = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bitflags."1.2.1" { inherit profileName; };
      fuchsia_zircon_sys = rustPackages."registry+https://github.com/rust-lang/crates.io-index".fuchsia-zircon-sys."0.3.3" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".fuchsia-zircon-sys."0.3.3" = overridableMkRustCrate (profileName: rec {
    name = "fuchsia-zircon-sys";
    version = "0.3.3";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "3dcaa9ae7725d12cdb85b3ad99a434db70b468c09ded17e012d86b5c1010f7a7"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".futures."0.1.29" = overridableMkRustCrate (profileName: rec {
    name = "futures";
    version = "0.1.29";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "1b980f2816d6ee8673b6517b52cb0e808a180efc92e5c19d02cdda79066703ef"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "use_std" ]
      [ "with-deprecated" ]
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".futures."0.3.8" = overridableMkRustCrate (profileName: rec {
    name = "futures";
    version = "0.3.8";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "9b3b0c040a1fe6529d30b3c5944b280c7f0dcb2930d2c3062bca967b602583d0"; };
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower") "alloc")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower") "async-await")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "compat")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower") "default")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower") "executor")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower") "futures-executor")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower") "std")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "thread-pool")
    ];
    dependencies = {
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower" then "futures_channel" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-channel."0.3.8" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower" then "futures_core" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-core."0.3.8" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower" then "futures_executor" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-executor."0.3.8" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower" then "futures_io" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-io."0.3.8" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower" then "futures_sink" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-sink."0.3.8" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower" then "futures_task" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-task."0.3.8" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower" then "futures_util" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-util."0.3.8" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".futures-channel."0.3.8" = overridableMkRustCrate (profileName: rec {
    name = "futures-channel";
    version = "0.3.8";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "4b7109687aa4e177ef6fe84553af6280ef2778bdb7783ba44c9dc3399110fe64"; };
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "alloc")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "default")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower") "futures-sink")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower") "sink")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "std")
    ];
    dependencies = {
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "futures_core" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-core."0.3.8" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower" then "futures_sink" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-sink."0.3.8" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".futures-core."0.3.8" = overridableMkRustCrate (profileName: rec {
    name = "futures-core";
    version = "0.3.8";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "847ce131b72ffb13b6109a221da9ad97a64cbe48feb1028356b836b47b8f1748"; };
    features = builtins.concatLists [
      [ "alloc" ]
      [ "default" ]
      [ "std" ]
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".futures-executor."0.3.8" = overridableMkRustCrate (profileName: rec {
    name = "futures-executor";
    version = "0.3.8";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "4caa2b2b68b880003057c1dd49f1ed937e38f22fcf6c212188a121f08cf40a65"; };
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "num_cpus")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower") "std")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "thread-pool")
    ];
    dependencies = {
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower" then "futures_core" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-core."0.3.8" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower" then "futures_task" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-task."0.3.8" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower" then "futures_util" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-util."0.3.8" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" then "num_cpus" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".num_cpus."1.13.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".futures-io."0.3.8" = overridableMkRustCrate (profileName: rec {
    name = "futures-io";
    version = "0.3.8";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "611834ce18aaa1bd13c4b374f5d653e1027cf99b6b502584ff8c9a64413b30bb"; };
    features = builtins.concatLists [
      [ "std" ]
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".futures-macro."0.3.8" = overridableMkRustCrate (profileName: rec {
    name = "futures-macro";
    version = "0.3.8";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "77408a692f1f97bcc61dc001d752e00643408fbc922e4d634c655df50d595556"; };
    dependencies = {
      proc_macro_hack = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro-hack."0.5.19" { profileName = "__noProfile"; };
      proc_macro2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."1.0.24" { inherit profileName; };
      quote = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quote."1.0.6" { inherit profileName; };
      syn = rustPackages."registry+https://github.com/rust-lang/crates.io-index".syn."1.0.60" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".futures-sink."0.3.8" = overridableMkRustCrate (profileName: rec {
    name = "futures-sink";
    version = "0.3.8";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "f878195a49cee50e006b02b93cf7e0a95a38ac7b776b4c4d9cc1207cd20fcb3d"; };
    features = builtins.concatLists [
      [ "alloc" ]
      [ "default" ]
      [ "std" ]
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".futures-task."0.3.8" = overridableMkRustCrate (profileName: rec {
    name = "futures-task";
    version = "0.3.8";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "7c554eb5bf48b2426c4771ab68c6b14468b6e76cc90996f528c3338d761a4d0d"; };
    features = builtins.concatLists [
      [ "alloc" ]
      [ "once_cell" ]
      [ "std" ]
    ];
    dependencies = {
      once_cell = rustPackages."registry+https://github.com/rust-lang/crates.io-index".once_cell."1.5.2" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".futures-util."0.3.8" = overridableMkRustCrate (profileName: rec {
    name = "futures-util";
    version = "0.3.8";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "d304cff4a7b99cfb7986f7d43fbe93d175e72e704a8860787cc95e9ffd85cbd2"; };
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "alloc")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "async-await")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "async-await-macro")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower") "channel")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "compat")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "default")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower") "futures-channel")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "futures-io")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "futures-macro")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower") "futures-sink")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "futures_01")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "io")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "memchr")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "proc-macro-hack")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "proc-macro-nested")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower") "sink")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "slab")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "std")
    ];
    dependencies = {
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" then "futures_01" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures."0.1.29" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower" then "futures_channel" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-channel."0.3.8" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "futures_core" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-core."0.3.8" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "futures_io" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-io."0.3.8" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "futures_macro" else null } = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-macro."0.3.8" { profileName = "__noProfile"; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower" then "futures_sink" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-sink."0.3.8" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "futures_task" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-task."0.3.8" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "memchr" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".memchr."2.3.3" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "pin_project" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".pin-project."1.0.1" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "pin_utils" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".pin-utils."0.1.0" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "proc_macro_hack" else null } = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro-hack."0.5.19" { profileName = "__noProfile"; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "proc_macro_nested" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro-nested."0.1.4" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "slab" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".slab."0.4.2" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".gag."0.1.10" = overridableMkRustCrate (profileName: rec {
    name = "gag";
    version = "0.1.10";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "8cc0b9f53275dc5fada808f1d2f82e3688a6c14d735633d1590b7be8eb2307b5"; };
    dependencies = {
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      tempfile = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tempfile."3.1.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".generic-array."0.12.3" = overridableMkRustCrate (profileName: rec {
    name = "generic-array";
    version = "0.12.3";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "c68f0274ae0e023facc3c97b2e00f076be70e254bc851d972503b328db79b2ec"; };
    dependencies = {
      typenum = rustPackages."registry+https://github.com/rust-lang/crates.io-index".typenum."1.12.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".generic-array."0.14.3" = overridableMkRustCrate (profileName: rec {
    name = "generic-array";
    version = "0.14.3";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "60fb4bb6bba52f78a471264d9a3b7d026cc0af47b22cd2cffbc0b787ca003e63"; };
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "more_lengths")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "serde")
    ];
    dependencies = {
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "serde" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "typenum" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".typenum."1.12.0" { inherit profileName; };
    };
    buildDependencies = {
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "version_check" else null } = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".version_check."0.9.2" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".gethostname."0.2.1" = overridableMkRustCrate (profileName: rec {
    name = "gethostname";
    version = "0.2.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "e692e296bfac1d2533ef168d0b60ff5897b8b70a4009276834014dd8924cc028"; };
    dependencies = {
      ${ if !hostPlatform.isWindows then "libc" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      ${ if hostPlatform.isWindows then "winapi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.3.8" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".getrandom."0.1.14" = overridableMkRustCrate (profileName: rec {
    name = "getrandom";
    version = "0.1.14";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "7abc8dd8451921606d809ba32e95b6111925cd2906060d2dcc29c070220503eb"; };
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "std")
    ];
    dependencies = {
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-config" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "cfg_if" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".cfg-if."0.1.10" { inherit profileName; };
      ${ if (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-config" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") && hostPlatform.isUnix then "libc" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      ${ if (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-config" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") && hostPlatform.parsed.kernel.name == "wasi" then "wasi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".wasi."0.9.0+wasi-snapshot-preview1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".getrandom."0.2.0" = overridableMkRustCrate (profileName: rec {
    name = "getrandom";
    version = "0.2.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "ee8025cf36f917e6a52cce185b7c7177689b838b7ec138364e50cc2277a56cf4"; };
    features = builtins.concatLists [
      [ "std" ]
    ];
    dependencies = {
      cfg_if = rustPackages."registry+https://github.com/rust-lang/crates.io-index".cfg-if."0.1.10" { inherit profileName; };
      ${ if hostPlatform.isUnix then "libc" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      ${ if hostPlatform.parsed.kernel.name == "wasi" then "wasi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".wasi."0.9.0+wasi-snapshot-preview1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".gimli."0.21.0" = overridableMkRustCrate (profileName: rec {
    name = "gimli";
    version = "0.21.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "bcc8e0c9bce37868955864dbecd2b1ab2bdf967e6f28066d65aaac620444b65c"; };
    features = builtins.concatLists [
      [ "read" ]
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".glob."0.3.0" = overridableMkRustCrate (profileName: rec {
    name = "glob";
    version = "0.3.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "9b919933a397b79c37e33b77bb2aa3dc8eb6e165ad809e58ff75bc7db2e34574"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".globset."0.4.5" = overridableMkRustCrate (profileName: rec {
    name = "globset";
    version = "0.4.5";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "7ad1da430bd7281dde2576f44c84cc3f0f7b475e7202cd503042dff01a8c8120"; };
    dependencies = {
      aho_corasick = rustPackages."registry+https://github.com/rust-lang/crates.io-index".aho-corasick."0.7.10" { inherit profileName; };
      bstr = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bstr."0.2.13" { inherit profileName; };
      fnv = rustPackages."registry+https://github.com/rust-lang/crates.io-index".fnv."1.0.7" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      regex = rustPackages."registry+https://github.com/rust-lang/crates.io-index".regex."1.3.9" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".goauth."0.9.0" = overridableMkRustCrate (profileName: rec {
    name = "goauth";
    version = "0.9.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "d94101e84ede813c04773b0a43396c01b5a3a9376537dbce1125858ae090ae60"; };
    dependencies = {
      arc_swap = rustPackages."registry+https://github.com/rust-lang/crates.io-index".arc-swap."1.2.0" { inherit profileName; };
      futures = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures."0.3.8" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      reqwest = rustPackages."registry+https://github.com/rust-lang/crates.io-index".reqwest."0.11.2" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      serde_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_derive."1.0.122" { profileName = "__noProfile"; };
      serde_json = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_json."1.0.62" { inherit profileName; };
      simpl = rustPackages."registry+https://github.com/rust-lang/crates.io-index".simpl."0.1.0" { inherit profileName; };
      smpl_jwt = rustPackages."registry+https://github.com/rust-lang/crates.io-index".smpl_jwt."0.6.0" { inherit profileName; };
      time = rustPackages."registry+https://github.com/rust-lang/crates.io-index".time."0.2.25" { inherit profileName; };
      tokio = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio."1.1.1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".goblin."0.3.0" = overridableMkRustCrate (profileName: rec {
    name = "goblin";
    version = "0.3.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "c69552f48b18aa6102ce0c82dd9bc9d3f8af5fc0a5797069b1b466b90570e39c"; };
    features = builtins.concatLists [
      [ "alloc" ]
      [ "archive" ]
      [ "default" ]
      [ "elf32" ]
      [ "elf64" ]
      [ "endian_fd" ]
      [ "log" ]
      [ "mach32" ]
      [ "mach64" ]
      [ "pe32" ]
      [ "pe64" ]
      [ "std" ]
    ];
    dependencies = {
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      plain = rustPackages."registry+https://github.com/rust-lang/crates.io-index".plain."0.2.3" { inherit profileName; };
      scroll = rustPackages."registry+https://github.com/rust-lang/crates.io-index".scroll."0.10.1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".h2."0.2.5" = overridableMkRustCrate (profileName: rec {
    name = "h2";
    version = "0.2.5";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "79b7246d7e4b979c03fa093da39cfb3617a96bbeee6310af63991668d7e843ff"; };
    dependencies = {
      bytes = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bytes."0.5.4" { inherit profileName; };
      fnv = rustPackages."registry+https://github.com/rust-lang/crates.io-index".fnv."1.0.7" { inherit profileName; };
      futures_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-core."0.3.8" { inherit profileName; };
      futures_sink = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-sink."0.3.8" { inherit profileName; };
      futures_util = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-util."0.3.8" { inherit profileName; };
      http = rustPackages."registry+https://github.com/rust-lang/crates.io-index".http."0.2.1" { inherit profileName; };
      indexmap = rustPackages."registry+https://github.com/rust-lang/crates.io-index".indexmap."1.5.1" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      slab = rustPackages."registry+https://github.com/rust-lang/crates.io-index".slab."0.4.2" { inherit profileName; };
      tokio = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio."0.2.22" { inherit profileName; };
      tokio_util = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-util."0.3.1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".h2."0.3.0" = overridableMkRustCrate (profileName: rec {
    name = "h2";
    version = "0.3.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "6b67e66362108efccd8ac053abafc8b7a8d86a37e6e48fc4f6f7485eb5e9e6a5"; };
    dependencies = {
      bytes = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bytes."1.0.1" { inherit profileName; };
      fnv = rustPackages."registry+https://github.com/rust-lang/crates.io-index".fnv."1.0.7" { inherit profileName; };
      futures_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-core."0.3.8" { inherit profileName; };
      futures_sink = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-sink."0.3.8" { inherit profileName; };
      futures_util = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-util."0.3.8" { inherit profileName; };
      http = rustPackages."registry+https://github.com/rust-lang/crates.io-index".http."0.2.1" { inherit profileName; };
      indexmap = rustPackages."registry+https://github.com/rust-lang/crates.io-index".indexmap."1.5.1" { inherit profileName; };
      slab = rustPackages."registry+https://github.com/rust-lang/crates.io-index".slab."0.4.2" { inherit profileName; };
      tokio = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio."1.1.1" { inherit profileName; };
      tokio_util = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-util."0.6.3" { inherit profileName; };
      tracing = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tracing."0.1.18" { inherit profileName; };
      tracing_futures = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tracing-futures."0.2.4" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".half."1.6.0" = overridableMkRustCrate (profileName: rec {
    name = "half";
    version = "1.6.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "d36fab90f82edc3c747f9d438e06cf0a491055896f2a279638bb5beed6c40177"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".hash32."0.1.1" = overridableMkRustCrate (profileName: rec {
    name = "hash32";
    version = "0.1.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "d4041af86e63ac4298ce40e5cca669066e75b6f1aa3390fe2561ffa5e1d9f4cc"; };
    dependencies = {
      byteorder = rustPackages."registry+https://github.com/rust-lang/crates.io-index".byteorder."1.3.4" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".hashbrown."0.8.2" = overridableMkRustCrate (profileName: rec {
    name = "hashbrown";
    version = "0.8.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "e91b62f79061a0bc2e046024cb7ba44b08419ed238ecbd9adbd787434b9e8c25"; };
    features = builtins.concatLists [
      [ "raw" ]
    ];
    buildDependencies = {
      autocfg = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".autocfg."1.0.0" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".hashbrown."0.9.1" = overridableMkRustCrate (profileName: rec {
    name = "hashbrown";
    version = "0.9.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "d7afe4a420e3fe79967a00898cc1f4db7c8a49a9333a29f8a4bd76a253d5cd04"; };
    features = builtins.concatLists [
      [ "ahash" ]
      [ "default" ]
      [ "inline-more" ]
    ];
    dependencies = {
      ahash = rustPackages."registry+https://github.com/rust-lang/crates.io-index".ahash."0.4.6" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".hermit-abi."0.1.13" = overridableMkRustCrate (profileName: rec {
    name = "hermit-abi";
    version = "0.1.13";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "91780f809e750b0a89f5544be56617ff6b1227ee485bcb06ebe10cdf89bd3b71"; };
    features = builtins.concatLists [
      [ "default" ]
    ];
    dependencies = {
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".hex."0.4.2" = overridableMkRustCrate (profileName: rec {
    name = "hex";
    version = "0.4.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "644f9158b2f133fd50f5fb3242878846d9eb792e445c893805ff0e3824006e35"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "std" ]
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".hidapi."1.2.5" = overridableMkRustCrate (profileName: rec {
    name = "hidapi";
    version = "1.2.5";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "76c352a18370f7e7e47bcbfcbdc5432b8c80c705b5d751a25232c659fcf5c775"; };
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-remote-wallet/linux-shared-hidraw") "linux-shared-hidraw")
      (lib.optional (rootFeatures' ? "solana-remote-wallet/linux-shared-libusb") "linux-shared-libusb")
      (lib.optional (rootFeatures' ? "solana-remote-wallet/default" || rootFeatures' ? "solana-remote-wallet/linux-static-hidraw" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower") "linux-static-hidraw")
      (lib.optional (rootFeatures' ? "solana-remote-wallet/linux-static-libusb") "linux-static-libusb")
    ];
    dependencies = {
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower" then "libc" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
    };
    buildDependencies = {
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower" then "cc" else null } = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".cc."1.0.49" { profileName = "__noProfile"; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower" then "pkg_config" else null } = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".pkg-config."0.3.17" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".histogram."0.6.9" = overridableMkRustCrate (profileName: rec {
    name = "histogram";
    version = "0.6.9";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "12cb882ccb290b8646e554b157ab0b71e64e8d5bef775cd66b6531e52d302669"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".hmac."0.7.1" = overridableMkRustCrate (profileName: rec {
    name = "hmac";
    version = "0.7.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "5dcb5e64cda4c23119ab41ba960d1e170a774c8e4b9d9e6a9bc18aabf5e59695"; };
    dependencies = {
      crypto_mac = rustPackages."registry+https://github.com/rust-lang/crates.io-index".crypto-mac."0.7.0" { inherit profileName; };
      digest = rustPackages."registry+https://github.com/rust-lang/crates.io-index".digest."0.8.1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".hmac."0.8.1" = overridableMkRustCrate (profileName: rec {
    name = "hmac";
    version = "0.8.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "126888268dcc288495a26bf004b38c5fdbb31682f992c84ceb046a1f0fe38840"; };
    dependencies = {
      crypto_mac = rustPackages."registry+https://github.com/rust-lang/crates.io-index".crypto-mac."0.8.0" { inherit profileName; };
      digest = rustPackages."registry+https://github.com/rust-lang/crates.io-index".digest."0.9.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".hmac."0.10.1" = overridableMkRustCrate (profileName: rec {
    name = "hmac";
    version = "0.10.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "c1441c6b1e930e2817404b5046f1f989899143a12bf92de603b69f4e0aee1e15"; };
    dependencies = {
      crypto_mac = rustPackages."registry+https://github.com/rust-lang/crates.io-index".crypto-mac."0.10.0" { inherit profileName; };
      digest = rustPackages."registry+https://github.com/rust-lang/crates.io-index".digest."0.9.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".hmac-drbg."0.2.0" = overridableMkRustCrate (profileName: rec {
    name = "hmac-drbg";
    version = "0.2.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "c6e570451493f10f6581b48cdd530413b63ea9e780f544bfd3bdcaa0d89d1a7b"; };
    dependencies = {
      digest = rustPackages."registry+https://github.com/rust-lang/crates.io-index".digest."0.8.1" { inherit profileName; };
      generic_array = rustPackages."registry+https://github.com/rust-lang/crates.io-index".generic-array."0.12.3" { inherit profileName; };
      hmac = rustPackages."registry+https://github.com/rust-lang/crates.io-index".hmac."0.7.1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".http."0.2.1" = overridableMkRustCrate (profileName: rec {
    name = "http";
    version = "0.2.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "28d569972648b2c512421b5f2a405ad6ac9666547189d0c5477a3f200f3e02f9"; };
    dependencies = {
      bytes = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bytes."0.5.4" { inherit profileName; };
      fnv = rustPackages."registry+https://github.com/rust-lang/crates.io-index".fnv."1.0.7" { inherit profileName; };
      itoa = rustPackages."registry+https://github.com/rust-lang/crates.io-index".itoa."0.4.5" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".http-body."0.3.1" = overridableMkRustCrate (profileName: rec {
    name = "http-body";
    version = "0.3.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "13d5ff830006f7646652e057693569bfe0d51760c0085a071769d142a205111b"; };
    dependencies = {
      bytes = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bytes."0.5.4" { inherit profileName; };
      http = rustPackages."registry+https://github.com/rust-lang/crates.io-index".http."0.2.1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".http-body."0.4.0" = overridableMkRustCrate (profileName: rec {
    name = "http-body";
    version = "0.4.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "2861bd27ee074e5ee891e8b539837a9430012e249d7f0ca2d795650f579c1994"; };
    dependencies = {
      bytes = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bytes."1.0.1" { inherit profileName; };
      http = rustPackages."registry+https://github.com/rust-lang/crates.io-index".http."0.2.1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".httparse."1.3.4" = overridableMkRustCrate (profileName: rec {
    name = "httparse";
    version = "1.3.4";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "cd179ae861f0c2e53da70d892f5f3029f9594be0c41dc5269cd371691b1dc2f9"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "std" ]
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".httpdate."0.3.2" = overridableMkRustCrate (profileName: rec {
    name = "httpdate";
    version = "0.3.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "494b4d60369511e7dea41cf646832512a94e542f68bb9c49e54518e0f468eb47"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".humantime."1.3.0" = overridableMkRustCrate (profileName: rec {
    name = "humantime";
    version = "1.3.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "df004cfca50ef23c36850aaaa59ad52cc70d0e90243c3c7737a4dd32dc7a3c4f"; };
    dependencies = {
      quick_error = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quick-error."1.2.3" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".humantime."2.0.1" = overridableMkRustCrate (profileName: rec {
    name = "humantime";
    version = "2.0.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "3c1ad908cc71012b7bea4d0c53ba96a8cba9962f048fa68d143376143d863b7a"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".hyper."0.10.16" = overridableMkRustCrate (profileName: rec {
    name = "hyper";
    version = "0.10.16";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "0a0652d9a2609a968c14be1a9ea00bf4b1d64e2e1f53a1b51b6fff3a6e829273"; };
    dependencies = {
      base64 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".base64."0.9.3" { inherit profileName; };
      httparse = rustPackages."registry+https://github.com/rust-lang/crates.io-index".httparse."1.3.4" { inherit profileName; };
      language_tags = rustPackages."registry+https://github.com/rust-lang/crates.io-index".language-tags."0.2.2" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.3.9" { inherit profileName; };
      mime = rustPackages."registry+https://github.com/rust-lang/crates.io-index".mime."0.2.6" { inherit profileName; };
      num_cpus = rustPackages."registry+https://github.com/rust-lang/crates.io-index".num_cpus."1.13.0" { inherit profileName; };
      time = rustPackages."registry+https://github.com/rust-lang/crates.io-index".time."0.1.43" { inherit profileName; };
      traitobject = rustPackages."registry+https://github.com/rust-lang/crates.io-index".traitobject."0.1.0" { inherit profileName; };
      typeable = rustPackages."registry+https://github.com/rust-lang/crates.io-index".typeable."0.1.2" { inherit profileName; };
      unicase = rustPackages."registry+https://github.com/rust-lang/crates.io-index".unicase."1.4.2" { inherit profileName; };
      url = rustPackages."registry+https://github.com/rust-lang/crates.io-index".url."1.7.2" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".hyper."0.13.10" = overridableMkRustCrate (profileName: rec {
    name = "hyper";
    version = "0.13.10";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "8a6f157065790a3ed2f88679250419b5cdd96e714a0d65f7797fd337186e96bb"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "runtime" ]
      [ "socket2" ]
      [ "stream" ]
      [ "tcp" ]
    ];
    dependencies = {
      bytes = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bytes."0.5.4" { inherit profileName; };
      futures_channel = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-channel."0.3.8" { inherit profileName; };
      futures_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-core."0.3.8" { inherit profileName; };
      futures_util = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-util."0.3.8" { inherit profileName; };
      h2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".h2."0.2.5" { inherit profileName; };
      http = rustPackages."registry+https://github.com/rust-lang/crates.io-index".http."0.2.1" { inherit profileName; };
      http_body = rustPackages."registry+https://github.com/rust-lang/crates.io-index".http-body."0.3.1" { inherit profileName; };
      httparse = rustPackages."registry+https://github.com/rust-lang/crates.io-index".httparse."1.3.4" { inherit profileName; };
      httpdate = rustPackages."registry+https://github.com/rust-lang/crates.io-index".httpdate."0.3.2" { inherit profileName; };
      itoa = rustPackages."registry+https://github.com/rust-lang/crates.io-index".itoa."0.4.5" { inherit profileName; };
      pin_project = rustPackages."registry+https://github.com/rust-lang/crates.io-index".pin-project."1.0.1" { inherit profileName; };
      socket2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".socket2."0.3.17" { inherit profileName; };
      tokio = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio."0.2.22" { inherit profileName; };
      tower_service = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tower-service."0.3.0" { inherit profileName; };
      tracing = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tracing."0.1.18" { inherit profileName; };
      want = rustPackages."registry+https://github.com/rust-lang/crates.io-index".want."0.3.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".hyper."0.14.3" = overridableMkRustCrate (profileName: rec {
    name = "hyper";
    version = "0.14.3";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "5ee5fc98172988e4394a3094002a75125e8fb864a88318732e8b613ec5adbda3"; };
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "client")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "default")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "full")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "h2")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "http1")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "http2")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "runtime")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "server")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "socket2")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "stream")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "tcp")
    ];
    dependencies = {
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "bytes" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bytes."1.0.1" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "futures_channel" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-channel."0.3.8" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "futures_core" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-core."0.3.8" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "futures_util" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-util."0.3.8" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "h2" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".h2."0.3.0" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "http" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".http."0.2.1" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "http_body" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".http-body."0.4.0" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "httparse" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".httparse."1.3.4" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "httpdate" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".httpdate."0.3.2" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "itoa" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".itoa."0.4.5" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "pin_project_lite" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".pin-project-lite."0.2.4" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "socket2" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".socket2."0.3.17" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "tokio" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio."1.1.1" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "tower_service" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tower-service."0.3.0" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "tracing" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tracing."0.1.18" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "want" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".want."0.3.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".hyper-rustls."0.22.1" = overridableMkRustCrate (profileName: rec {
    name = "hyper-rustls";
    version = "0.22.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "5f9f7a97316d44c0af9b0301e65010573a853a9fc97046d7331d7f6bc0fd5a64"; };
    dependencies = {
      futures_util = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-util."0.3.8" { inherit profileName; };
      hyper = rustPackages."registry+https://github.com/rust-lang/crates.io-index".hyper."0.14.3" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      rustls = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rustls."0.19.0" { inherit profileName; };
      tokio = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio."1.1.1" { inherit profileName; };
      tokio_rustls = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-rustls."0.22.0" { inherit profileName; };
      webpki = rustPackages."registry+https://github.com/rust-lang/crates.io-index".webpki."0.21.2" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".hyper-tls."0.5.0" = overridableMkRustCrate (profileName: rec {
    name = "hyper-tls";
    version = "0.5.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "d6183ddfa99b85da61a140bea0efc93fdf56ceaa041b37d553518030827f9905"; };
    dependencies = {
      bytes = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bytes."1.0.1" { inherit profileName; };
      hyper = rustPackages."registry+https://github.com/rust-lang/crates.io-index".hyper."0.14.3" { inherit profileName; };
      native_tls = rustPackages."registry+https://github.com/rust-lang/crates.io-index".native-tls."0.2.7" { inherit profileName; };
      tokio = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio."1.1.1" { inherit profileName; };
      tokio_native_tls = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-native-tls."0.3.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".idna."0.1.5" = overridableMkRustCrate (profileName: rec {
    name = "idna";
    version = "0.1.5";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "38f09e0f0b1fb55fdee1f17470ad800da77af5186a1a76c026b679358b7e844e"; };
    dependencies = {
      matches = rustPackages."registry+https://github.com/rust-lang/crates.io-index".matches."0.1.8" { inherit profileName; };
      unicode_bidi = rustPackages."registry+https://github.com/rust-lang/crates.io-index".unicode-bidi."0.3.4" { inherit profileName; };
      unicode_normalization = rustPackages."registry+https://github.com/rust-lang/crates.io-index".unicode-normalization."0.1.16" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".idna."0.2.0" = overridableMkRustCrate (profileName: rec {
    name = "idna";
    version = "0.2.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "02e2673c30ee86b5b96a9cb52ad15718aa1f966f5ab9ad54a8b95d5ca33120a9"; };
    dependencies = {
      matches = rustPackages."registry+https://github.com/rust-lang/crates.io-index".matches."0.1.8" { inherit profileName; };
      unicode_bidi = rustPackages."registry+https://github.com/rust-lang/crates.io-index".unicode-bidi."0.3.4" { inherit profileName; };
      unicode_normalization = rustPackages."registry+https://github.com/rust-lang/crates.io-index".unicode-normalization."0.1.16" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".ieee754."0.2.6" = overridableMkRustCrate (profileName: rec {
    name = "ieee754";
    version = "0.2.6";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "9007da9cacbd3e6343da136e98b0d2df013f553d35bdec8b518f07bea768e19c"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".indexed."0.1.1" = overridableMkRustCrate (profileName: rec {
    name = "indexed";
    version = "0.1.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "d480125acf340d6a6e59dab69ae19d6fca3a906e1eade277671272cc8f73794b"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".indexmap."1.5.1" = overridableMkRustCrate (profileName: rec {
    name = "indexmap";
    version = "1.5.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "86b45e59b16c76b11bf9738fd5d38879d3bd28ad292d7b313608becb17ae2df9"; };
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "rayon")
    ];
    dependencies = {
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "hashbrown" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".hashbrown."0.8.2" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" then "rayon" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rayon."1.5.0" { inherit profileName; };
    };
    buildDependencies = {
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "autocfg" else null } = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".autocfg."1.0.0" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".indicatif."0.15.0" = overridableMkRustCrate (profileName: rec {
    name = "indicatif";
    version = "0.15.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "7baab56125e25686df467fe470785512329883aab42696d661247aca2a2896e4"; };
    features = builtins.concatLists [
      [ "default" ]
    ];
    dependencies = {
      console = rustPackages."registry+https://github.com/rust-lang/crates.io-index".console."0.11.3" { inherit profileName; };
      lazy_static = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
      number_prefix = rustPackages."registry+https://github.com/rust-lang/crates.io-index".number_prefix."0.3.0" { inherit profileName; };
      regex = rustPackages."registry+https://github.com/rust-lang/crates.io-index".regex."1.3.9" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".input_buffer."0.3.1" = overridableMkRustCrate (profileName: rec {
    name = "input_buffer";
    version = "0.3.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "19a8a95243d5a0398cae618ec29477c6e3cb631152be5c19481f80bc71559754"; };
    dependencies = {
      bytes = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bytes."0.5.4" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".instant."0.1.6" = overridableMkRustCrate (profileName: rec {
    name = "instant";
    version = "0.1.6";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "5b141fdc7836c525d4d594027d318c84161ca17aaf8113ab1f81ab93ae897485"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".iovec."0.1.4" = overridableMkRustCrate (profileName: rec {
    name = "iovec";
    version = "0.1.4";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "b2b3ea6ff95e175473f8ffe6a7eb7c00d054240321b84c57051175fe3c1e075e"; };
    dependencies = {
      ${ if hostPlatform.isUnix then "libc" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".ipnet."2.3.0" = overridableMkRustCrate (profileName: rec {
    name = "ipnet";
    version = "2.3.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "47be2f14c678be2fdcab04ab1171db51b2762ce6f0a8ee87c8dd4a04ed216135"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".itertools."0.9.0" = overridableMkRustCrate (profileName: rec {
    name = "itertools";
    version = "0.9.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "284f18f85651fe11e8a991b2adb42cb078325c996ed026d994719efcfca1d54b"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "use_std" ]
    ];
    dependencies = {
      either = rustPackages."registry+https://github.com/rust-lang/crates.io-index".either."1.5.3" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".itoa."0.4.5" = overridableMkRustCrate (profileName: rec {
    name = "itoa";
    version = "0.4.5";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "b8b7a7c0c47db5545ed3fef7468ee7bb5b74691498139e4b3f6a20685dc6dd8e"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "std" ]
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".jemalloc-ctl."0.3.3" = overridableMkRustCrate (profileName: rec {
    name = "jemalloc-ctl";
    version = "0.3.3";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "c502a5ff9dd2924f1ed32ba96e3b65735d837b4bfd978d3161b1702e66aca4b7"; };
    features = builtins.concatLists [
      [ "default" ]
    ];
    dependencies = {
      jemalloc_sys = rustPackages."registry+https://github.com/rust-lang/crates.io-index".jemalloc-sys."0.3.2" { inherit profileName; };
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      paste = rustPackages."registry+https://github.com/rust-lang/crates.io-index".paste."0.1.16" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".jemalloc-sys."0.3.2" = overridableMkRustCrate (profileName: rec {
    name = "jemalloc-sys";
    version = "0.3.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "0d3b9f3f5c9b31aa0f5ed3260385ac205db665baa41d49bb8338008ae94ede45"; };
    features = builtins.concatLists [
      [ "background_threads_runtime_support" ]
      [ "default" ]
    ];
    dependencies = {
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
    };
    buildDependencies = {
      cc = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".cc."1.0.49" { profileName = "__noProfile"; };
      fs_extra = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".fs_extra."1.2.0" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".jemallocator."0.3.2" = overridableMkRustCrate (profileName: rec {
    name = "jemallocator";
    version = "0.3.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "43ae63fcfc45e99ab3d1b29a46782ad679e98436c3169d15a167a1108a724b69"; };
    features = builtins.concatLists [
      [ "background_threads_runtime_support" ]
      [ "default" ]
    ];
    dependencies = {
      jemalloc_sys = rustPackages."registry+https://github.com/rust-lang/crates.io-index".jemalloc-sys."0.3.2" { inherit profileName; };
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".jobserver."0.1.21" = overridableMkRustCrate (profileName: rec {
    name = "jobserver";
    version = "0.1.21";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "5c71313ebb9439f74b00d9d2dcec36440beaf57a6aa0623068441dd7cd81a7f2"; };
    dependencies = {
      ${ if hostPlatform.isUnix then "libc" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".js-sys."0.3.47" = overridableMkRustCrate (profileName: rec {
    name = "js-sys";
    version = "0.3.47";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "5cfb73131c35423a367daf8cbd24100af0d077668c8c2943f0e7dd775fef0f65"; };
    dependencies = {
      wasm_bindgen = rustPackages."registry+https://github.com/rust-lang/crates.io-index".wasm-bindgen."0.2.70" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".jsonrpc-client-transports."17.0.0" = overridableMkRustCrate (profileName: rec {
    name = "jsonrpc-client-transports";
    version = "17.0.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "15b6c6ad01c7354d60de493148c30ac8a82b759e22ae678c8705e9b8e0c566a4"; };
    features = builtins.concatLists [
      [ "ipc" ]
      [ "jsonrpc-server-utils" ]
      [ "parity-tokio-ipc" ]
      [ "tokio" ]
      [ "websocket" ]
      [ "ws" ]
    ];
    dependencies = {
      derive_more = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".derive_more."0.99.11" { profileName = "__noProfile"; };
      futures = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures."0.3.8" { inherit profileName; };
      jsonrpc_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".jsonrpc-core."17.0.0" { inherit profileName; };
      jsonrpc_pubsub = rustPackages."registry+https://github.com/rust-lang/crates.io-index".jsonrpc-pubsub."17.0.0" { inherit profileName; };
      jsonrpc_server_utils = rustPackages."registry+https://github.com/rust-lang/crates.io-index".jsonrpc-server-utils."17.0.0" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      parity_tokio_ipc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".parity-tokio-ipc."0.8.0" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      serde_json = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_json."1.0.62" { inherit profileName; };
      tokio = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio."0.2.22" { inherit profileName; };
      url = rustPackages."registry+https://github.com/rust-lang/crates.io-index".url."1.7.2" { inherit profileName; };
      websocket = rustPackages."registry+https://github.com/rust-lang/crates.io-index".websocket."0.24.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".jsonrpc-core."17.0.0" = overridableMkRustCrate (profileName: rec {
    name = "jsonrpc-core";
    version = "17.0.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "07569945133257ff557eb37b015497104cea61a2c9edaf126c1cbd6e8332397f"; };
    dependencies = {
      futures = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures."0.3.8" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      serde_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_derive."1.0.122" { profileName = "__noProfile"; };
      serde_json = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_json."1.0.62" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".jsonrpc-core-client."17.0.0" = overridableMkRustCrate (profileName: rec {
    name = "jsonrpc-core-client";
    version = "17.0.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "7ac9d56dc729912796637c30f475bbf834594607b27740dfea6e5fa7ba40d1f1"; };
    features = builtins.concatLists [
      [ "ipc" ]
      [ "ws" ]
    ];
    dependencies = {
      futures = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures."0.3.8" { inherit profileName; };
      jsonrpc_client_transports = rustPackages."registry+https://github.com/rust-lang/crates.io-index".jsonrpc-client-transports."17.0.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".jsonrpc-derive."17.0.0" = overridableMkRustCrate (profileName: rec {
    name = "jsonrpc-derive";
    version = "17.0.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "b68ba7e76e5c7796cfa4d2a30e83986550c34404c6d40551c902ca6f7bd4a137"; };
    dependencies = {
      proc_macro_crate = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro-crate."0.1.5" { inherit profileName; };
      proc_macro2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."1.0.24" { inherit profileName; };
      quote = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quote."1.0.6" { inherit profileName; };
      syn = rustPackages."registry+https://github.com/rust-lang/crates.io-index".syn."1.0.60" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".jsonrpc-http-server."17.0.0" = overridableMkRustCrate (profileName: rec {
    name = "jsonrpc-http-server";
    version = "17.0.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "eff2303c4f0562afcbd2dae75e3e21815095f8994749a80fbcd365877e44ed64"; };
    dependencies = {
      futures = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures."0.3.8" { inherit profileName; };
      hyper = rustPackages."registry+https://github.com/rust-lang/crates.io-index".hyper."0.13.10" { inherit profileName; };
      jsonrpc_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".jsonrpc-core."17.0.0" { inherit profileName; };
      jsonrpc_server_utils = rustPackages."registry+https://github.com/rust-lang/crates.io-index".jsonrpc-server-utils."17.0.0" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      net2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".net2."0.2.37" { inherit profileName; };
      parking_lot = rustPackages."registry+https://github.com/rust-lang/crates.io-index".parking_lot."0.11.0" { inherit profileName; };
      unicase = rustPackages."registry+https://github.com/rust-lang/crates.io-index".unicase."2.6.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".jsonrpc-ipc-server."17.0.1" = overridableMkRustCrate (profileName: rec {
    name = "jsonrpc-ipc-server";
    version = "17.0.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "14c4cd89e5ea7e7f0884e828fc35bb83591a371b92439675eae28efa66c24a97"; };
    dependencies = {
      futures = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures."0.3.8" { inherit profileName; };
      jsonrpc_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".jsonrpc-core."17.0.0" { inherit profileName; };
      jsonrpc_server_utils = rustPackages."registry+https://github.com/rust-lang/crates.io-index".jsonrpc-server-utils."17.0.0" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      parity_tokio_ipc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".parity-tokio-ipc."0.8.0" { inherit profileName; };
      parking_lot = rustPackages."registry+https://github.com/rust-lang/crates.io-index".parking_lot."0.11.0" { inherit profileName; };
      tower_service = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tower-service."0.3.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".jsonrpc-pubsub."17.0.0" = overridableMkRustCrate (profileName: rec {
    name = "jsonrpc-pubsub";
    version = "17.0.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "0c48dbebce7a9c88ab272a4db7d6478aa4c6d9596e6c086366e89efc4e9ed89e"; };
    dependencies = {
      futures = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures."0.3.8" { inherit profileName; };
      jsonrpc_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".jsonrpc-core."17.0.0" { inherit profileName; };
      lazy_static = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      parking_lot = rustPackages."registry+https://github.com/rust-lang/crates.io-index".parking_lot."0.11.0" { inherit profileName; };
      rand = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand."0.7.3" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".jsonrpc-server-utils."17.0.0" = overridableMkRustCrate (profileName: rec {
    name = "jsonrpc-server-utils";
    version = "17.0.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "f4207cce738bf713a82525065b750a008f28351324f438f56b33d698ada95bb4"; };
    dependencies = {
      bytes = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bytes."0.5.4" { inherit profileName; };
      futures = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures."0.3.8" { inherit profileName; };
      globset = rustPackages."registry+https://github.com/rust-lang/crates.io-index".globset."0.4.5" { inherit profileName; };
      jsonrpc_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".jsonrpc-core."17.0.0" { inherit profileName; };
      lazy_static = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      tokio = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio."0.2.22" { inherit profileName; };
      tokio_util = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-util."0.3.1" { inherit profileName; };
      unicase = rustPackages."registry+https://github.com/rust-lang/crates.io-index".unicase."2.6.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".jsonrpc-ws-server."17.0.0" = overridableMkRustCrate (profileName: rec {
    name = "jsonrpc-ws-server";
    version = "17.0.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "abe06e1385e4a912711703123ba44f735627d666f87e5fec764ad1338ec617dc"; };
    dependencies = {
      futures = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures."0.3.8" { inherit profileName; };
      jsonrpc_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".jsonrpc-core."17.0.0" { inherit profileName; };
      jsonrpc_server_utils = rustPackages."registry+https://github.com/rust-lang/crates.io-index".jsonrpc-server-utils."17.0.0" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      parity_ws = rustPackages."registry+https://github.com/rust-lang/crates.io-index".parity-ws."0.10.0" { inherit profileName; };
      parking_lot = rustPackages."registry+https://github.com/rust-lang/crates.io-index".parking_lot."0.11.0" { inherit profileName; };
      slab = rustPackages."registry+https://github.com/rust-lang/crates.io-index".slab."0.4.2" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".keccak."0.1.0" = overridableMkRustCrate (profileName: rec {
    name = "keccak";
    version = "0.1.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "67c21572b4949434e4fc1e1978b99c5f77064153c59d998bf13ecd96fb5ecba7"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".kernel32-sys."0.2.2" = overridableMkRustCrate (profileName: rec {
    name = "kernel32-sys";
    version = "0.2.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "7507624b29483431c0ba2d82aece8ca6cdba9382bff4ddd0f7490560c056098d"; };
    dependencies = {
      winapi = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.2.8" { inherit profileName; };
    };
    buildDependencies = {
      build = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi-build."0.1.1" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".language-tags."0.2.2" = overridableMkRustCrate (profileName: rec {
    name = "language-tags";
    version = "0.2.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "a91d884b6667cd606bb5a69aa0c99ba811a115fc68915e7056ec08a46e93199a"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" = overridableMkRustCrate (profileName: rec {
    name = "lazy_static";
    version = "1.4.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "e2abad23fbc42b3700f2f279844dc832adb2b2eb069b2df918f455c4e18cc646"; };
    features = builtins.concatLists [
      [ "spin" ]
      [ "spin_no_std" ]
    ];
    dependencies = {
      spin = rustPackages."registry+https://github.com/rust-lang/crates.io-index".spin."0.5.2" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".lazycell."1.2.1" = overridableMkRustCrate (profileName: rec {
    name = "lazycell";
    version = "1.2.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "b294d6fa9ee409a054354afc4352b0b9ef7ca222c69b8812cbea9e7d2bf3783f"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" = overridableMkRustCrate (profileName: rec {
    name = "libc";
    version = "0.2.81";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "1482821306169ec4d07f6aca392a4681f66c75c9918aa49641a2595db64053cb"; };
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "align")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-config" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-rayon-threadlimit" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "default")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "extra_traits")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-config" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-rayon-threadlimit" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "std")
      (lib.optional (rootFeatures' ? "solana-core") "use_std")
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".libloading."0.5.2" = overridableMkRustCrate (profileName: rec {
    name = "libloading";
    version = "0.5.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "f2b111a074963af1d37a139918ac6d49ad1d0d5e47f72fd55388619691a7d753"; };
    dependencies = {
      ${ if hostPlatform.isWindows then "winapi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.3.8" { inherit profileName; };
    };
    buildDependencies = {
      cc = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".cc."1.0.49" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".libloading."0.6.2" = overridableMkRustCrate (profileName: rec {
    name = "libloading";
    version = "0.6.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "2cadb8e769f070c45df05c78c7520eb4cd17061d4ab262e43cfc68b4d00ac71c"; };
    dependencies = {
      ${ if hostPlatform.isWindows then "winapi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.3.8" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".librocksdb-sys."6.11.4" = overridableMkRustCrate (profileName: rec {
    name = "librocksdb-sys";
    version = "6.11.4";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "eb5b56f651c204634b936be2f92dbb42c36867e00ff7fe2405591f3b9fa66f09"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "lz4" ]
      [ "static" ]
    ];
    dependencies = {
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
    };
    buildDependencies = {
      bindgen = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".bindgen."0.54.0" { profileName = "__noProfile"; };
      cc = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".cc."1.0.49" { profileName = "__noProfile"; };
      glob = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".glob."0.3.0" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".libsecp256k1."0.3.5" = overridableMkRustCrate (profileName: rec {
    name = "libsecp256k1";
    version = "0.3.5";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "1fc1e2c808481a63dc6da2074752fdd4336a3c8fcc68b83db6f1fd5224ae7962"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "hmac" ]
      [ "hmac-drbg" ]
      [ "sha2" ]
      [ "std" ]
      [ "typenum" ]
    ];
    dependencies = {
      arrayref = rustPackages."registry+https://github.com/rust-lang/crates.io-index".arrayref."0.3.6" { inherit profileName; };
      crunchy = rustPackages."registry+https://github.com/rust-lang/crates.io-index".crunchy."0.2.2" { inherit profileName; };
      digest = rustPackages."registry+https://github.com/rust-lang/crates.io-index".digest."0.8.1" { inherit profileName; };
      hmac_drbg = rustPackages."registry+https://github.com/rust-lang/crates.io-index".hmac-drbg."0.2.0" { inherit profileName; };
      rand = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand."0.7.3" { inherit profileName; };
      sha2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".sha2."0.8.2" { inherit profileName; };
      subtle = rustPackages."registry+https://github.com/rust-lang/crates.io-index".subtle."2.2.2" { inherit profileName; };
      typenum = rustPackages."registry+https://github.com/rust-lang/crates.io-index".typenum."1.12.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".linked-hash-map."0.5.3" = overridableMkRustCrate (profileName: rec {
    name = "linked-hash-map";
    version = "0.5.3";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "8dd5a6d5999d9907cda8ed67bbd137d3af8085216c2ac62de5be860bd41f304a"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".lock_api."0.3.4" = overridableMkRustCrate (profileName: rec {
    name = "lock_api";
    version = "0.3.4";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "c4da24a77a3d8a6d4862d95f72e6fdb9c09a643ecdb402d754004a557f2bec75"; };
    dependencies = {
      scopeguard = rustPackages."registry+https://github.com/rust-lang/crates.io-index".scopeguard."1.1.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".lock_api."0.4.1" = overridableMkRustCrate (profileName: rec {
    name = "lock_api";
    version = "0.4.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "28247cc5a5be2f05fbcd76dd0cf2c7d3b5400cb978a28042abcd4fa0b3f8261c"; };
    dependencies = {
      scopeguard = rustPackages."registry+https://github.com/rust-lang/crates.io-index".scopeguard."1.1.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".log."0.3.9" = overridableMkRustCrate (profileName: rec {
    name = "log";
    version = "0.3.9";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "e19e8d5c34a3e0e2223db8e060f9e8264aeeb5c5fc64a4ee9965c062211c024b"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "use_std" ]
    ];
    dependencies = {
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" = overridableMkRustCrate (profileName: rec {
    name = "log";
    version = "0.4.11";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "4fabed175da42fed1fa0746b0ea71f412aa9d35e76e95e59b192c64b9dc2bf8b"; };
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-logger" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "std")
    ];
    dependencies = {
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-logger" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "cfg_if" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".cfg-if."0.1.10" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".lru."0.6.1" = overridableMkRustCrate (profileName: rec {
    name = "lru";
    version = "0.6.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "be716eb6878ca2263eb5d00a781aa13264a794f519fe6af4fbb2668b2d5441c0"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "hashbrown" ]
    ];
    dependencies = {
      hashbrown = rustPackages."registry+https://github.com/rust-lang/crates.io-index".hashbrown."0.9.1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".matches."0.1.8" = overridableMkRustCrate (profileName: rec {
    name = "matches";
    version = "0.1.8";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "7ffc5c5338469d4d3ea17d269fa8ea3512ad247247c30bd2df69e68309ed0a08"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".maybe-uninit."2.0.0" = overridableMkRustCrate (profileName: rec {
    name = "maybe-uninit";
    version = "2.0.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "60302e4db3a61da70c0cb7991976248362f30319e88850c487b9b95bbf059e00"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".memchr."1.0.2" = overridableMkRustCrate (profileName: rec {
    name = "memchr";
    version = "1.0.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "148fab2e51b4f1cfc66da2a7c32981d1d3c083a803978268bb11fe4b86925e7a"; };
    features = builtins.concatLists [
      [ "libc" ]
      [ "use_std" ]
    ];
    dependencies = {
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".memchr."2.3.3" = overridableMkRustCrate (profileName: rec {
    name = "memchr";
    version = "2.3.3";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "3728d817d99e5ac407411fa471ff9800a778d88a24685968b36824eaf4bee400"; };
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-logger" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "default")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-logger" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-scripts" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "std")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-logger" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-scripts" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "use_std")
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".memmap2."0.1.0" = overridableMkRustCrate (profileName: rec {
    name = "memmap2";
    version = "0.1.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "d9b70ca2a6103ac8b665dc150b142ef0e4e89df640c9e6cf295d189c3caebe5a"; };
    dependencies = {
      ${ if hostPlatform.isUnix then "libc" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".memoffset."0.5.4" = overridableMkRustCrate (profileName: rec {
    name = "memoffset";
    version = "0.5.4";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "b4fc2c02a7e374099d4ee95a193111f72d2110197fe200272371758f6c3643d8"; };
    features = builtins.concatLists [
      [ "default" ]
    ];
    buildDependencies = {
      autocfg = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".autocfg."1.0.0" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".memoffset."0.6.1" = overridableMkRustCrate (profileName: rec {
    name = "memoffset";
    version = "0.6.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "157b4208e3059a8f9e78d559edc658e13df41410cb3ae03979c83130067fdd87"; };
    features = builtins.concatLists [
      [ "default" ]
    ];
    buildDependencies = {
      autocfg = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".autocfg."1.0.0" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".mime."0.2.6" = overridableMkRustCrate (profileName: rec {
    name = "mime";
    version = "0.2.6";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "ba626b8a6de5da682e1caa06bdb42a335aee5a84db8e5046a3e8ab17ba0a3ae0"; };
    dependencies = {
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.3.9" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".mime."0.3.16" = overridableMkRustCrate (profileName: rec {
    name = "mime";
    version = "0.3.16";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "2a60c7ce501c71e03a9c9c0d35b861413ae925bd979cc7a4e30d060069aaac8d"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".miniz_oxide."0.3.6" = overridableMkRustCrate (profileName: rec {
    name = "miniz_oxide";
    version = "0.3.6";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "aa679ff6578b1cddee93d7e82e263b94a575e0bfced07284eb0c037c1d2416a5"; };
    dependencies = {
      adler32 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".adler32."1.0.4" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".mio."0.6.22" = overridableMkRustCrate (profileName: rec {
    name = "mio";
    version = "0.6.22";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "fce347092656428bc8eaf6201042cb551b8d67855af7374542a92a0fbfcac430"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "with-deprecated" ]
    ];
    dependencies = {
      cfg_if = rustPackages."registry+https://github.com/rust-lang/crates.io-index".cfg-if."0.1.10" { inherit profileName; };
      ${ if hostPlatform.parsed.kernel.name == "fuchsia" then "fuchsia_zircon" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".fuchsia-zircon."0.3.3" { inherit profileName; };
      ${ if hostPlatform.parsed.kernel.name == "fuchsia" then "fuchsia_zircon_sys" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".fuchsia-zircon-sys."0.3.3" { inherit profileName; };
      iovec = rustPackages."registry+https://github.com/rust-lang/crates.io-index".iovec."0.1.4" { inherit profileName; };
      ${ if hostPlatform.isWindows then "kernel32" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".kernel32-sys."0.2.2" { inherit profileName; };
      ${ if hostPlatform.isUnix then "libc" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      ${ if hostPlatform.isWindows then "miow" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".miow."0.2.2" { inherit profileName; };
      net2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".net2."0.2.37" { inherit profileName; };
      slab = rustPackages."registry+https://github.com/rust-lang/crates.io-index".slab."0.4.2" { inherit profileName; };
      ${ if hostPlatform.isWindows then "winapi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.2.8" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".mio."0.7.6" = overridableMkRustCrate (profileName: rec {
    name = "mio";
    version = "0.7.6";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "f33bc887064ef1fd66020c9adfc45bb9f33d75a42096c81e7c56c65b75dd1a8b"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "net" ]
      [ "os-ext" ]
      [ "os-poll" ]
      [ "os-util" ]
      [ "tcp" ]
      [ "udp" ]
      [ "uds" ]
    ];
    dependencies = {
      ${ if hostPlatform.isUnix then "libc" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      ${ if hostPlatform.isWindows then "miow" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".miow."0.3.6" { inherit profileName; };
      ${ if hostPlatform.isWindows then "ntapi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".ntapi."0.3.4" { inherit profileName; };
      ${ if hostPlatform.isWindows then "winapi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.3.8" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".mio-extras."2.0.6" = overridableMkRustCrate (profileName: rec {
    name = "mio-extras";
    version = "2.0.6";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "52403fe290012ce777c4626790c8951324a2b9e3316b3143779c72b029742f19"; };
    dependencies = {
      lazycell = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazycell."1.2.1" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      mio = rustPackages."registry+https://github.com/rust-lang/crates.io-index".mio."0.6.22" { inherit profileName; };
      slab = rustPackages."registry+https://github.com/rust-lang/crates.io-index".slab."0.4.2" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".mio-named-pipes."0.1.7" = overridableMkRustCrate (profileName: rec {
    name = "mio-named-pipes";
    version = "0.1.7";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "0840c1c50fd55e521b247f949c241c9997709f23bd7f023b9762cd561e935656"; };
    dependencies = {
      ${ if hostPlatform.isWindows then "log" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      ${ if hostPlatform.isWindows then "mio" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".mio."0.6.22" { inherit profileName; };
      ${ if hostPlatform.isWindows then "miow" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".miow."0.3.6" { inherit profileName; };
      ${ if hostPlatform.isWindows then "winapi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.3.8" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".mio-uds."0.6.8" = overridableMkRustCrate (profileName: rec {
    name = "mio-uds";
    version = "0.6.8";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "afcb699eb26d4332647cc848492bbc15eafb26f08d0304550d5aa1f612e066f0"; };
    dependencies = {
      ${ if hostPlatform.isUnix then "iovec" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".iovec."0.1.4" { inherit profileName; };
      ${ if hostPlatform.isUnix then "libc" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      ${ if hostPlatform.isUnix then "mio" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".mio."0.6.22" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".miow."0.2.2" = overridableMkRustCrate (profileName: rec {
    name = "miow";
    version = "0.2.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "ebd808424166322d4a38da87083bfddd3ac4c131334ed55856112eb06d46944d"; };
    dependencies = {
      kernel32 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".kernel32-sys."0.2.2" { inherit profileName; };
      net2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".net2."0.2.37" { inherit profileName; };
      winapi = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.2.8" { inherit profileName; };
      ws2_32 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".ws2_32-sys."0.2.1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".miow."0.3.6" = overridableMkRustCrate (profileName: rec {
    name = "miow";
    version = "0.3.6";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "5a33c1b55807fbed163481b5ba66db4b2fa6cde694a5027be10fb724206c5897"; };
    dependencies = {
      socket2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".socket2."0.3.17" { inherit profileName; };
      winapi = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.3.8" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".native-tls."0.2.7" = overridableMkRustCrate (profileName: rec {
    name = "native-tls";
    version = "0.2.7";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "b8d96b2e1c8da3957d58100b09f102c6d9cfdfced01b7ec5a8974044bb09dbd4"; };
    dependencies = {
      ${ if hostPlatform.parsed.kernel.name == "darwin" || hostPlatform.parsed.kernel.name == "ios" then "lazy_static" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
      ${ if hostPlatform.parsed.kernel.name == "darwin" || hostPlatform.parsed.kernel.name == "ios" then "libc" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      ${ if !(hostPlatform.parsed.kernel.name == "windows" || hostPlatform.parsed.kernel.name == "darwin" || hostPlatform.parsed.kernel.name == "ios") then "log" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      ${ if !(hostPlatform.parsed.kernel.name == "windows" || hostPlatform.parsed.kernel.name == "darwin" || hostPlatform.parsed.kernel.name == "ios") then "openssl" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".openssl."0.10.29" { inherit profileName; };
      ${ if !(hostPlatform.parsed.kernel.name == "windows" || hostPlatform.parsed.kernel.name == "darwin" || hostPlatform.parsed.kernel.name == "ios") then "openssl_probe" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".openssl-probe."0.1.2" { inherit profileName; };
      ${ if !(hostPlatform.parsed.kernel.name == "windows" || hostPlatform.parsed.kernel.name == "darwin" || hostPlatform.parsed.kernel.name == "ios") then "openssl_sys" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".openssl-sys."0.9.57" { inherit profileName; };
      ${ if hostPlatform.parsed.kernel.name == "windows" then "schannel" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".schannel."0.1.19" { inherit profileName; };
      ${ if hostPlatform.parsed.kernel.name == "darwin" || hostPlatform.parsed.kernel.name == "ios" then "security_framework" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".security-framework."2.1.2" { inherit profileName; };
      ${ if hostPlatform.parsed.kernel.name == "darwin" || hostPlatform.parsed.kernel.name == "ios" then "security_framework_sys" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".security-framework-sys."2.1.1" { inherit profileName; };
      ${ if hostPlatform.parsed.kernel.name == "darwin" || hostPlatform.parsed.kernel.name == "ios" then "tempfile" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tempfile."3.1.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".net2."0.2.37" = overridableMkRustCrate (profileName: rec {
    name = "net2";
    version = "0.2.37";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "391630d12b68002ae1e25e8f974306474966550ad82dac6886fb8910c19568ae"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "duration" ]
    ];
    dependencies = {
      cfg_if = rustPackages."registry+https://github.com/rust-lang/crates.io-index".cfg-if."0.1.10" { inherit profileName; };
      ${ if hostPlatform.parsed.kernel.name == "redox" || hostPlatform.isUnix || hostPlatform.parsed.kernel.name == "wasi" then "libc" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      ${ if hostPlatform.isWindows then "winapi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.3.8" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".nix."0.17.0" = overridableMkRustCrate (profileName: rec {
    name = "nix";
    version = "0.17.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "50e4785f2c3b7589a0d0c1dd60285e1188adac4006e8abd6dd578e1567027363"; };
    dependencies = {
      bitflags = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bitflags."1.2.1" { inherit profileName; };
      cfg_if = rustPackages."registry+https://github.com/rust-lang/crates.io-index".cfg-if."0.1.10" { inherit profileName; };
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      void = rustPackages."registry+https://github.com/rust-lang/crates.io-index".void."1.0.2" { inherit profileName; };
    };
    buildDependencies = {
      ${ if hostPlatform.parsed.kernel.name == "dragonfly" then "cc" else null } = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".cc."1.0.49" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".nix."0.19.0" = overridableMkRustCrate (profileName: rec {
    name = "nix";
    version = "0.19.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "85db2feff6bf70ebc3a4793191517d5f0331100a2f10f9bf93b5e5214f32b7b7"; };
    dependencies = {
      bitflags = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bitflags."1.2.1" { inherit profileName; };
      cfg_if = rustPackages."registry+https://github.com/rust-lang/crates.io-index".cfg-if."0.1.10" { inherit profileName; };
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
    };
    buildDependencies = {
      ${ if hostPlatform.parsed.kernel.name == "dragonfly" then "cc" else null } = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".cc."1.0.49" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".nom."3.2.1" = overridableMkRustCrate (profileName: rec {
    name = "nom";
    version = "3.2.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "05aec50c70fd288702bcd93284a8444607f3292dbdf2a30de5ea5dcdbe72287b"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "std" ]
      [ "stream" ]
    ];
    dependencies = {
      memchr = rustPackages."registry+https://github.com/rust-lang/crates.io-index".memchr."1.0.2" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".nom."5.1.1" = overridableMkRustCrate (profileName: rec {
    name = "nom";
    version = "5.1.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "0b471253da97532da4b61552249c521e01e736071f71c1a4f7ebbfbf0a06aad6"; };
    features = builtins.concatLists [
      [ "alloc" ]
      [ "std" ]
    ];
    dependencies = {
      memchr = rustPackages."registry+https://github.com/rust-lang/crates.io-index".memchr."2.3.3" { inherit profileName; };
    };
    buildDependencies = {
      version_check = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".version_check."0.9.2" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".ntapi."0.3.4" = overridableMkRustCrate (profileName: rec {
    name = "ntapi";
    version = "0.3.4";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "7a31937dea023539c72ddae0e3571deadc1414b300483fa7aaec176168cfa9d2"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "user" ]
    ];
    dependencies = {
      winapi = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.3.8" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".num-bigint."0.3.1" = overridableMkRustCrate (profileName: rec {
    name = "num-bigint";
    version = "0.3.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "5e9a41747ae4633fce5adffb4d2e81ffc5e89593cb19917f8fb2cc5ff76507bf"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "std" ]
    ];
    dependencies = {
      num_integer = rustPackages."registry+https://github.com/rust-lang/crates.io-index".num-integer."0.1.42" { inherit profileName; };
      num_traits = rustPackages."registry+https://github.com/rust-lang/crates.io-index".num-traits."0.2.11" { inherit profileName; };
    };
    buildDependencies = {
      autocfg = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".autocfg."1.0.0" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".num-derive."0.3.2" = overridableMkRustCrate (profileName: rec {
    name = "num-derive";
    version = "0.3.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "6f09b9841adb6b5e1f89ef7087ea636e0fd94b2851f887c1e3eb5d5f8228fab3"; };
    dependencies = {
      proc_macro2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."1.0.24" { inherit profileName; };
      quote = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quote."1.0.6" { inherit profileName; };
      syn = rustPackages."registry+https://github.com/rust-lang/crates.io-index".syn."1.0.60" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".num-integer."0.1.42" = overridableMkRustCrate (profileName: rec {
    name = "num-integer";
    version = "0.1.42";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "3f6ea62e9d81a77cd3ee9a2a5b9b609447857f3d358704331e4ef39eb247fcba"; };
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "i128")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "std")
    ];
    dependencies = {
      ${ if rootFeatures' ? "solana-sdk/chrono" || rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "num_traits" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".num-traits."0.2.11" { inherit profileName; };
    };
    buildDependencies = {
      ${ if rootFeatures' ? "solana-sdk/chrono" || rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "autocfg" else null } = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".autocfg."1.0.0" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".num-traits."0.2.11" = overridableMkRustCrate (profileName: rec {
    name = "num-traits";
    version = "0.2.11";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "c62be47e61d1842b9170f0fdeec8eba98e60e90e5446449a0545e5152acd7096"; };
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "default")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "i128")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "std")
    ];
    buildDependencies = {
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "autocfg" else null } = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".autocfg."1.0.0" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".num_cpus."1.13.0" = overridableMkRustCrate (profileName: rec {
    name = "num_cpus";
    version = "1.13.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "05499f3756671c15885fee9034446956fff3f243d6077b91e5767df161f766b3"; };
    dependencies = {
      ${ if (hostPlatform.parsed.cpu.name == "x86_64" || hostPlatform.parsed.cpu.name == "aarch64") && hostPlatform.parsed.kernel.name == "hermit" then "hermit_abi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".hermit-abi."0.1.13" { inherit profileName; };
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".num_enum."0.5.1" = overridableMkRustCrate (profileName: rec {
    name = "num_enum";
    version = "0.5.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "226b45a5c2ac4dd696ed30fa6b94b057ad909c7b7fc2e0d0808192bced894066"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "std" ]
    ];
    dependencies = {
      derivative = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".derivative."2.1.1" { profileName = "__noProfile"; };
      num_enum_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".num_enum_derive."0.5.1" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".num_enum_derive."0.5.1" = overridableMkRustCrate (profileName: rec {
    name = "num_enum_derive";
    version = "0.5.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "1c0fd9eba1d5db0994a239e09c1be402d35622277e35468ba891aa5e3188ce7e"; };
    features = builtins.concatLists [
      [ "proc-macro-crate" ]
      [ "std" ]
    ];
    dependencies = {
      proc_macro_crate = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro-crate."0.1.5" { inherit profileName; };
      proc_macro2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."1.0.24" { inherit profileName; };
      quote = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quote."1.0.6" { inherit profileName; };
      syn = rustPackages."registry+https://github.com/rust-lang/crates.io-index".syn."1.0.60" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".number_prefix."0.3.0" = overridableMkRustCrate (profileName: rec {
    name = "number_prefix";
    version = "0.3.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "17b02fc0ff9a9e4b35b3342880f48e896ebf69f2967921fe8646bf5b7125956a"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "std" ]
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".object."0.19.0" = overridableMkRustCrate (profileName: rec {
    name = "object";
    version = "0.19.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "9cbca9424c482ee628fa549d9c812e2cd22f1180b9222c9200fdfa6eb31aecb2"; };
    features = builtins.concatLists [
      [ "coff" ]
      [ "elf" ]
      [ "macho" ]
      [ "pe" ]
      [ "read_core" ]
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".once_cell."1.5.2" = overridableMkRustCrate (profileName: rec {
    name = "once_cell";
    version = "1.5.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "13bd41f508810a131401606d54ac32a467c97172d74ba7662562ebba5ad07fa0"; };
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "alloc")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower") "default")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower") "parking_lot")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "std")
    ];
    dependencies = {
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower" then "parking_lot" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".parking_lot."0.11.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".opaque-debug."0.2.3" = overridableMkRustCrate (profileName: rec {
    name = "opaque-debug";
    version = "0.2.3";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "2839e79665f131bdb5782e51f2c6c9599c133c6098982a54c794358bf432529c"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".opaque-debug."0.3.0" = overridableMkRustCrate (profileName: rec {
    name = "opaque-debug";
    version = "0.3.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "624a8340c38c1b80fd549087862da4ba43e08858af025b236e509b6649fc13d5"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".openssl."0.10.29" = overridableMkRustCrate (profileName: rec {
    name = "openssl";
    version = "0.10.29";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "cee6d85f4cb4c4f59a6a85d5b68a233d280c82e29e822913b9c8b129fbf20bdd"; };
    dependencies = {
      bitflags = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bitflags."1.2.1" { inherit profileName; };
      cfg_if = rustPackages."registry+https://github.com/rust-lang/crates.io-index".cfg-if."0.1.10" { inherit profileName; };
      foreign_types = rustPackages."registry+https://github.com/rust-lang/crates.io-index".foreign-types."0.3.2" { inherit profileName; };
      lazy_static = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      openssl_sys = rustPackages."registry+https://github.com/rust-lang/crates.io-index".openssl-sys."0.9.57" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".openssl-probe."0.1.2" = overridableMkRustCrate (profileName: rec {
    name = "openssl-probe";
    version = "0.1.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "77af24da69f9d9341038eba93a073b1fdaaa1b788221b00a69bce9e762cb32de"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".openssl-sys."0.9.57" = overridableMkRustCrate (profileName: rec {
    name = "openssl-sys";
    version = "0.9.57";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "7410fef80af8ac071d4f63755c0ab89ac3df0fd1ea91f1d1f37cf5cec4395990"; };
    dependencies = {
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
    };
    buildDependencies = {
      autocfg = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".autocfg."1.0.0" { profileName = "__noProfile"; };
      cc = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".cc."1.0.49" { profileName = "__noProfile"; };
      pkg_config = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".pkg-config."0.3.17" { profileName = "__noProfile"; };
      ${ if hostPlatform.parsed.abi.name == "msvc" then "vcpkg" else null } = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".vcpkg."0.2.8" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".ouroboros."0.5.1" = overridableMkRustCrate (profileName: rec {
    name = "ouroboros";
    version = "0.5.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "cc04551635026d3ac7bc646698ea1836a85ed2a26b7094fe1d15d8b14854c4a2"; };
    dependencies = {
      ouroboros_macro = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".ouroboros_macro."0.5.1" { profileName = "__noProfile"; };
      stable_deref_trait = rustPackages."registry+https://github.com/rust-lang/crates.io-index".stable_deref_trait."1.2.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".ouroboros_macro."0.5.1" = overridableMkRustCrate (profileName: rec {
    name = "ouroboros_macro";
    version = "0.5.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "cec33dfceabec83cd0e95a5ce9d20e76ab3a5cbfef59659b8c927f69b93ed8ae"; };
    dependencies = {
      inflector = rustPackages."registry+https://github.com/rust-lang/crates.io-index".Inflector."0.11.4" { inherit profileName; };
      proc_macro2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."1.0.24" { inherit profileName; };
      quote = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quote."1.0.6" { inherit profileName; };
      syn = rustPackages."registry+https://github.com/rust-lang/crates.io-index".syn."1.0.60" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".parity-tokio-ipc."0.8.0" = overridableMkRustCrate (profileName: rec {
    name = "parity-tokio-ipc";
    version = "0.8.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "fd7f6c69d7687501b2205fe51ade1d7b8797bb3aa141fe5bf13dd78c0483bc89"; };
    dependencies = {
      futures = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures."0.3.8" { inherit profileName; };
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      mio_named_pipes = rustPackages."registry+https://github.com/rust-lang/crates.io-index".mio-named-pipes."0.1.7" { inherit profileName; };
      miow = rustPackages."registry+https://github.com/rust-lang/crates.io-index".miow."0.3.6" { inherit profileName; };
      rand = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand."0.7.3" { inherit profileName; };
      tokio = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio."0.2.22" { inherit profileName; };
      ${ if hostPlatform.isWindows then "winapi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.3.8" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".parity-ws."0.10.0" = overridableMkRustCrate (profileName: rec {
    name = "parity-ws";
    version = "0.10.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "9e02a625dd75084c2a7024f07c575b61b782f729d18702dabb3cdbf31911dc61"; };
    features = builtins.concatLists [
      [ "default" ]
    ];
    dependencies = {
      byteorder = rustPackages."registry+https://github.com/rust-lang/crates.io-index".byteorder."1.3.4" { inherit profileName; };
      bytes = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bytes."0.4.12" { inherit profileName; };
      httparse = rustPackages."registry+https://github.com/rust-lang/crates.io-index".httparse."1.3.4" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      mio = rustPackages."registry+https://github.com/rust-lang/crates.io-index".mio."0.6.22" { inherit profileName; };
      mio_extras = rustPackages."registry+https://github.com/rust-lang/crates.io-index".mio-extras."2.0.6" { inherit profileName; };
      rand = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand."0.7.3" { inherit profileName; };
      sha1 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".sha-1."0.8.2" { inherit profileName; };
      slab = rustPackages."registry+https://github.com/rust-lang/crates.io-index".slab."0.4.2" { inherit profileName; };
      url = rustPackages."registry+https://github.com/rust-lang/crates.io-index".url."2.2.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".parking_lot."0.9.0" = overridableMkRustCrate (profileName: rec {
    name = "parking_lot";
    version = "0.9.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "f842b1982eb6c2fe34036a4fbfb06dd185a3f5c8edfaacdf7d1ea10b07de6252"; };
    features = builtins.concatLists [
      [ "default" ]
    ];
    dependencies = {
      lock_api = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lock_api."0.3.4" { inherit profileName; };
      parking_lot_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".parking_lot_core."0.6.2" { inherit profileName; };
    };
    buildDependencies = {
      rustc_version = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".rustc_version."0.2.3" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".parking_lot."0.10.2" = overridableMkRustCrate (profileName: rec {
    name = "parking_lot";
    version = "0.10.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "d3a704eb390aafdc107b0e392f56a82b668e3a71366993b5340f5833fd62505e"; };
    features = builtins.concatLists [
      [ "default" ]
    ];
    dependencies = {
      lock_api = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lock_api."0.3.4" { inherit profileName; };
      parking_lot_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".parking_lot_core."0.7.2" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".parking_lot."0.11.0" = overridableMkRustCrate (profileName: rec {
    name = "parking_lot";
    version = "0.11.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "a4893845fa2ca272e647da5d0e46660a314ead9c2fdd9a883aabc32e481a8733"; };
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower") "default")
    ];
    dependencies = {
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower" then "instant" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".instant."0.1.6" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower" then "lock_api" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lock_api."0.4.1" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower" then "parking_lot_core" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".parking_lot_core."0.8.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".parking_lot_core."0.6.2" = overridableMkRustCrate (profileName: rec {
    name = "parking_lot_core";
    version = "0.6.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "b876b1b9e7ac6e1a74a6da34d25c42e17e8862aa409cbbbdcfc8d86c6f3bc62b"; };
    dependencies = {
      cfg_if = rustPackages."registry+https://github.com/rust-lang/crates.io-index".cfg-if."0.1.10" { inherit profileName; };
      ${ if hostPlatform.parsed.kernel.name == "cloudabi" then "cloudabi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".cloudabi."0.0.3" { inherit profileName; };
      ${ if hostPlatform.isUnix then "libc" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      ${ if hostPlatform.parsed.kernel.name == "redox" then "syscall" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".redox_syscall."0.1.56" { inherit profileName; };
      smallvec = rustPackages."registry+https://github.com/rust-lang/crates.io-index".smallvec."0.6.14" { inherit profileName; };
      ${ if hostPlatform.isWindows then "winapi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.3.8" { inherit profileName; };
    };
    buildDependencies = {
      rustc_version = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".rustc_version."0.2.3" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".parking_lot_core."0.7.2" = overridableMkRustCrate (profileName: rec {
    name = "parking_lot_core";
    version = "0.7.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "d58c7c768d4ba344e3e8d72518ac13e259d7c7ade24167003b8488e10b6740a3"; };
    dependencies = {
      cfg_if = rustPackages."registry+https://github.com/rust-lang/crates.io-index".cfg-if."0.1.10" { inherit profileName; };
      ${ if hostPlatform.parsed.kernel.name == "cloudabi" then "cloudabi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".cloudabi."0.0.3" { inherit profileName; };
      ${ if hostPlatform.isUnix then "libc" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      ${ if hostPlatform.parsed.kernel.name == "redox" then "syscall" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".redox_syscall."0.1.56" { inherit profileName; };
      smallvec = rustPackages."registry+https://github.com/rust-lang/crates.io-index".smallvec."1.6.1" { inherit profileName; };
      ${ if hostPlatform.isWindows then "winapi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.3.8" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".parking_lot_core."0.8.0" = overridableMkRustCrate (profileName: rec {
    name = "parking_lot_core";
    version = "0.8.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "c361aa727dd08437f2f1447be8b59a33b0edd15e0fcee698f935613d9efbca9b"; };
    dependencies = {
      cfg_if = rustPackages."registry+https://github.com/rust-lang/crates.io-index".cfg-if."0.1.10" { inherit profileName; };
      ${ if hostPlatform.parsed.kernel.name == "cloudabi" then "cloudabi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".cloudabi."0.1.0" { inherit profileName; };
      instant = rustPackages."registry+https://github.com/rust-lang/crates.io-index".instant."0.1.6" { inherit profileName; };
      ${ if hostPlatform.isUnix then "libc" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      ${ if hostPlatform.parsed.kernel.name == "redox" then "syscall" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".redox_syscall."0.1.56" { inherit profileName; };
      smallvec = rustPackages."registry+https://github.com/rust-lang/crates.io-index".smallvec."1.6.1" { inherit profileName; };
      ${ if hostPlatform.isWindows then "winapi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.3.8" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".paste."0.1.16" = overridableMkRustCrate (profileName: rec {
    name = "paste";
    version = "0.1.16";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "d508492eeb1e5c38ee696371bf7b9fc33c83d46a7d451606b96458fbbbdc2dec"; };
    dependencies = {
      paste_impl = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".paste-impl."0.1.16" { profileName = "__noProfile"; };
      proc_macro_hack = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro-hack."0.5.19" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".paste-impl."0.1.16" = overridableMkRustCrate (profileName: rec {
    name = "paste-impl";
    version = "0.1.16";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "84f328a6a63192b333fce5fbb4be79db6758a4d518dfac6d54412f1492f72d32"; };
    dependencies = {
      proc_macro_hack = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro-hack."0.5.19" { profileName = "__noProfile"; };
      proc_macro2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."1.0.24" { inherit profileName; };
      quote = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quote."1.0.6" { inherit profileName; };
      syn = rustPackages."registry+https://github.com/rust-lang/crates.io-index".syn."1.0.60" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".pbkdf2."0.3.0" = overridableMkRustCrate (profileName: rec {
    name = "pbkdf2";
    version = "0.3.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "006c038a43a45995a9670da19e67600114740e8511d4333bf97a56e66a7542d9"; };
    dependencies = {
      byteorder = rustPackages."registry+https://github.com/rust-lang/crates.io-index".byteorder."1.3.4" { inherit profileName; };
      crypto_mac = rustPackages."registry+https://github.com/rust-lang/crates.io-index".crypto-mac."0.7.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".pbkdf2."0.4.0" = overridableMkRustCrate (profileName: rec {
    name = "pbkdf2";
    version = "0.4.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "216eaa586a190f0a738f2f918511eecfa90f13295abec0e457cdebcceda80cbd"; };
    dependencies = {
      crypto_mac = rustPackages."registry+https://github.com/rust-lang/crates.io-index".crypto-mac."0.8.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".pbkdf2."0.6.0" = overridableMkRustCrate (profileName: rec {
    name = "pbkdf2";
    version = "0.6.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "b3b8c0d71734018084da0c0354193a5edfb81b20d2d57a92c5b154aefc554a4a"; };
    dependencies = {
      crypto_mac = rustPackages."registry+https://github.com/rust-lang/crates.io-index".crypto-mac."0.10.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".peeking_take_while."0.1.2" = overridableMkRustCrate (profileName: rec {
    name = "peeking_take_while";
    version = "0.1.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "19b17cddbe7ec3f8bc800887bab5e717348c95ea2ca0b1bf0837fb964dc67099"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".percent-encoding."1.0.1" = overridableMkRustCrate (profileName: rec {
    name = "percent-encoding";
    version = "1.0.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "31010dd2e1ac33d5b46a5b413495239882813e0369f8ed8a5e266f173602f831"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".percent-encoding."2.1.0" = overridableMkRustCrate (profileName: rec {
    name = "percent-encoding";
    version = "2.1.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "d4fd5641d01c8f18a23da7b6fe29298ff4b55afcccdf78973b24cf3175fee32e"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".pest."2.1.3" = overridableMkRustCrate (profileName: rec {
    name = "pest";
    version = "2.1.3";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "10f4872ae94d7b90ae48754df22fd42ad52ce740b8f370b03da4835417403e53"; };
    dependencies = {
      ucd_trie = rustPackages."registry+https://github.com/rust-lang/crates.io-index".ucd-trie."0.1.3" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".pickledb."0.4.1" = overridableMkRustCrate (profileName: rec {
    name = "pickledb";
    version = "0.4.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "9161694d67f6c5163519d42be942ae36bbdb55f439460144f105bc4f9f7d1d61"; };
    dependencies = {
      bincode = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bincode."1.3.1" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      serde_cbor = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_cbor."0.11.1" { inherit profileName; };
      serde_json = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_json."1.0.62" { inherit profileName; };
      serde_yaml = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_yaml."0.8.13" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".pin-project."0.4.23" = overridableMkRustCrate (profileName: rec {
    name = "pin-project";
    version = "0.4.23";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "ca4433fff2ae79342e497d9f8ee990d174071408f28f726d6d83af93e58e48aa"; };
    dependencies = {
      pin_project_internal = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".pin-project-internal."0.4.23" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".pin-project."1.0.1" = overridableMkRustCrate (profileName: rec {
    name = "pin-project";
    version = "1.0.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "ee41d838744f60d959d7074e3afb6b35c7456d0f61cad38a24e35e6553f73841"; };
    dependencies = {
      pin_project_internal = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".pin-project-internal."1.0.1" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".pin-project-internal."0.4.23" = overridableMkRustCrate (profileName: rec {
    name = "pin-project-internal";
    version = "0.4.23";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "2c0e815c3ee9a031fdf5af21c10aa17c573c9c6a566328d99e3936c34e36461f"; };
    dependencies = {
      proc_macro2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."1.0.24" { inherit profileName; };
      quote = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quote."1.0.6" { inherit profileName; };
      syn = rustPackages."registry+https://github.com/rust-lang/crates.io-index".syn."1.0.60" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".pin-project-internal."1.0.1" = overridableMkRustCrate (profileName: rec {
    name = "pin-project-internal";
    version = "1.0.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "81a4ffa594b66bff340084d4081df649a7dc049ac8d7fc458d8e628bfbbb2f86"; };
    dependencies = {
      proc_macro2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."1.0.24" { inherit profileName; };
      quote = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quote."1.0.6" { inherit profileName; };
      syn = rustPackages."registry+https://github.com/rust-lang/crates.io-index".syn."1.0.60" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".pin-project-lite."0.1.5" = overridableMkRustCrate (profileName: rec {
    name = "pin-project-lite";
    version = "0.1.5";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "f7505eeebd78492e0f6108f7171c4948dbb120ee8119d9d77d0afa5469bef67f"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".pin-project-lite."0.2.4" = overridableMkRustCrate (profileName: rec {
    name = "pin-project-lite";
    version = "0.2.4";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "439697af366c49a6d0a010c56a0d97685bc140ce0d377b13a2ea2aa42d64a827"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".pin-utils."0.1.0" = overridableMkRustCrate (profileName: rec {
    name = "pin-utils";
    version = "0.1.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "8b870d8c151b6f2fb93e84a13146138f05d02ed11c7e7c54f8826aaaf7c9f184"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".pkg-config."0.3.17" = overridableMkRustCrate (profileName: rec {
    name = "pkg-config";
    version = "0.3.17";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "05da548ad6865900e60eaba7f589cc0783590a92e940c26953ff81ddbab2d677"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".plain."0.2.3" = overridableMkRustCrate (profileName: rec {
    name = "plain";
    version = "0.2.3";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "b4596b6d070b27117e987119b4dac604f3c58cfb0b191112e24771b2faeac1a6"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".ppv-lite86."0.2.8" = overridableMkRustCrate (profileName: rec {
    name = "ppv-lite86";
    version = "0.2.8";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "237a5ed80e274dbc66f86bd59c1e25edc039660be53194b5fe0a482e0f2612ea"; };
    features = builtins.concatLists [
      [ "simd" ]
      [ "std" ]
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".predicates."1.0.4" = overridableMkRustCrate (profileName: rec {
    name = "predicates";
    version = "1.0.4";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "347a1b6f0b21e636bc9872fb60b83b8e185f6f5516298b8238699f7f9a531030"; };
    features = builtins.concatLists [
      [ "difference" ]
    ];
    dependencies = {
      difference = rustPackages."registry+https://github.com/rust-lang/crates.io-index".difference."2.0.0" { inherit profileName; };
      predicates_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".predicates-core."1.0.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".predicates-core."1.0.0" = overridableMkRustCrate (profileName: rec {
    name = "predicates-core";
    version = "1.0.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "06075c3a3e92559ff8929e7a280684489ea27fe44805174c3ebd9328dcb37178"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".predicates-tree."1.0.0" = overridableMkRustCrate (profileName: rec {
    name = "predicates-tree";
    version = "1.0.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "8e63c4859013b38a76eca2414c64911fba30def9e3202ac461a2d22831220124"; };
    dependencies = {
      predicates_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".predicates-core."1.0.0" { inherit profileName; };
      treeline = rustPackages."registry+https://github.com/rust-lang/crates.io-index".treeline."0.1.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".pretty-hex."0.2.1" = overridableMkRustCrate (profileName: rec {
    name = "pretty-hex";
    version = "0.2.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "bc5c99d529f0d30937f6f4b8a86d988047327bb88d04d2c4afc356de74722131"; };
    features = builtins.concatLists [
      [ "alloc" ]
      [ "default" ]
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".proc-macro-crate."0.1.5" = overridableMkRustCrate (profileName: rec {
    name = "proc-macro-crate";
    version = "0.1.5";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "1d6ea3c4595b96363c13943497db34af4460fb474a95c43f4446ad341b8c9785"; };
    dependencies = {
      toml = rustPackages."registry+https://github.com/rust-lang/crates.io-index".toml."0.5.6" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".proc-macro-hack."0.5.19" = overridableMkRustCrate (profileName: rec {
    name = "proc-macro-hack";
    version = "0.5.19";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "dbf0c48bc1d91375ae5c3cd81e3722dff1abcf81a30960240640d223f59fe0e5"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".proc-macro-nested."0.1.4" = overridableMkRustCrate (profileName: rec {
    name = "proc-macro-nested";
    version = "0.1.4";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "8e946095f9d3ed29ec38de908c22f95d9ac008e424c7bcae54c75a79c527c694"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".proc-macro2."0.4.30" = overridableMkRustCrate (profileName: rec {
    name = "proc-macro2";
    version = "0.4.30";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "cf3d2011ab5c909338f7887f4fc896d35932e29146c12c8d01da6b22a80ba759"; };
    features = builtins.concatLists [
      [ "proc-macro" ]
    ];
    dependencies = {
      unicode_xid = rustPackages."registry+https://github.com/rust-lang/crates.io-index".unicode-xid."0.1.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".proc-macro2."1.0.24" = overridableMkRustCrate (profileName: rec {
    name = "proc-macro2";
    version = "1.0.24";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "1e0704ee1a7e00d7bb417d0770ea303c1bccbabf0ef1667dae92b5967f5f8a71"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "proc-macro" ]
    ];
    dependencies = {
      unicode_xid = rustPackages."registry+https://github.com/rust-lang/crates.io-index".unicode-xid."0.2.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".prost."0.7.0" = overridableMkRustCrate (profileName: rec {
    name = "prost";
    version = "0.7.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "9e6984d2f1a23009bd270b8bb56d0926810a3d483f59c987d77969e9d8e840b2"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "prost-derive" ]
      [ "std" ]
    ];
    dependencies = {
      bytes = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bytes."1.0.1" { inherit profileName; };
      prost_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".prost-derive."0.7.0" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".prost-derive."0.7.0" = overridableMkRustCrate (profileName: rec {
    name = "prost-derive";
    version = "0.7.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "169a15f3008ecb5160cba7d37bcd690a7601b6d30cfb87a117d45e59d52af5d4"; };
    dependencies = {
      anyhow = rustPackages."registry+https://github.com/rust-lang/crates.io-index".anyhow."1.0.38" { inherit profileName; };
      itertools = rustPackages."registry+https://github.com/rust-lang/crates.io-index".itertools."0.9.0" { inherit profileName; };
      proc_macro2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."1.0.24" { inherit profileName; };
      quote = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quote."1.0.6" { inherit profileName; };
      syn = rustPackages."registry+https://github.com/rust-lang/crates.io-index".syn."1.0.60" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".prost-types."0.7.0" = overridableMkRustCrate (profileName: rec {
    name = "prost-types";
    version = "0.7.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "b518d7cdd93dab1d1122cf07fa9a60771836c668dde9d9e2a139f957f0d9f1bb"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "std" ]
    ];
    dependencies = {
      bytes = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bytes."1.0.1" { inherit profileName; };
      prost = rustPackages."registry+https://github.com/rust-lang/crates.io-index".prost."0.7.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".quick-error."1.2.3" = overridableMkRustCrate (profileName: rec {
    name = "quick-error";
    version = "1.2.3";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "a1d01941d82fa2ab50be1e79e6714289dd7cde78eba4c074bc5a4374f650dfe0"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".quote."0.6.13" = overridableMkRustCrate (profileName: rec {
    name = "quote";
    version = "0.6.13";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "6ce23b6b870e8f94f81fb0a363d65d86675884b34a09043c81e5562f11c1f8e1"; };
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "default")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "proc-macro")
    ];
    dependencies = {
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "proc_macro2" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."0.4.30" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".quote."1.0.6" = overridableMkRustCrate (profileName: rec {
    name = "quote";
    version = "1.0.6";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "54a21852a652ad6f610c9510194f398ff6f8692e334fd1145fed931f7fbe44ea"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "proc-macro" ]
    ];
    dependencies = {
      proc_macro2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."1.0.24" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".rand."0.4.6" = overridableMkRustCrate (profileName: rec {
    name = "rand";
    version = "0.4.6";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "552840b97013b1a26992c11eac34bdd778e464601a4c2054b5f0bff7c6761293"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "libc" ]
      [ "std" ]
    ];
    dependencies = {
      ${ if hostPlatform.parsed.kernel.name == "fuchsia" then "fuchsia_cprng" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".fuchsia-cprng."0.1.1" { inherit profileName; };
      ${ if hostPlatform.isUnix then "libc" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      ${ if hostPlatform.parsed.abi.name == "sgx" then "rand_core" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand_core."0.3.1" { inherit profileName; };
      ${ if hostPlatform.parsed.abi.name == "sgx" then "rdrand" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rdrand."0.4.0" { inherit profileName; };
      ${ if hostPlatform.isWindows then "winapi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.3.8" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".rand."0.6.5" = overridableMkRustCrate (profileName: rec {
    name = "rand";
    version = "0.6.5";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "6d71dacdc3c88c1fde3885a3be3fbab9f35724e6ce99467f7d9c5026132184ca"; };
    features = builtins.concatLists [
      [ "alloc" ]
      [ "default" ]
      [ "rand_os" ]
      [ "std" ]
    ];
    dependencies = {
      ${ if hostPlatform.isUnix then "libc" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      rand_chacha = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand_chacha."0.1.1" { inherit profileName; };
      rand_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand_core."0.4.2" { inherit profileName; };
      rand_hc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand_hc."0.1.0" { inherit profileName; };
      rand_isaac = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand_isaac."0.1.1" { inherit profileName; };
      rand_jitter = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand_jitter."0.1.4" { inherit profileName; };
      rand_os = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand_os."0.1.3" { inherit profileName; };
      rand_pcg = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand_pcg."0.1.2" { inherit profileName; };
      rand_xorshift = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand_xorshift."0.1.1" { inherit profileName; };
      ${ if hostPlatform.isWindows then "winapi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.3.8" { inherit profileName; };
    };
    buildDependencies = {
      autocfg = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".autocfg."0.1.7" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".rand."0.7.3" = overridableMkRustCrate (profileName: rec {
    name = "rand";
    version = "0.7.3";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "6a6b1679d49b24bbfe0c803429aa1874472f50d9b363131f0e89fc356b544d03"; };
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "alloc")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "default")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "getrandom")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "getrandom_package")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "libc")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "rand_pcg")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "small_rng")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "std")
    ];
    dependencies = {
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "getrandom_package" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".getrandom."0.1.14" { inherit profileName; };
      ${ if (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") && hostPlatform.isUnix then "libc" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      ${ if (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") && !(hostPlatform.parsed.kernel.name == "emscripten") then "rand_chacha" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand_chacha."0.2.2" { inherit profileName; };
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "rand_core" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand_core."0.5.1" { inherit profileName; };
      ${ if (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") && hostPlatform.parsed.kernel.name == "emscripten" then "rand_hc" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand_hc."0.2.0" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" then "rand_pcg" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand_pcg."0.2.1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".rand."0.8.3" = overridableMkRustCrate (profileName: rec {
    name = "rand";
    version = "0.8.3";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "0ef9e7e66b4468674bfcb0c81af8b7fa0bb154fa9f28eb840da5c447baeb8d7e"; };
    features = builtins.concatLists [
      [ "alloc" ]
      [ "default" ]
      [ "getrandom" ]
      [ "libc" ]
      [ "rand_chacha" ]
      [ "rand_hc" ]
      [ "small_rng" ]
      [ "std" ]
      [ "std_rng" ]
    ];
    dependencies = {
      ${ if hostPlatform.isUnix then "libc" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      ${ if !(hostPlatform.parsed.kernel.name == "emscripten") then "rand_chacha" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand_chacha."0.3.0" { inherit profileName; };
      rand_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand_core."0.6.2" { inherit profileName; };
      ${ if hostPlatform.parsed.kernel.name == "emscripten" then "rand_hc" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand_hc."0.3.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".rand_chacha."0.1.1" = overridableMkRustCrate (profileName: rec {
    name = "rand_chacha";
    version = "0.1.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "556d3a1ca6600bfcbab7c7c91ccb085ac7fbbcd70e008a98742e7847f4f7bcef"; };
    dependencies = {
      rand_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand_core."0.3.1" { inherit profileName; };
    };
    buildDependencies = {
      autocfg = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".autocfg."0.1.7" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".rand_chacha."0.2.2" = overridableMkRustCrate (profileName: rec {
    name = "rand_chacha";
    version = "0.2.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "f4c8ed856279c9737206bf725bf36935d8666ead7aa69b52be55af369d193402"; };
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/rand_chacha" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "default")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/rand_chacha" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "simd")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "std")
    ];
    dependencies = {
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "ppv_lite86" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".ppv-lite86."0.2.8" { inherit profileName; };
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "rand_core" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand_core."0.5.1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".rand_chacha."0.3.0" = overridableMkRustCrate (profileName: rec {
    name = "rand_chacha";
    version = "0.3.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "e12735cf05c9e10bf21534da50a147b924d555dc7a547c42e6bb2d5b6017ae0d"; };
    features = builtins.concatLists [
      [ "std" ]
    ];
    dependencies = {
      ppv_lite86 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".ppv-lite86."0.2.8" { inherit profileName; };
      rand_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand_core."0.6.2" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".rand_core."0.3.1" = overridableMkRustCrate (profileName: rec {
    name = "rand_core";
    version = "0.3.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "7a6fdeb83b075e8266dcc8762c22776f6877a63111121f5f8c7411e5be7eed4b"; };
    dependencies = {
      rand_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand_core."0.4.2" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".rand_core."0.4.2" = overridableMkRustCrate (profileName: rec {
    name = "rand_core";
    version = "0.4.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "9c33a3c44ca05fa6f1807d8e6743f3824e8509beca625669633be0acbdf509dc"; };
    features = builtins.concatLists [
      [ "alloc" ]
      [ "std" ]
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".rand_core."0.5.1" = overridableMkRustCrate (profileName: rec {
    name = "rand_core";
    version = "0.5.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "90bde5296fc891b0cef12a6d03ddccc162ce7b2aff54160af9338f8d40df6d19"; };
    features = builtins.concatLists [
      [ "alloc" ]
      [ "getrandom" ]
      [ "std" ]
    ];
    dependencies = {
      getrandom = rustPackages."registry+https://github.com/rust-lang/crates.io-index".getrandom."0.1.14" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".rand_core."0.6.2" = overridableMkRustCrate (profileName: rec {
    name = "rand_core";
    version = "0.6.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "34cf66eb183df1c5876e2dcf6b13d57340741e8dc255b48e40a26de954d06ae7"; };
    features = builtins.concatLists [
      [ "alloc" ]
      [ "getrandom" ]
      [ "std" ]
    ];
    dependencies = {
      getrandom = rustPackages."registry+https://github.com/rust-lang/crates.io-index".getrandom."0.2.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".rand_hc."0.1.0" = overridableMkRustCrate (profileName: rec {
    name = "rand_hc";
    version = "0.1.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "7b40677c7be09ae76218dc623efbf7b18e34bced3f38883af07bb75630a21bc4"; };
    dependencies = {
      rand_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand_core."0.3.1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".rand_hc."0.2.0" = overridableMkRustCrate (profileName: rec {
    name = "rand_hc";
    version = "0.2.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "ca3129af7b92a17112d59ad498c6f81eaf463253766b90396d39ea7a39d6613c"; };
    dependencies = {
      rand_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand_core."0.5.1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".rand_hc."0.3.0" = overridableMkRustCrate (profileName: rec {
    name = "rand_hc";
    version = "0.3.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "3190ef7066a446f2e7f42e239d161e905420ccab01eb967c9eb27d21b2322a73"; };
    dependencies = {
      rand_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand_core."0.6.2" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".rand_isaac."0.1.1" = overridableMkRustCrate (profileName: rec {
    name = "rand_isaac";
    version = "0.1.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "ded997c9d5f13925be2a6fd7e66bf1872597f759fd9dd93513dd7e92e5a5ee08"; };
    dependencies = {
      rand_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand_core."0.3.1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".rand_jitter."0.1.4" = overridableMkRustCrate (profileName: rec {
    name = "rand_jitter";
    version = "0.1.4";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "1166d5c91dc97b88d1decc3285bb0a99ed84b05cfd0bc2341bdf2d43fc41e39b"; };
    features = builtins.concatLists [
      [ "std" ]
    ];
    dependencies = {
      ${ if hostPlatform.parsed.kernel.name == "darwin" || hostPlatform.parsed.kernel.name == "ios" then "libc" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      rand_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand_core."0.4.2" { inherit profileName; };
      ${ if hostPlatform.parsed.kernel.name == "windows" then "winapi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.3.8" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".rand_os."0.1.3" = overridableMkRustCrate (profileName: rec {
    name = "rand_os";
    version = "0.1.3";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "7b75f676a1e053fc562eafbb47838d67c84801e38fc1ba459e8f180deabd5071"; };
    dependencies = {
      ${ if hostPlatform.parsed.kernel.name == "cloudabi" then "cloudabi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".cloudabi."0.0.3" { inherit profileName; };
      ${ if hostPlatform.parsed.kernel.name == "fuchsia" then "fuchsia_cprng" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".fuchsia-cprng."0.1.1" { inherit profileName; };
      ${ if hostPlatform.isUnix then "libc" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      rand_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand_core."0.4.2" { inherit profileName; };
      ${ if hostPlatform.parsed.abi.name == "sgx" then "rdrand" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rdrand."0.4.0" { inherit profileName; };
      ${ if hostPlatform.isWindows then "winapi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.3.8" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".rand_pcg."0.1.2" = overridableMkRustCrate (profileName: rec {
    name = "rand_pcg";
    version = "0.1.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "abf9b09b01790cfe0364f52bf32995ea3c39f4d2dd011eac241d2914146d0b44"; };
    dependencies = {
      rand_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand_core."0.4.2" { inherit profileName; };
    };
    buildDependencies = {
      autocfg = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".autocfg."0.1.7" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".rand_pcg."0.2.1" = overridableMkRustCrate (profileName: rec {
    name = "rand_pcg";
    version = "0.2.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "16abd0c1b639e9eb4d7c50c0b8100b0d0f849be2349829c740fe8e6eb4816429"; };
    dependencies = {
      rand_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand_core."0.5.1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".rand_xorshift."0.1.1" = overridableMkRustCrate (profileName: rec {
    name = "rand_xorshift";
    version = "0.1.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "cbf7e9e623549b0e21f6e97cf8ecf247c1a8fd2e8a992ae265314300b2455d5c"; };
    dependencies = {
      rand_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand_core."0.3.1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".raptorq."1.4.2" = overridableMkRustCrate (profileName: rec {
    name = "raptorq";
    version = "1.4.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "8e3e0cd5c27717803cbd3151329de9aa784376703a3a850b00c0dae30da86cf2"; };
    dependencies = {
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".rayon."1.5.0" = overridableMkRustCrate (profileName: rec {
    name = "rayon";
    version = "1.5.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "8b0d8e0819fadc20c74ea8373106ead0600e3a67ef1fe8da56e39b9ae7275674"; };
    dependencies = {
      crossbeam_deque = rustPackages."registry+https://github.com/rust-lang/crates.io-index".crossbeam-deque."0.8.0" { inherit profileName; };
      either = rustPackages."registry+https://github.com/rust-lang/crates.io-index".either."1.5.3" { inherit profileName; };
      rayon_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rayon-core."1.9.0" { inherit profileName; };
    };
    buildDependencies = {
      autocfg = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".autocfg."1.0.0" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".rayon-core."1.9.0" = overridableMkRustCrate (profileName: rec {
    name = "rayon-core";
    version = "1.9.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "9ab346ac5921dc62ffa9f89b7a773907511cdfa5490c572ae9be1be33e8afa4a"; };
    dependencies = {
      crossbeam_channel = rustPackages."registry+https://github.com/rust-lang/crates.io-index".crossbeam-channel."0.5.0" { inherit profileName; };
      crossbeam_deque = rustPackages."registry+https://github.com/rust-lang/crates.io-index".crossbeam-deque."0.8.0" { inherit profileName; };
      crossbeam_utils = rustPackages."registry+https://github.com/rust-lang/crates.io-index".crossbeam-utils."0.8.1" { inherit profileName; };
      lazy_static = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
      num_cpus = rustPackages."registry+https://github.com/rust-lang/crates.io-index".num_cpus."1.13.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".rdrand."0.4.0" = overridableMkRustCrate (profileName: rec {
    name = "rdrand";
    version = "0.4.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "678054eb77286b51581ba43620cc911abf02758c91f93f479767aed0f90458b2"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "std" ]
    ];
    dependencies = {
      rand_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand_core."0.3.1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".redox_syscall."0.1.56" = overridableMkRustCrate (profileName: rec {
    name = "redox_syscall";
    version = "0.1.56";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "2439c63f3f6139d1b57529d16bc3b8bb855230c8efcc5d3a896c8bea7c3b1e84"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".redox_users."0.3.4" = overridableMkRustCrate (profileName: rec {
    name = "redox_users";
    version = "0.3.4";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "09b23093265f8d200fa7b4c2c76297f47e681c655f6f1285a8780d6a022f7431"; };
    dependencies = {
      getrandom = rustPackages."registry+https://github.com/rust-lang/crates.io-index".getrandom."0.1.14" { inherit profileName; };
      syscall = rustPackages."registry+https://github.com/rust-lang/crates.io-index".redox_syscall."0.1.56" { inherit profileName; };
      argon2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rust-argon2."0.7.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".reed-solomon-erasure."4.0.2" = overridableMkRustCrate (profileName: rec {
    name = "reed-solomon-erasure";
    version = "4.0.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "a415a013dd7c5d4221382329a5a3482566da675737494935cbbbcdec04662f9d"; };
    features = builtins.concatLists [
      [ "cc" ]
      [ "default" ]
      [ "libc" ]
      [ "simd-accel" ]
    ];
    dependencies = {
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      smallvec = rustPackages."registry+https://github.com/rust-lang/crates.io-index".smallvec."1.6.1" { inherit profileName; };
    };
    buildDependencies = {
      cc = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".cc."1.0.49" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".regex."1.3.9" = overridableMkRustCrate (profileName: rec {
    name = "regex";
    version = "1.3.9";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "9c3780fcf44b193bc4d09f36d2a3c87b251da4a046c87795a0d35f4f927ad8e6"; };
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-logger" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "aho-corasick")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-watchtower") "default")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-logger" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "memchr")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-logger" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "perf")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-logger" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "perf-cache")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-logger" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "perf-dfa")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-logger" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "perf-inline")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-logger" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "perf-literal")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-logger" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "std")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-logger" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "thread_local")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-watchtower") "unicode")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-watchtower") "unicode-age")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-watchtower") "unicode-bool")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-watchtower") "unicode-case")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-watchtower") "unicode-gencat")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-watchtower") "unicode-perl")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-watchtower") "unicode-script")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-watchtower") "unicode-segment")
    ];
    dependencies = {
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-logger" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "aho_corasick" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".aho-corasick."0.7.10" { inherit profileName; };
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-logger" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "memchr" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".memchr."2.3.3" { inherit profileName; };
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-logger" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "regex_syntax" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".regex-syntax."0.6.18" { inherit profileName; };
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-logger" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "thread_local" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".thread_local."1.0.1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".regex-automata."0.1.9" = overridableMkRustCrate (profileName: rec {
    name = "regex-automata";
    version = "0.1.9";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "ae1ded71d66a4a97f5e961fd0cb25a5f366a42a41570d16a763a69c092c26ae4"; };
    dependencies = {
      byteorder = rustPackages."registry+https://github.com/rust-lang/crates.io-index".byteorder."1.3.4" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".regex-syntax."0.6.18" = overridableMkRustCrate (profileName: rec {
    name = "regex-syntax";
    version = "0.6.18";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "26412eb97c6b088a6997e05f69403a802a92d520de2f8e63c2b65f9e0f47c4e8"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "unicode" ]
      [ "unicode-age" ]
      [ "unicode-bool" ]
      [ "unicode-case" ]
      [ "unicode-gencat" ]
      [ "unicode-perl" ]
      [ "unicode-script" ]
      [ "unicode-segment" ]
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".remove_dir_all."0.5.0" = overridableMkRustCrate (profileName: rec {
    name = "remove_dir_all";
    version = "0.5.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "dfc5b3ce5d5ea144bb04ebd093a9e14e9765bcfec866aecda9b6dec43b3d1e24"; };
    dependencies = {
      ${ if hostPlatform.isWindows then "winapi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.3.8" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".reqwest."0.11.2" = overridableMkRustCrate (profileName: rec {
    name = "reqwest";
    version = "0.11.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "bf12057f289428dbf5c591c74bf10392e4a8003f993405a902f20117019022d4"; };
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "__rustls")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "__tls")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "blocking")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "default")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "default-tls")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "hyper-rustls")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "hyper-tls")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "json")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "native-tls-crate")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "rustls")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "rustls-tls")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "rustls-tls-webpki-roots")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "serde_json")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "tokio-native-tls")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "tokio-rustls")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "webpki-roots")
    ];
    dependencies = {
      ${ if (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") && !(hostPlatform.parsed.cpu.name == "wasm32") then "base64" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".base64."0.13.0" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "bytes" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bytes."1.0.1" { inherit profileName; };
      ${ if (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") && !(hostPlatform.parsed.cpu.name == "wasm32") then "encoding_rs" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".encoding_rs."0.8.23" { inherit profileName; };
      ${ if (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") && !(hostPlatform.parsed.cpu.name == "wasm32") then "futures_core" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-core."0.3.8" { inherit profileName; };
      ${ if (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") && !(hostPlatform.parsed.cpu.name == "wasm32") then "futures_util" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-util."0.3.8" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "http" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".http."0.2.1" { inherit profileName; };
      ${ if (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") && !(hostPlatform.parsed.cpu.name == "wasm32") then "http_body" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".http-body."0.4.0" { inherit profileName; };
      ${ if (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") && !(hostPlatform.parsed.cpu.name == "wasm32") then "hyper" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".hyper."0.14.3" { inherit profileName; };
      ${ if (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") && !(hostPlatform.parsed.cpu.name == "wasm32") then "hyper_rustls" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".hyper-rustls."0.22.1" { inherit profileName; };
      ${ if (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") && !(hostPlatform.parsed.cpu.name == "wasm32") then "hyper_tls" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".hyper-tls."0.5.0" { inherit profileName; };
      ${ if (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") && !(hostPlatform.parsed.cpu.name == "wasm32") then "ipnet" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".ipnet."2.3.0" { inherit profileName; };
      ${ if (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") && hostPlatform.parsed.cpu.name == "wasm32" then "js_sys" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".js-sys."0.3.47" { inherit profileName; };
      ${ if (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") && !(hostPlatform.parsed.cpu.name == "wasm32") then "lazy_static" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
      ${ if (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") && !(hostPlatform.parsed.cpu.name == "wasm32") then "log" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      ${ if (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") && !(hostPlatform.parsed.cpu.name == "wasm32") then "mime" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".mime."0.3.16" { inherit profileName; };
      ${ if (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") && !(hostPlatform.parsed.cpu.name == "wasm32") then "native_tls_crate" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".native-tls."0.2.7" { inherit profileName; };
      ${ if (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") && !(hostPlatform.parsed.cpu.name == "wasm32") then "percent_encoding" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".percent-encoding."2.1.0" { inherit profileName; };
      ${ if (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") && !(hostPlatform.parsed.cpu.name == "wasm32") then "pin_project_lite" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".pin-project-lite."0.2.4" { inherit profileName; };
      ${ if (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") && !(hostPlatform.parsed.cpu.name == "wasm32") then "rustls" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rustls."0.19.0" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "serde" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "serde_json" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_json."1.0.62" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "serde_urlencoded" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_urlencoded."0.7.0" { inherit profileName; };
      ${ if (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") && !(hostPlatform.parsed.cpu.name == "wasm32") then "tokio" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio."1.1.1" { inherit profileName; };
      ${ if (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") && !(hostPlatform.parsed.cpu.name == "wasm32") then "tokio_native_tls" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-native-tls."0.3.0" { inherit profileName; };
      ${ if (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") && !(hostPlatform.parsed.cpu.name == "wasm32") then "tokio_rustls" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-rustls."0.22.0" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "url" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".url."2.2.0" { inherit profileName; };
      ${ if (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") && hostPlatform.parsed.cpu.name == "wasm32" then "wasm_bindgen" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".wasm-bindgen."0.2.70" { inherit profileName; };
      ${ if (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") && hostPlatform.parsed.cpu.name == "wasm32" then "wasm_bindgen_futures" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".wasm-bindgen-futures."0.4.20" { inherit profileName; };
      ${ if (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") && hostPlatform.parsed.cpu.name == "wasm32" then "web_sys" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".web-sys."0.3.40" { inherit profileName; };
      ${ if (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") && !(hostPlatform.parsed.cpu.name == "wasm32") then "webpki_roots" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".webpki-roots."0.21.0" { inherit profileName; };
      ${ if (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") && hostPlatform.isWindows then "winreg" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winreg."0.7.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".retain_mut."0.1.2" = overridableMkRustCrate (profileName: rec {
    name = "retain_mut";
    version = "0.1.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "53552c6c49e1e13f1a203ef0080ab3bbef0beb570a528993e83df057a9d9bba1"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".ring."0.16.12" = overridableMkRustCrate (profileName: rec {
    name = "ring";
    version = "0.16.12";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "1ba5a8ec64ee89a76c98c549af81ff14813df09c3e6dc4766c3856da48597a0c"; };
    features = builtins.concatLists [
      [ "alloc" ]
      [ "default" ]
      [ "dev_urandom_fallback" ]
      [ "lazy_static" ]
    ];
    dependencies = {
      ${ if hostPlatform.parsed.kernel.name == "android" || hostPlatform.parsed.kernel.name == "freebsd" || hostPlatform.parsed.kernel.name == "linux" || hostPlatform.parsed.kernel.name == "netbsd" || hostPlatform.parsed.kernel.name == "openbsd" || hostPlatform.parsed.kernel.name == "solaris" then "lazy_static" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
      ${ if hostPlatform.parsed.kernel.name == "android" || hostPlatform.parsed.kernel.name == "linux" then "libc" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      ${ if (hostPlatform.parsed.cpu.name == "aarch64" || hostPlatform.parsed.cpu.name == "arm" || hostPlatform.parsed.cpu.name == "x86" || hostPlatform.parsed.cpu.name == "x86_64") && !(hostPlatform.parsed.kernel.name == "ios") then "spin" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".spin."0.5.2" { inherit profileName; };
      untrusted = rustPackages."registry+https://github.com/rust-lang/crates.io-index".untrusted."0.7.1" { inherit profileName; };
      ${ if hostPlatform.parsed.cpu.name == "wasm32" && hostPlatform.parsed.vendor.name == "unknown" && hostPlatform.parsed.kernel.name == "unknown" && hostPlatform.parsed.abi.name == "" then "web_sys" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".web-sys."0.3.40" { inherit profileName; };
      ${ if hostPlatform.parsed.kernel.name == "windows" then "winapi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.3.8" { inherit profileName; };
    };
    buildDependencies = {
      cc = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".cc."1.0.49" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".rocksdb."0.15.0" = overridableMkRustCrate (profileName: rec {
    name = "rocksdb";
    version = "0.15.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "23d83c02c429044d58474eaf5ae31e062d0de894e21125b47437ec0edc1397e6"; };
    features = builtins.concatLists [
      [ "lz4" ]
    ];
    dependencies = {
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      librocksdb_sys = rustPackages."registry+https://github.com/rust-lang/crates.io-index".librocksdb-sys."6.11.4" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".rpassword."4.0.5" = overridableMkRustCrate (profileName: rec {
    name = "rpassword";
    version = "4.0.5";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "99371657d3c8e4d816fb6221db98fa408242b0b53bac08f8676a41f8554fe99f"; };
    dependencies = {
      ${ if hostPlatform.isUnix then "libc" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      ${ if hostPlatform.isWindows then "winapi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.3.8" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".rust-argon2."0.7.0" = overridableMkRustCrate (profileName: rec {
    name = "rust-argon2";
    version = "0.7.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "2bc8af4bda8e1ff4932523b94d3dd20ee30a87232323eda55903ffd71d2fb017"; };
    dependencies = {
      base64 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".base64."0.11.0" { inherit profileName; };
      blake2b_simd = rustPackages."registry+https://github.com/rust-lang/crates.io-index".blake2b_simd."0.5.10" { inherit profileName; };
      constant_time_eq = rustPackages."registry+https://github.com/rust-lang/crates.io-index".constant_time_eq."0.1.5" { inherit profileName; };
      crossbeam_utils = rustPackages."registry+https://github.com/rust-lang/crates.io-index".crossbeam-utils."0.7.2" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".rustc-demangle."0.1.16" = overridableMkRustCrate (profileName: rec {
    name = "rustc-demangle";
    version = "0.1.16";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "4c691c0e608126e00913e33f0ccf3727d5fc84573623b8d65b2df340b5201783"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".rustc-hash."1.1.0" = overridableMkRustCrate (profileName: rec {
    name = "rustc-hash";
    version = "1.1.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "08d43f7aa6b08d49f382cde6a7982047c3426db949b1424bc4b7ec9ae12c6ce2"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "std" ]
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".rustc_version."0.2.3" = overridableMkRustCrate (profileName: rec {
    name = "rustc_version";
    version = "0.2.3";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "138e3e0acb6c9fb258b19b67cb8abd63c00679d2851805ea151465464fe9030a"; };
    dependencies = {
      semver = rustPackages."registry+https://github.com/rust-lang/crates.io-index".semver."0.9.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".rustls."0.19.0" = overridableMkRustCrate (profileName: rec {
    name = "rustls";
    version = "0.19.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "064fd21ff87c6e87ed4506e68beb42459caa4a0e2eb144932e6776768556980b"; };
    features = builtins.concatLists [
      [ "dangerous_configuration" ]
      [ "default" ]
      [ "log" ]
      [ "logging" ]
    ];
    dependencies = {
      base64 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".base64."0.13.0" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      ring = rustPackages."registry+https://github.com/rust-lang/crates.io-index".ring."0.16.12" { inherit profileName; };
      sct = rustPackages."registry+https://github.com/rust-lang/crates.io-index".sct."0.6.0" { inherit profileName; };
      webpki = rustPackages."registry+https://github.com/rust-lang/crates.io-index".webpki."0.21.2" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".rustversion."1.0.4" = overridableMkRustCrate (profileName: rec {
    name = "rustversion";
    version = "1.0.4";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "cb5d2a036dc6d2d8fd16fde3498b04306e29bd193bf306a57427019b823d5acd"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".ryu."1.0.4" = overridableMkRustCrate (profileName: rec {
    name = "ryu";
    version = "1.0.4";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "ed3d612bc64430efeb3f7ee6ef26d590dce0c43249217bddc62112540c7941e1"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".safemem."0.3.3" = overridableMkRustCrate (profileName: rec {
    name = "safemem";
    version = "0.3.3";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "ef703b7cb59335eae2eb93ceb664c0eb7ea6bf567079d843e09420219668e072"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "std" ]
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".same-file."1.0.6" = overridableMkRustCrate (profileName: rec {
    name = "same-file";
    version = "1.0.6";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "93fc1dc3aaa9bfed95e02e6eadabb4baf7e3078b0bd1b4d7b6b0b68378900502"; };
    dependencies = {
      ${ if hostPlatform.isWindows then "winapi_util" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi-util."0.1.5" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".schannel."0.1.19" = overridableMkRustCrate (profileName: rec {
    name = "schannel";
    version = "0.1.19";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "8f05ba609c234e60bee0d547fe94a4c7e9da733d1c962cf6e59efa4cd9c8bc75"; };
    dependencies = {
      lazy_static = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
      winapi = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.3.8" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".scopeguard."1.1.0" = overridableMkRustCrate (profileName: rec {
    name = "scopeguard";
    version = "1.1.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "d29ab0c6d3fc0ee92fe66e2d99f700eab17a8d57d1c1d3b748380fb20baa78cd"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".scroll."0.10.1" = overridableMkRustCrate (profileName: rec {
    name = "scroll";
    version = "0.10.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "abb2332cb595d33f7edd5700f4cbf94892e680c7f0ae56adab58a35190b66cb1"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "derive" ]
      [ "scroll_derive" ]
      [ "std" ]
    ];
    dependencies = {
      scroll_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".scroll_derive."0.10.2" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".scroll_derive."0.10.2" = overridableMkRustCrate (profileName: rec {
    name = "scroll_derive";
    version = "0.10.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "e367622f934864ffa1c704ba2b82280aab856e3d8213c84c5720257eb34b15b9"; };
    dependencies = {
      proc_macro2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."1.0.24" { inherit profileName; };
      quote = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quote."1.0.6" { inherit profileName; };
      syn = rustPackages."registry+https://github.com/rust-lang/crates.io-index".syn."1.0.60" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".sct."0.6.0" = overridableMkRustCrate (profileName: rec {
    name = "sct";
    version = "0.6.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "e3042af939fca8c3453b7af0f1c66e533a15a86169e39de2657310ade8f98d3c"; };
    dependencies = {
      ring = rustPackages."registry+https://github.com/rust-lang/crates.io-index".ring."0.16.12" { inherit profileName; };
      untrusted = rustPackages."registry+https://github.com/rust-lang/crates.io-index".untrusted."0.7.1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".security-framework."2.1.2" = overridableMkRustCrate (profileName: rec {
    name = "security-framework";
    version = "2.1.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "d493c5f39e02dfb062cd8f33301f90f9b13b650e8c1b1d0fd75c19dd64bff69d"; };
    features = builtins.concatLists [
      [ "OSX_10_9" ]
      [ "default" ]
    ];
    dependencies = {
      bitflags = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bitflags."1.2.1" { inherit profileName; };
      core_foundation = rustPackages."registry+https://github.com/rust-lang/crates.io-index".core-foundation."0.9.1" { inherit profileName; };
      core_foundation_sys = rustPackages."registry+https://github.com/rust-lang/crates.io-index".core-foundation-sys."0.8.2" { inherit profileName; };
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      security_framework_sys = rustPackages."registry+https://github.com/rust-lang/crates.io-index".security-framework-sys."2.1.1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".security-framework-sys."2.1.1" = overridableMkRustCrate (profileName: rec {
    name = "security-framework-sys";
    version = "2.1.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "dee48cdde5ed250b0d3252818f646e174ab414036edb884dde62d80a3ac6082d"; };
    features = builtins.concatLists [
      [ "OSX_10_9" ]
      [ "default" ]
    ];
    dependencies = {
      core_foundation_sys = rustPackages."registry+https://github.com/rust-lang/crates.io-index".core-foundation-sys."0.8.2" { inherit profileName; };
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".semver."0.9.0" = overridableMkRustCrate (profileName: rec {
    name = "semver";
    version = "0.9.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "1d7eb9ef2c18661902cc47e535f9bc51b78acd254da71d375c2f6720d9a40403"; };
    features = builtins.concatLists [
      [ "default" ]
    ];
    dependencies = {
      semver_parser = rustPackages."registry+https://github.com/rust-lang/crates.io-index".semver-parser."0.7.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".semver."0.11.0" = overridableMkRustCrate (profileName: rec {
    name = "semver";
    version = "0.11.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "f301af10236f6df4160f7c3f04eec6dbc70ace82d23326abad5edee88801c6b6"; };
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cargo-test-bpf" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower") "default")
      (lib.optional (rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cargo-test-bpf") "serde")
    ];
    dependencies = {
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cargo-test-bpf" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower" then "semver_parser" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".semver-parser."0.10.1" { inherit profileName; };
      ${ if rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cargo-test-bpf" then "serde" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".semver-parser."0.7.0" = overridableMkRustCrate (profileName: rec {
    name = "semver-parser";
    version = "0.7.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "388a1df253eca08550bef6c72392cfe7c30914bf41df5269b68cbd6ff8f570a3"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".semver-parser."0.10.1" = overridableMkRustCrate (profileName: rec {
    name = "semver-parser";
    version = "0.10.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "42ef146c2ad5e5f4b037cd6ce2ebb775401729b19a82040c1beac9d36c7d1428"; };
    dependencies = {
      pest = rustPackages."registry+https://github.com/rust-lang/crates.io-index".pest."2.1.3" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" = overridableMkRustCrate (profileName: rec {
    name = "serde";
    version = "1.0.122";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "974ef1bd2ad8a507599b336595454081ff68a9599b4890af7643c0c0ed73a62c"; };
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cargo-test-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-config" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-scripts" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "default")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cargo-test-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-scripts" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "derive")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "rc")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cargo-test-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-scripts" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "serde_derive")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cargo-test-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-config" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-scripts" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "std")
    ];
    dependencies = {
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cargo-test-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-scripts" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "serde_derive" else null } = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_derive."1.0.122" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".serde_bytes."0.11.4" = overridableMkRustCrate (profileName: rec {
    name = "serde_bytes";
    version = "0.11.4";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "3bf487fbf5c6239d7ea2ff8b10cb6b811cd4b5080d1c2aeed1dec18753c06e10"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "std" ]
    ];
    dependencies = {
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".serde_cbor."0.11.1" = overridableMkRustCrate (profileName: rec {
    name = "serde_cbor";
    version = "0.11.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "1e18acfa2f90e8b735b2836ab8d538de304cbb6729a7360729ea5a895d15a622"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "std" ]
    ];
    dependencies = {
      half = rustPackages."registry+https://github.com/rust-lang/crates.io-index".half."1.6.0" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".serde_derive."1.0.122" = overridableMkRustCrate (profileName: rec {
    name = "serde_derive";
    version = "1.0.122";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "8dee1f300f838c8ac340ecb0112b3ac472464fa67e87292bdb3dfc9c49128e17"; };
    features = builtins.concatLists [
      [ "default" ]
    ];
    dependencies = {
      proc_macro2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."1.0.24" { inherit profileName; };
      quote = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quote."1.0.6" { inherit profileName; };
      syn = rustPackages."registry+https://github.com/rust-lang/crates.io-index".syn."1.0.60" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".serde_json."1.0.62" = overridableMkRustCrate (profileName: rec {
    name = "serde_json";
    version = "1.0.62";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "ea1c6153794552ea7cf7cf63b1231a25de00ec90db326ba6264440fa08e31486"; };
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/serde_json" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cargo-test-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "default")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/serde_json" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cargo-test-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "std")
      (lib.optional (rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cargo-test-bpf") "unbounded_depth")
    ];
    dependencies = {
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/serde_json" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cargo-test-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "itoa" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".itoa."0.4.5" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/serde_json" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cargo-test-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "ryu" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".ryu."1.0.4" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/serde_json" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cargo-test-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "serde" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".serde_urlencoded."0.7.0" = overridableMkRustCrate (profileName: rec {
    name = "serde_urlencoded";
    version = "0.7.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "edfa57a7f8d9c1d260a549e7224100f6c43d43f9103e06dd8b4095a9b2b43ce9"; };
    dependencies = {
      form_urlencoded = rustPackages."registry+https://github.com/rust-lang/crates.io-index".form_urlencoded."1.0.0" { inherit profileName; };
      itoa = rustPackages."registry+https://github.com/rust-lang/crates.io-index".itoa."0.4.5" { inherit profileName; };
      ryu = rustPackages."registry+https://github.com/rust-lang/crates.io-index".ryu."1.0.4" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".serde_yaml."0.8.13" = overridableMkRustCrate (profileName: rec {
    name = "serde_yaml";
    version = "0.8.13";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "ae3e2dd40a7cdc18ca80db804b7f461a39bb721160a85c9a1fa30134bf3c02a5"; };
    dependencies = {
      dtoa = rustPackages."registry+https://github.com/rust-lang/crates.io-index".dtoa."0.4.5" { inherit profileName; };
      linked_hash_map = rustPackages."registry+https://github.com/rust-lang/crates.io-index".linked-hash-map."0.5.3" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      yaml_rust = rustPackages."registry+https://github.com/rust-lang/crates.io-index".yaml-rust."0.4.3" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".serial_test."0.4.0" = overridableMkRustCrate (profileName: rec {
    name = "serial_test";
    version = "0.4.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "fef5f7c7434b2f2c598adc6f9494648a1e41274a75c0ba4056f680ae0c117fd6"; };
    dependencies = {
      lazy_static = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
      parking_lot = rustPackages."registry+https://github.com/rust-lang/crates.io-index".parking_lot."0.10.2" { inherit profileName; };
      serial_test_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".serial_test_derive."0.4.0" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".serial_test_derive."0.4.0" = overridableMkRustCrate (profileName: rec {
    name = "serial_test_derive";
    version = "0.4.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "d08338d8024b227c62bd68a12c7c9883f5c66780abaef15c550dc56f46ee6515"; };
    dependencies = {
      proc_macro2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."1.0.24" { inherit profileName; };
      quote = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quote."1.0.6" { inherit profileName; };
      syn = rustPackages."registry+https://github.com/rust-lang/crates.io-index".syn."1.0.60" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".sha-1."0.8.2" = overridableMkRustCrate (profileName: rec {
    name = "sha-1";
    version = "0.8.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "f7d94d0bede923b3cea61f3f1ff57ff8cdfd77b400fb8f9998949e0cf04163df"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "std" ]
    ];
    dependencies = {
      block_buffer = rustPackages."registry+https://github.com/rust-lang/crates.io-index".block-buffer."0.7.3" { inherit profileName; };
      digest = rustPackages."registry+https://github.com/rust-lang/crates.io-index".digest."0.8.1" { inherit profileName; };
      fake_simd = rustPackages."registry+https://github.com/rust-lang/crates.io-index".fake-simd."0.1.2" { inherit profileName; };
      opaque_debug = rustPackages."registry+https://github.com/rust-lang/crates.io-index".opaque-debug."0.2.3" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".sha1."0.6.0" = overridableMkRustCrate (profileName: rec {
    name = "sha1";
    version = "0.6.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "2579985fda508104f7587689507983eadd6a6e84dd35d6d115361f530916fa0d"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".sha2."0.8.2" = overridableMkRustCrate (profileName: rec {
    name = "sha2";
    version = "0.8.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "a256f46ea78a0c0d9ff00077504903ac881a1dafdc20da66545699e7776b3e69"; };
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-sdk") "default")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "std")
    ];
    dependencies = {
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "block_buffer" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".block-buffer."0.7.3" { inherit profileName; };
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "digest" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".digest."0.8.1" { inherit profileName; };
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "fake_simd" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".fake-simd."0.1.2" { inherit profileName; };
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "opaque_debug" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".opaque-debug."0.2.3" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".sha2."0.9.2" = overridableMkRustCrate (profileName: rec {
    name = "sha2";
    version = "0.9.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "6e7aab86fe2149bad8c507606bdb3f4ef5e7b2380eb92350f56122cca72a42a8"; };
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "default")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "std")
    ];
    dependencies = {
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "block_buffer" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".block-buffer."0.9.0" { inherit profileName; };
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "cfg_if" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".cfg-if."1.0.0" { inherit profileName; };
      ${ if (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") && (hostPlatform.parsed.cpu.name == "x86" || hostPlatform.parsed.cpu.name == "x86_64") then "cpuid_bool" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".cpuid-bool."0.1.2" { inherit profileName; };
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "digest" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".digest."0.9.0" { inherit profileName; };
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "opaque_debug" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".opaque-debug."0.3.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".sha3."0.9.1" = overridableMkRustCrate (profileName: rec {
    name = "sha3";
    version = "0.9.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "f81199417d4e5de3f04b1e871023acea7389672c4135918f05aa9cbf2f2fa809"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "std" ]
    ];
    dependencies = {
      block_buffer = rustPackages."registry+https://github.com/rust-lang/crates.io-index".block-buffer."0.9.0" { inherit profileName; };
      digest = rustPackages."registry+https://github.com/rust-lang/crates.io-index".digest."0.9.0" { inherit profileName; };
      keccak = rustPackages."registry+https://github.com/rust-lang/crates.io-index".keccak."0.1.0" { inherit profileName; };
      opaque_debug = rustPackages."registry+https://github.com/rust-lang/crates.io-index".opaque-debug."0.3.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".shlex."0.1.1" = overridableMkRustCrate (profileName: rec {
    name = "shlex";
    version = "0.1.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "7fdf1b9db47230893d76faad238fd6097fd6d6a9245cd7a4d90dbd639536bbd2"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".signal-hook."0.1.15" = overridableMkRustCrate (profileName: rec {
    name = "signal-hook";
    version = "0.1.15";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "8ff2db2112d6c761e12522c65f7768548bd6e8cd23d2a9dae162520626629bd6"; };
    dependencies = {
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      signal_hook_registry = rustPackages."registry+https://github.com/rust-lang/crates.io-index".signal-hook-registry."1.2.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".signal-hook-registry."1.2.0" = overridableMkRustCrate (profileName: rec {
    name = "signal-hook-registry";
    version = "1.2.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "94f478ede9f64724c5d173d7bb56099ec3e2d9fc2774aac65d34b8b890405f41"; };
    dependencies = {
      arc_swap = rustPackages."registry+https://github.com/rust-lang/crates.io-index".arc-swap."0.4.8" { inherit profileName; };
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".signature."1.1.0" = overridableMkRustCrate (profileName: rec {
    name = "signature";
    version = "1.1.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "65211b7b6fc3f14ff9fc7a2011a434e3e6880585bd2e9e9396315ae24cbf7852"; };
    features = builtins.concatLists [
      [ "std" ]
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".simpl."0.1.0" = overridableMkRustCrate (profileName: rec {
    name = "simpl";
    version = "0.1.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "2a30f10c911c0355f80f1c2faa8096efc4a58cdf8590b954d5b395efa071c711"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".slab."0.4.2" = overridableMkRustCrate (profileName: rec {
    name = "slab";
    version = "0.4.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "c111b5bd5695e56cffe5129854aa230b39c93a305372fdbb2668ca2394eea9f8"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".smallvec."0.6.14" = overridableMkRustCrate (profileName: rec {
    name = "smallvec";
    version = "0.6.14";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "b97fcaeba89edba30f044a10c6a3cc39df9c3f17d7cd829dd1446cab35f890e0"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "std" ]
    ];
    dependencies = {
      maybe_uninit = rustPackages."registry+https://github.com/rust-lang/crates.io-index".maybe-uninit."2.0.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".smallvec."1.6.1" = overridableMkRustCrate (profileName: rec {
    name = "smallvec";
    version = "1.6.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "fe0f37c9e8f3c5a4a66ad655a93c74daac4ad00c441533bf5c6e7990bb42604e"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".smpl_jwt."0.6.0" = overridableMkRustCrate (profileName: rec {
    name = "smpl_jwt";
    version = "0.6.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "2fb8aea73548d19a00cbaed99a48dbdc61fed3469f39d035617eefd7a88fffd0"; };
    dependencies = {
      base64 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".base64."0.13.0" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      openssl = rustPackages."registry+https://github.com/rust-lang/crates.io-index".openssl."0.10.29" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      serde_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_derive."1.0.122" { profileName = "__noProfile"; };
      serde_json = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_json."1.0.62" { inherit profileName; };
      simpl = rustPackages."registry+https://github.com/rust-lang/crates.io-index".simpl."0.1.0" { inherit profileName; };
      time = rustPackages."registry+https://github.com/rust-lang/crates.io-index".time."0.2.25" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".socket2."0.3.17" = overridableMkRustCrate (profileName: rec {
    name = "socket2";
    version = "0.3.17";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "2c29947abdee2a218277abeca306f25789c938e500ea5a9d4b12a5a504466902"; };
    dependencies = {
      ${ if hostPlatform.isUnix || hostPlatform.parsed.kernel.name == "redox" then "cfg_if" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".cfg-if."1.0.0" { inherit profileName; };
      ${ if hostPlatform.isUnix || hostPlatform.parsed.kernel.name == "redox" then "libc" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      ${ if hostPlatform.parsed.kernel.name == "redox" then "syscall" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".redox_syscall."0.1.56" { inherit profileName; };
      ${ if hostPlatform.isWindows then "winapi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.3.8" { inherit profileName; };
    };
  });
  
  "unknown".solana-account-decoder."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-account-decoder";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/account-decoder");
    dependencies = {
      inflector = rustPackages."registry+https://github.com/rust-lang/crates.io-index".Inflector."0.11.4" { inherit profileName; };
      base64 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".base64."0.12.3" { inherit profileName; };
      bincode = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bincode."1.3.1" { inherit profileName; };
      bs58 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bs58."0.3.1" { inherit profileName; };
      bv = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bv."0.11.1" { inherit profileName; };
      lazy_static = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      serde_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_derive."1.0.122" { profileName = "__noProfile"; };
      serde_json = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_json."1.0.62" { inherit profileName; };
      solana_config_program = rustPackages."unknown".solana-config-program."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      solana_stake_program = rustPackages."unknown".solana-stake-program."1.6.6" { inherit profileName; };
      solana_vote_program = rustPackages."unknown".solana-vote-program."1.6.6" { inherit profileName; };
      spl_token_v2_0 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".spl-token."3.1.0" { inherit profileName; };
      thiserror = rustPackages."registry+https://github.com/rust-lang/crates.io-index".thiserror."1.0.23" { inherit profileName; };
      zstd = rustPackages."registry+https://github.com/rust-lang/crates.io-index".zstd."0.5.3+zstd.1.4.5" { inherit profileName; };
    };
  });
  
  "unknown".solana-accounts-bench."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-accounts-bench";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/accounts-bench");
    dependencies = {
      clap = rustPackages."registry+https://github.com/rust-lang/crates.io-index".clap."2.33.3" { inherit profileName; };
      crossbeam_channel = rustPackages."registry+https://github.com/rust-lang/crates.io-index".crossbeam-channel."0.4.4" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      rand = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand."0.7.3" { inherit profileName; };
      rayon = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rayon."1.5.0" { inherit profileName; };
      solana_logger = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
      solana_measure = rustPackages."unknown".solana-measure."1.6.6" { inherit profileName; };
      solana_runtime = rustPackages."unknown".solana-runtime."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      solana_version = rustPackages."unknown".solana-version."1.6.6" { inherit profileName; };
    };
  });
  
  "unknown".solana-accounts-cluster-bench."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-accounts-cluster-bench";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/accounts-cluster-bench");
    dependencies = {
      clap = rustPackages."registry+https://github.com/rust-lang/crates.io-index".clap."2.33.3" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      rand = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand."0.7.3" { inherit profileName; };
      rayon = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rayon."1.5.0" { inherit profileName; };
      solana_account_decoder = rustPackages."unknown".solana-account-decoder."1.6.6" { inherit profileName; };
      solana_clap_utils = rustPackages."unknown".solana-clap-utils."1.6.6" { inherit profileName; };
      solana_client = rustPackages."unknown".solana-client."1.6.6" { inherit profileName; };
      solana_core = rustPackages."unknown".solana-core."1.6.6" { inherit profileName; };
      solana_faucet = rustPackages."unknown".solana-faucet."1.6.6" { inherit profileName; };
      solana_logger = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
      solana_measure = rustPackages."unknown".solana-measure."1.6.6" { inherit profileName; };
      solana_net_utils = rustPackages."unknown".solana-net-utils."1.6.6" { inherit profileName; };
      solana_runtime = rustPackages."unknown".solana-runtime."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      solana_transaction_status = rustPackages."unknown".solana-transaction-status."1.6.6" { inherit profileName; };
      solana_version = rustPackages."unknown".solana-version."1.6.6" { inherit profileName; };
      spl_token_v2_0 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".spl-token."3.1.0" { inherit profileName; };
    };
    devDependencies = {
      solana_local_cluster = rustPackages."unknown".solana-local-cluster."1.6.6" { inherit profileName; };
    };
  });
  
  "unknown".solana-banking-bench."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-banking-bench";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/banking-bench");
    dependencies = {
      clap = rustPackages."registry+https://github.com/rust-lang/crates.io-index".clap."2.33.3" { inherit profileName; };
      crossbeam_channel = rustPackages."registry+https://github.com/rust-lang/crates.io-index".crossbeam-channel."0.4.4" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      rand = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand."0.7.3" { inherit profileName; };
      rayon = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rayon."1.5.0" { inherit profileName; };
      solana_clap_utils = rustPackages."unknown".solana-clap-utils."1.6.6" { inherit profileName; };
      solana_core = rustPackages."unknown".solana-core."1.6.6" { inherit profileName; };
      solana_ledger = rustPackages."unknown".solana-ledger."1.6.6" { inherit profileName; };
      solana_logger = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
      solana_measure = rustPackages."unknown".solana-measure."1.6.6" { inherit profileName; };
      solana_perf = rustPackages."unknown".solana-perf."1.6.6" { inherit profileName; };
      solana_runtime = rustPackages."unknown".solana-runtime."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      solana_streamer = rustPackages."unknown".solana-streamer."1.6.6" { inherit profileName; };
      solana_version = rustPackages."unknown".solana-version."1.6.6" { inherit profileName; };
    };
  });
  
  "unknown".solana-banks-client."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-banks-client";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/banks-client");
    dependencies = {
      bincode = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bincode."1.3.1" { inherit profileName; };
      borsh = rustPackages."registry+https://github.com/rust-lang/crates.io-index".borsh."0.8.1" { inherit profileName; };
      borsh_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".borsh-derive."0.8.1" { profileName = "__noProfile"; };
      futures = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures."0.3.8" { inherit profileName; };
      mio = rustPackages."registry+https://github.com/rust-lang/crates.io-index".mio."0.7.6" { inherit profileName; };
      solana_banks_interface = rustPackages."unknown".solana-banks-interface."1.6.6" { inherit profileName; };
      solana_program = rustPackages."unknown".solana-program."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      tarpc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tarpc."0.24.1" { inherit profileName; };
      tokio = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio."1.1.1" { inherit profileName; };
      tokio_serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-serde."0.8.0" { inherit profileName; };
    };
    devDependencies = {
      solana_banks_server = rustPackages."unknown".solana-banks-server."1.6.6" { inherit profileName; };
      solana_runtime = rustPackages."unknown".solana-runtime."1.6.6" { inherit profileName; };
    };
  });
  
  "unknown".solana-banks-interface."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-banks-interface";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/banks-interface");
    dependencies = {
      mio = rustPackages."registry+https://github.com/rust-lang/crates.io-index".mio."0.7.6" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      tarpc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tarpc."0.24.1" { inherit profileName; };
    };
    devDependencies = {
      tokio = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio."1.1.1" { inherit profileName; };
    };
  });
  
  "unknown".solana-banks-server."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-banks-server";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/banks-server");
    dependencies = {
      bincode = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bincode."1.3.1" { inherit profileName; };
      futures = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures."0.3.8" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      mio = rustPackages."registry+https://github.com/rust-lang/crates.io-index".mio."0.7.6" { inherit profileName; };
      solana_banks_interface = rustPackages."unknown".solana-banks-interface."1.6.6" { inherit profileName; };
      solana_metrics = rustPackages."unknown".solana-metrics."1.6.6" { inherit profileName; };
      solana_runtime = rustPackages."unknown".solana-runtime."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      tarpc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tarpc."0.24.1" { inherit profileName; };
      tokio = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio."1.1.1" { inherit profileName; };
      tokio_serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-serde."0.8.0" { inherit profileName; };
      tokio_stream = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-stream."0.1.2" { inherit profileName; };
    };
  });
  
  "unknown".solana-bench-exchange."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-bench-exchange";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/bench-exchange");
    dependencies = {
      clap = rustPackages."registry+https://github.com/rust-lang/crates.io-index".clap."2.33.3" { inherit profileName; };
      itertools = rustPackages."registry+https://github.com/rust-lang/crates.io-index".itertools."0.9.0" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      num_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".num-derive."0.3.2" { profileName = "__noProfile"; };
      num_traits = rustPackages."registry+https://github.com/rust-lang/crates.io-index".num-traits."0.2.11" { inherit profileName; };
      rand = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand."0.7.3" { inherit profileName; };
      rayon = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rayon."1.5.0" { inherit profileName; };
      serde_json = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_json."1.0.62" { inherit profileName; };
      serde_yaml = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_yaml."0.8.13" { inherit profileName; };
      solana_clap_utils = rustPackages."unknown".solana-clap-utils."1.6.6" { inherit profileName; };
      solana_client = rustPackages."unknown".solana-client."1.6.6" { inherit profileName; };
      solana_core = rustPackages."unknown".solana-core."1.6.6" { inherit profileName; };
      solana_exchange_program = rustPackages."unknown".solana-exchange-program."1.6.6" { inherit profileName; };
      solana_faucet = rustPackages."unknown".solana-faucet."1.6.6" { inherit profileName; };
      solana_genesis = rustPackages."unknown".solana-genesis."1.6.6" { inherit profileName; };
      solana_logger = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
      solana_metrics = rustPackages."unknown".solana-metrics."1.6.6" { inherit profileName; };
      solana_net_utils = rustPackages."unknown".solana-net-utils."1.6.6" { inherit profileName; };
      solana_runtime = rustPackages."unknown".solana-runtime."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      solana_version = rustPackages."unknown".solana-version."1.6.6" { inherit profileName; };
    };
    devDependencies = {
      solana_local_cluster = rustPackages."unknown".solana-local-cluster."1.6.6" { inherit profileName; };
    };
  });
  
  "unknown".solana-bench-streamer."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-bench-streamer";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/bench-streamer");
    dependencies = {
      clap = rustPackages."registry+https://github.com/rust-lang/crates.io-index".clap."2.33.3" { inherit profileName; };
      solana_clap_utils = rustPackages."unknown".solana-clap-utils."1.6.6" { inherit profileName; };
      solana_logger = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
      solana_net_utils = rustPackages."unknown".solana-net-utils."1.6.6" { inherit profileName; };
      solana_streamer = rustPackages."unknown".solana-streamer."1.6.6" { inherit profileName; };
      solana_version = rustPackages."unknown".solana-version."1.6.6" { inherit profileName; };
    };
  });
  
  "unknown".solana-bench-tps."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-bench-tps";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/bench-tps");
    dependencies = {
      bincode = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bincode."1.3.1" { inherit profileName; };
      clap = rustPackages."registry+https://github.com/rust-lang/crates.io-index".clap."2.33.3" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      rayon = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rayon."1.5.0" { inherit profileName; };
      serde_json = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_json."1.0.62" { inherit profileName; };
      serde_yaml = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_yaml."0.8.13" { inherit profileName; };
      solana_clap_utils = rustPackages."unknown".solana-clap-utils."1.6.6" { inherit profileName; };
      solana_client = rustPackages."unknown".solana-client."1.6.6" { inherit profileName; };
      solana_core = rustPackages."unknown".solana-core."1.6.6" { inherit profileName; };
      solana_faucet = rustPackages."unknown".solana-faucet."1.6.6" { inherit profileName; };
      solana_genesis = rustPackages."unknown".solana-genesis."1.6.6" { inherit profileName; };
      solana_logger = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
      solana_measure = rustPackages."unknown".solana-measure."1.6.6" { inherit profileName; };
      solana_metrics = rustPackages."unknown".solana-metrics."1.6.6" { inherit profileName; };
      solana_net_utils = rustPackages."unknown".solana-net-utils."1.6.6" { inherit profileName; };
      solana_runtime = rustPackages."unknown".solana-runtime."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      solana_version = rustPackages."unknown".solana-version."1.6.6" { inherit profileName; };
    };
    devDependencies = {
      serial_test = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serial_test."0.4.0" { inherit profileName; };
      solana_local_cluster = rustPackages."unknown".solana-local-cluster."1.6.6" { inherit profileName; };
    };
  });
  
  "unknown".solana-bpf-loader-program."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-bpf-loader-program";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/programs/bpf_loader");
    dependencies = {
      bincode = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bincode."1.3.1" { inherit profileName; };
      byteorder = rustPackages."registry+https://github.com/rust-lang/crates.io-index".byteorder."1.3.4" { inherit profileName; };
      curve25519_dalek = rustPackages."registry+https://github.com/rust-lang/crates.io-index".curve25519-dalek."3.0.0" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      num_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".num-derive."0.3.2" { profileName = "__noProfile"; };
      num_traits = rustPackages."registry+https://github.com/rust-lang/crates.io-index".num-traits."0.2.11" { inherit profileName; };
      rand_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand_core."0.6.2" { inherit profileName; };
      solana_measure = rustPackages."unknown".solana-measure."1.6.6" { inherit profileName; };
      solana_runtime = rustPackages."unknown".solana-runtime."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      solana_rbpf = rustPackages."registry+https://github.com/rust-lang/crates.io-index".solana_rbpf."0.2.7" { inherit profileName; };
      thiserror = rustPackages."registry+https://github.com/rust-lang/crates.io-index".thiserror."1.0.23" { inherit profileName; };
    };
    devDependencies = {
      rand = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand."0.7.3" { inherit profileName; };
      rustversion = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".rustversion."1.0.4" { profileName = "__noProfile"; };
    };
  });
  
  "unknown".solana-budget-program."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-budget-program";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/programs/budget");
    dependencies = {
      bincode = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bincode."1.3.1" { inherit profileName; };
      chrono = rustPackages."registry+https://github.com/rust-lang/crates.io-index".chrono."0.4.19" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      num_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".num-derive."0.3.2" { profileName = "__noProfile"; };
      num_traits = rustPackages."registry+https://github.com/rust-lang/crates.io-index".num-traits."0.2.11" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      serde_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_derive."1.0.122" { profileName = "__noProfile"; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      thiserror = rustPackages."registry+https://github.com/rust-lang/crates.io-index".thiserror."1.0.23" { inherit profileName; };
    };
    devDependencies = {
      solana_runtime = rustPackages."unknown".solana-runtime."1.6.6" { inherit profileName; };
    };
  });
  
  "unknown".solana-cargo-build-bpf."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-cargo-build-bpf";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/sdk/cargo-build-bpf");
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-cargo-build-bpf/program") "program")
    ];
    dependencies = {
      ${ if rootFeatures' ? "solana-cargo-build-bpf" then "bzip2" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bzip2."0.3.3" { inherit profileName; };
      ${ if rootFeatures' ? "solana-cargo-build-bpf" then "cargo_metadata" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".cargo_metadata."0.12.0" { inherit profileName; };
      ${ if rootFeatures' ? "solana-cargo-build-bpf" then "clap" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".clap."2.33.3" { inherit profileName; };
      ${ if rootFeatures' ? "solana-cargo-build-bpf" then "solana_download_utils" else null } = rustPackages."unknown".solana-download-utils."1.6.6" { inherit profileName; };
      ${ if rootFeatures' ? "solana-cargo-build-bpf" then "solana_sdk" else null } = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      ${ if rootFeatures' ? "solana-cargo-build-bpf" then "tar" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tar."0.4.28" { inherit profileName; };
    };
  });
  
  "unknown".solana-cargo-test-bpf."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-cargo-test-bpf";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/sdk/cargo-test-bpf");
    dependencies = {
      cargo_metadata = rustPackages."registry+https://github.com/rust-lang/crates.io-index".cargo_metadata."0.12.0" { inherit profileName; };
      clap = rustPackages."registry+https://github.com/rust-lang/crates.io-index".clap."2.33.3" { inherit profileName; };
    };
  });
  
  "unknown".solana-clap-utils."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-clap-utils";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/clap-utils");
    dependencies = {
      chrono = rustPackages."registry+https://github.com/rust-lang/crates.io-index".chrono."0.4.19" { inherit profileName; };
      clap = rustPackages."registry+https://github.com/rust-lang/crates.io-index".clap."2.33.3" { inherit profileName; };
      rpassword = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rpassword."4.0.5" { inherit profileName; };
      solana_remote_wallet = rustPackages."unknown".solana-remote-wallet."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      thiserror = rustPackages."registry+https://github.com/rust-lang/crates.io-index".thiserror."1.0.23" { inherit profileName; };
      bip39 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tiny-bip39."0.8.0" { inherit profileName; };
      uriparse = rustPackages."registry+https://github.com/rust-lang/crates.io-index".uriparse."0.6.3" { inherit profileName; };
      url = rustPackages."registry+https://github.com/rust-lang/crates.io-index".url."2.2.0" { inherit profileName; };
    };
  });
  
  "unknown".solana-cli."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-cli";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/cli");
    dependencies = {
      inflector = rustPackages."registry+https://github.com/rust-lang/crates.io-index".Inflector."0.11.4" { inherit profileName; };
      bincode = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bincode."1.3.1" { inherit profileName; };
      bs58 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bs58."0.3.1" { inherit profileName; };
      chrono = rustPackages."registry+https://github.com/rust-lang/crates.io-index".chrono."0.4.19" { inherit profileName; };
      clap = rustPackages."registry+https://github.com/rust-lang/crates.io-index".clap."2.33.3" { inherit profileName; };
      console = rustPackages."registry+https://github.com/rust-lang/crates.io-index".console."0.11.3" { inherit profileName; };
      criterion_stats = rustPackages."registry+https://github.com/rust-lang/crates.io-index".criterion-stats."0.3.0" { inherit profileName; };
      ctrlc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".ctrlc."3.1.5" { inherit profileName; };
      dirs_next = rustPackages."registry+https://github.com/rust-lang/crates.io-index".dirs-next."2.0.0" { inherit profileName; };
      humantime = rustPackages."registry+https://github.com/rust-lang/crates.io-index".humantime."2.0.1" { inherit profileName; };
      indicatif = rustPackages."registry+https://github.com/rust-lang/crates.io-index".indicatif."0.15.0" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      num_traits = rustPackages."registry+https://github.com/rust-lang/crates.io-index".num-traits."0.2.11" { inherit profileName; };
      pretty_hex = rustPackages."registry+https://github.com/rust-lang/crates.io-index".pretty-hex."0.2.1" { inherit profileName; };
      reqwest = rustPackages."registry+https://github.com/rust-lang/crates.io-index".reqwest."0.11.2" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      serde_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_derive."1.0.122" { profileName = "__noProfile"; };
      serde_json = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_json."1.0.62" { inherit profileName; };
      solana_account_decoder = rustPackages."unknown".solana-account-decoder."1.6.6" { inherit profileName; };
      solana_bpf_loader_program = rustPackages."unknown".solana-bpf-loader-program."1.6.6" { inherit profileName; };
      solana_clap_utils = rustPackages."unknown".solana-clap-utils."1.6.6" { inherit profileName; };
      solana_cli_config = rustPackages."unknown".solana-cli-config."1.6.6" { inherit profileName; };
      solana_cli_output = rustPackages."unknown".solana-cli-output."1.6.6" { inherit profileName; };
      solana_client = rustPackages."unknown".solana-client."1.6.6" { inherit profileName; };
      solana_config_program = rustPackages."unknown".solana-config-program."1.6.6" { inherit profileName; };
      solana_faucet = rustPackages."unknown".solana-faucet."1.6.6" { inherit profileName; };
      solana_logger = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
      solana_net_utils = rustPackages."unknown".solana-net-utils."1.6.6" { inherit profileName; };
      solana_remote_wallet = rustPackages."unknown".solana-remote-wallet."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      solana_stake_program = rustPackages."unknown".solana-stake-program."1.6.6" { inherit profileName; };
      solana_transaction_status = rustPackages."unknown".solana-transaction-status."1.6.6" { inherit profileName; };
      solana_version = rustPackages."unknown".solana-version."1.6.6" { inherit profileName; };
      solana_vote_program = rustPackages."unknown".solana-vote-program."1.6.6" { inherit profileName; };
      solana_rbpf = rustPackages."registry+https://github.com/rust-lang/crates.io-index".solana_rbpf."0.2.7" { inherit profileName; };
      spl_memo = rustPackages."registry+https://github.com/rust-lang/crates.io-index".spl-memo."3.0.1" { inherit profileName; };
      thiserror = rustPackages."registry+https://github.com/rust-lang/crates.io-index".thiserror."1.0.23" { inherit profileName; };
      bip39 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tiny-bip39."0.7.3" { inherit profileName; };
      url = rustPackages."registry+https://github.com/rust-lang/crates.io-index".url."2.2.0" { inherit profileName; };
    };
    devDependencies = {
      solana_core = rustPackages."unknown".solana-core."1.6.6" { inherit profileName; };
      tempfile = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tempfile."3.1.0" { inherit profileName; };
    };
  });
  
  "unknown".solana-cli-config."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-cli-config";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/cli-config");
    dependencies = {
      dirs_next = rustPackages."registry+https://github.com/rust-lang/crates.io-index".dirs-next."2.0.0" { inherit profileName; };
      lazy_static = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      serde_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_derive."1.0.122" { profileName = "__noProfile"; };
      serde_yaml = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_yaml."0.8.13" { inherit profileName; };
      url = rustPackages."registry+https://github.com/rust-lang/crates.io-index".url."2.2.0" { inherit profileName; };
    };
  });
  
  "unknown".solana-cli-output."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-cli-output";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/cli-output");
    dependencies = {
      inflector = rustPackages."registry+https://github.com/rust-lang/crates.io-index".Inflector."0.11.4" { inherit profileName; };
      base64 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".base64."0.13.0" { inherit profileName; };
      chrono = rustPackages."registry+https://github.com/rust-lang/crates.io-index".chrono."0.4.19" { inherit profileName; };
      console = rustPackages."registry+https://github.com/rust-lang/crates.io-index".console."0.11.3" { inherit profileName; };
      humantime = rustPackages."registry+https://github.com/rust-lang/crates.io-index".humantime."2.0.1" { inherit profileName; };
      indicatif = rustPackages."registry+https://github.com/rust-lang/crates.io-index".indicatif."0.15.0" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      serde_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_derive."1.0.122" { profileName = "__noProfile"; };
      serde_json = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_json."1.0.62" { inherit profileName; };
      solana_account_decoder = rustPackages."unknown".solana-account-decoder."1.6.6" { inherit profileName; };
      solana_clap_utils = rustPackages."unknown".solana-clap-utils."1.6.6" { inherit profileName; };
      solana_client = rustPackages."unknown".solana-client."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      solana_stake_program = rustPackages."unknown".solana-stake-program."1.6.6" { inherit profileName; };
      solana_transaction_status = rustPackages."unknown".solana-transaction-status."1.6.6" { inherit profileName; };
      solana_vote_program = rustPackages."unknown".solana-vote-program."1.6.6" { inherit profileName; };
      spl_memo = rustPackages."registry+https://github.com/rust-lang/crates.io-index".spl-memo."3.0.1" { inherit profileName; };
    };
  });
  
  "unknown".solana-client."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-client";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/client");
    dependencies = {
      base64 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".base64."0.13.0" { inherit profileName; };
      bincode = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bincode."1.3.1" { inherit profileName; };
      bs58 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bs58."0.3.1" { inherit profileName; };
      clap = rustPackages."registry+https://github.com/rust-lang/crates.io-index".clap."2.33.3" { inherit profileName; };
      indicatif = rustPackages."registry+https://github.com/rust-lang/crates.io-index".indicatif."0.15.0" { inherit profileName; };
      jsonrpc_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".jsonrpc-core."17.0.0" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      net2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".net2."0.2.37" { inherit profileName; };
      rayon = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rayon."1.5.0" { inherit profileName; };
      reqwest = rustPackages."registry+https://github.com/rust-lang/crates.io-index".reqwest."0.11.2" { inherit profileName; };
      semver = rustPackages."registry+https://github.com/rust-lang/crates.io-index".semver."0.11.0" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      serde_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_derive."1.0.122" { profileName = "__noProfile"; };
      serde_json = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_json."1.0.62" { inherit profileName; };
      solana_account_decoder = rustPackages."unknown".solana-account-decoder."1.6.6" { inherit profileName; };
      solana_clap_utils = rustPackages."unknown".solana-clap-utils."1.6.6" { inherit profileName; };
      solana_faucet = rustPackages."unknown".solana-faucet."1.6.6" { inherit profileName; };
      solana_net_utils = rustPackages."unknown".solana-net-utils."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      solana_transaction_status = rustPackages."unknown".solana-transaction-status."1.6.6" { inherit profileName; };
      solana_version = rustPackages."unknown".solana-version."1.6.6" { inherit profileName; };
      solana_vote_program = rustPackages."unknown".solana-vote-program."1.6.6" { inherit profileName; };
      thiserror = rustPackages."registry+https://github.com/rust-lang/crates.io-index".thiserror."1.0.23" { inherit profileName; };
      tokio = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio."1.1.1" { inherit profileName; };
      tungstenite = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tungstenite."0.10.1" { inherit profileName; };
      url = rustPackages."registry+https://github.com/rust-lang/crates.io-index".url."2.2.0" { inherit profileName; };
    };
    devDependencies = {
      assert_matches = rustPackages."registry+https://github.com/rust-lang/crates.io-index".assert_matches."1.3.0" { inherit profileName; };
      jsonrpc_http_server = rustPackages."registry+https://github.com/rust-lang/crates.io-index".jsonrpc-http-server."17.0.0" { inherit profileName; };
      solana_logger = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
    };
  });
  
  "unknown".solana-config-program."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-config-program";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/programs/config");
    dependencies = {
      bincode = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bincode."1.3.1" { inherit profileName; };
      chrono = rustPackages."registry+https://github.com/rust-lang/crates.io-index".chrono."0.4.19" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      rand_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand_core."0.6.2" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      serde_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_derive."1.0.122" { profileName = "__noProfile"; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
    };
    devDependencies = {
      solana_logger = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
    };
  });
  
  "unknown".solana-core."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-core";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/core");
    dependencies = {
      ahash = rustPackages."registry+https://github.com/rust-lang/crates.io-index".ahash."0.6.1" { inherit profileName; };
      base64 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".base64."0.12.3" { inherit profileName; };
      bincode = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bincode."1.3.1" { inherit profileName; };
      blake3 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".blake3."0.3.7" { inherit profileName; };
      bs58 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bs58."0.3.1" { inherit profileName; };
      bv = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bv."0.11.1" { inherit profileName; };
      byteorder = rustPackages."registry+https://github.com/rust-lang/crates.io-index".byteorder."1.3.4" { inherit profileName; };
      chrono = rustPackages."registry+https://github.com/rust-lang/crates.io-index".chrono."0.4.19" { inherit profileName; };
      core_affinity = rustPackages."registry+https://github.com/rust-lang/crates.io-index".core_affinity."0.5.10" { inherit profileName; };
      crossbeam_channel = rustPackages."registry+https://github.com/rust-lang/crates.io-index".crossbeam-channel."0.4.4" { inherit profileName; };
      ed25519_dalek = rustPackages."registry+https://github.com/rust-lang/crates.io-index".ed25519-dalek."1.0.1" { inherit profileName; };
      flate2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".flate2."1.0.14" { inherit profileName; };
      fs_extra = rustPackages."registry+https://github.com/rust-lang/crates.io-index".fs_extra."1.2.0" { inherit profileName; };
      indexmap = rustPackages."registry+https://github.com/rust-lang/crates.io-index".indexmap."1.5.1" { inherit profileName; };
      itertools = rustPackages."registry+https://github.com/rust-lang/crates.io-index".itertools."0.9.0" { inherit profileName; };
      jsonrpc_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".jsonrpc-core."17.0.0" { inherit profileName; };
      jsonrpc_core_client = rustPackages."registry+https://github.com/rust-lang/crates.io-index".jsonrpc-core-client."17.0.0" { inherit profileName; };
      jsonrpc_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".jsonrpc-derive."17.0.0" { profileName = "__noProfile"; };
      jsonrpc_http_server = rustPackages."registry+https://github.com/rust-lang/crates.io-index".jsonrpc-http-server."17.0.0" { inherit profileName; };
      jsonrpc_pubsub = rustPackages."registry+https://github.com/rust-lang/crates.io-index".jsonrpc-pubsub."17.0.0" { inherit profileName; };
      jsonrpc_ws_server = rustPackages."registry+https://github.com/rust-lang/crates.io-index".jsonrpc-ws-server."17.0.0" { inherit profileName; };
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      lru = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lru."0.6.1" { inherit profileName; };
      miow = rustPackages."registry+https://github.com/rust-lang/crates.io-index".miow."0.2.2" { inherit profileName; };
      net2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".net2."0.2.37" { inherit profileName; };
      num_traits = rustPackages."registry+https://github.com/rust-lang/crates.io-index".num-traits."0.2.11" { inherit profileName; };
      rand = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand."0.7.3" { inherit profileName; };
      rand_chacha = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand_chacha."0.2.2" { inherit profileName; };
      rand_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand_core."0.6.2" { inherit profileName; };
      raptorq = rustPackages."registry+https://github.com/rust-lang/crates.io-index".raptorq."1.4.2" { inherit profileName; };
      rayon = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rayon."1.5.0" { inherit profileName; };
      regex = rustPackages."registry+https://github.com/rust-lang/crates.io-index".regex."1.3.9" { inherit profileName; };
      retain_mut = rustPackages."registry+https://github.com/rust-lang/crates.io-index".retain_mut."0.1.2" { inherit profileName; };
      rustversion = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".rustversion."1.0.4" { profileName = "__noProfile"; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      serde_bytes = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_bytes."0.11.4" { inherit profileName; };
      serde_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_derive."1.0.122" { profileName = "__noProfile"; };
      serde_json = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_json."1.0.62" { inherit profileName; };
      solana_account_decoder = rustPackages."unknown".solana-account-decoder."1.6.6" { inherit profileName; };
      solana_banks_server = rustPackages."unknown".solana-banks-server."1.6.6" { inherit profileName; };
      solana_clap_utils = rustPackages."unknown".solana-clap-utils."1.6.6" { inherit profileName; };
      solana_client = rustPackages."unknown".solana-client."1.6.6" { inherit profileName; };
      solana_faucet = rustPackages."unknown".solana-faucet."1.6.6" { inherit profileName; };
      solana_frozen_abi = rustPackages."unknown".solana-frozen-abi."1.6.6" { inherit profileName; };
      solana_frozen_abi_macro = buildRustPackages."unknown".solana-frozen-abi-macro."1.6.6" { profileName = "__noProfile"; };
      solana_ledger = rustPackages."unknown".solana-ledger."1.6.6" { inherit profileName; };
      solana_logger = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
      solana_measure = rustPackages."unknown".solana-measure."1.6.6" { inherit profileName; };
      solana_merkle_tree = rustPackages."unknown".solana-merkle-tree."1.6.6" { inherit profileName; };
      solana_metrics = rustPackages."unknown".solana-metrics."1.6.6" { inherit profileName; };
      solana_net_utils = rustPackages."unknown".solana-net-utils."1.6.6" { inherit profileName; };
      solana_perf = rustPackages."unknown".solana-perf."1.6.6" { inherit profileName; };
      solana_program_test = rustPackages."unknown".solana-program-test."1.6.6" { inherit profileName; };
      solana_rayon_threadlimit = rustPackages."unknown".solana-rayon-threadlimit."1.6.6" { inherit profileName; };
      solana_runtime = rustPackages."unknown".solana-runtime."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      solana_stake_program = rustPackages."unknown".solana-stake-program."1.6.6" { inherit profileName; };
      solana_storage_bigtable = rustPackages."unknown".solana-storage-bigtable."1.6.6" { inherit profileName; };
      solana_streamer = rustPackages."unknown".solana-streamer."1.6.6" { inherit profileName; };
      solana_sys_tuner = rustPackages."unknown".solana-sys-tuner."1.6.6" { inherit profileName; };
      solana_transaction_status = rustPackages."unknown".solana-transaction-status."1.6.6" { inherit profileName; };
      solana_version = rustPackages."unknown".solana-version."1.6.6" { inherit profileName; };
      solana_vote_program = rustPackages."unknown".solana-vote-program."1.6.6" { inherit profileName; };
      spl_token_v2_0 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".spl-token."3.1.0" { inherit profileName; };
      tempfile = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tempfile."3.1.0" { inherit profileName; };
      thiserror = rustPackages."registry+https://github.com/rust-lang/crates.io-index".thiserror."1.0.23" { inherit profileName; };
      tokio_02 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio."0.2.22" { inherit profileName; };
      tokio = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio."1.1.1" { inherit profileName; };
      tokio_util = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-util."0.3.1" { inherit profileName; };
      trees = rustPackages."registry+https://github.com/rust-lang/crates.io-index".trees."0.2.1" { inherit profileName; };
    };
    devDependencies = {
      matches = rustPackages."registry+https://github.com/rust-lang/crates.io-index".matches."0.1.8" { inherit profileName; };
      num_cpus = rustPackages."registry+https://github.com/rust-lang/crates.io-index".num_cpus."1.13.0" { inherit profileName; };
      reqwest = rustPackages."registry+https://github.com/rust-lang/crates.io-index".reqwest."0.11.2" { inherit profileName; };
      serial_test = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serial_test."0.4.0" { inherit profileName; };
      symlink = rustPackages."registry+https://github.com/rust-lang/crates.io-index".symlink."0.1.0" { inherit profileName; };
      systemstat = rustPackages."registry+https://github.com/rust-lang/crates.io-index".systemstat."0.1.5" { inherit profileName; };
    };
    buildDependencies = {
      rustc_version = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".rustc_version."0.2.3" { profileName = "__noProfile"; };
    };
  });
  
  "unknown".solana-crate-features."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-crate-features";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/crate-features");
    dependencies = {
      backtrace = rustPackages."registry+https://github.com/rust-lang/crates.io-index".backtrace."0.3.48" { inherit profileName; };
      bytes = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bytes."0.4.12" { inherit profileName; };
      cc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".cc."1.0.49" { inherit profileName; };
      curve25519_dalek = rustPackages."registry+https://github.com/rust-lang/crates.io-index".curve25519-dalek."2.1.0" { inherit profileName; };
      ed25519_dalek = rustPackages."registry+https://github.com/rust-lang/crates.io-index".ed25519-dalek."1.0.1" { inherit profileName; };
      either = rustPackages."registry+https://github.com/rust-lang/crates.io-index".either."1.5.3" { inherit profileName; };
      lazy_static = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      rand_chacha = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand_chacha."0.2.2" { inherit profileName; };
      regex_syntax = rustPackages."registry+https://github.com/rust-lang/crates.io-index".regex-syntax."0.6.18" { inherit profileName; };
      reqwest = rustPackages."registry+https://github.com/rust-lang/crates.io-index".reqwest."0.11.2" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      syn_0_15 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".syn."0.15.44" { inherit profileName; };
      syn_1_0 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".syn."1.0.60" { inherit profileName; };
      tokio = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio."0.1.22" { inherit profileName; };
      winapi = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.3.8" { inherit profileName; };
    };
  });
  
  "unknown".solana-dos."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-dos";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/dos");
    dependencies = {
      bincode = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bincode."1.3.1" { inherit profileName; };
      clap = rustPackages."registry+https://github.com/rust-lang/crates.io-index".clap."2.33.3" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      rand = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand."0.7.3" { inherit profileName; };
      rayon = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rayon."1.5.0" { inherit profileName; };
      solana_clap_utils = rustPackages."unknown".solana-clap-utils."1.6.6" { inherit profileName; };
      solana_client = rustPackages."unknown".solana-client."1.6.6" { inherit profileName; };
      solana_core = rustPackages."unknown".solana-core."1.6.6" { inherit profileName; };
      solana_ledger = rustPackages."unknown".solana-ledger."1.6.6" { inherit profileName; };
      solana_logger = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
      solana_net_utils = rustPackages."unknown".solana-net-utils."1.6.6" { inherit profileName; };
      solana_runtime = rustPackages."unknown".solana-runtime."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      solana_version = rustPackages."unknown".solana-version."1.6.6" { inherit profileName; };
    };
  });
  
  "unknown".solana-download-utils."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-download-utils";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/download-utils");
    dependencies = {
      bzip2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bzip2."0.3.3" { inherit profileName; };
      console = rustPackages."registry+https://github.com/rust-lang/crates.io-index".console."0.11.3" { inherit profileName; };
      indicatif = rustPackages."registry+https://github.com/rust-lang/crates.io-index".indicatif."0.15.0" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      reqwest = rustPackages."registry+https://github.com/rust-lang/crates.io-index".reqwest."0.11.2" { inherit profileName; };
      solana_runtime = rustPackages."unknown".solana-runtime."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      tar = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tar."0.4.28" { inherit profileName; };
    };
  });
  
  "unknown".solana-exchange-program."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-exchange-program";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/programs/exchange");
    dependencies = {
      bincode = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bincode."1.3.1" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      num_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".num-derive."0.3.2" { profileName = "__noProfile"; };
      num_traits = rustPackages."registry+https://github.com/rust-lang/crates.io-index".num-traits."0.2.11" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      serde_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_derive."1.0.122" { profileName = "__noProfile"; };
      solana_logger = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
      solana_metrics = rustPackages."unknown".solana-metrics."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      thiserror = rustPackages."registry+https://github.com/rust-lang/crates.io-index".thiserror."1.0.23" { inherit profileName; };
    };
    devDependencies = {
      solana_runtime = rustPackages."unknown".solana-runtime."1.6.6" { inherit profileName; };
    };
  });
  
  "unknown".solana-failure-program."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-failure-program";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/programs/failure");
    dependencies = {
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
    };
    devDependencies = {
      solana_runtime = rustPackages."unknown".solana-runtime."1.6.6" { inherit profileName; };
    };
  });
  
  "unknown".solana-faucet."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-faucet";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/faucet");
    dependencies = {
      bincode = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bincode."1.3.1" { inherit profileName; };
      byteorder = rustPackages."registry+https://github.com/rust-lang/crates.io-index".byteorder."1.3.4" { inherit profileName; };
      clap = rustPackages."registry+https://github.com/rust-lang/crates.io-index".clap."2.33.3" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      serde_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_derive."1.0.122" { profileName = "__noProfile"; };
      solana_clap_utils = rustPackages."unknown".solana-clap-utils."1.6.6" { inherit profileName; };
      solana_cli_config = rustPackages."unknown".solana-cli-config."1.6.6" { inherit profileName; };
      solana_logger = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
      solana_metrics = rustPackages."unknown".solana-metrics."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      solana_version = rustPackages."unknown".solana-version."1.6.6" { inherit profileName; };
      spl_memo = rustPackages."registry+https://github.com/rust-lang/crates.io-index".spl-memo."3.0.1" { inherit profileName; };
      thiserror = rustPackages."registry+https://github.com/rust-lang/crates.io-index".thiserror."1.0.23" { inherit profileName; };
      tokio = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio."1.1.1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".solana-frozen-abi."1.6.4" = overridableMkRustCrate (profileName: rec {
    name = "solana-frozen-abi";
    version = "1.6.4";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "f81ac3925c8995d935d3af27390cf7b748a81837a29feb75a00e115f40dae6dc"; };
    dependencies = {
      bs58 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bs58."0.3.1" { inherit profileName; };
      bv = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bv."0.11.1" { inherit profileName; };
      ${ if !(hostPlatform.parsed.cpu.name == "bpf") then "generic_array" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".generic-array."0.14.3" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      ${ if !(hostPlatform.parsed.cpu.name == "bpf") then "memmap2" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".memmap2."0.1.0" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      serde_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_derive."1.0.122" { profileName = "__noProfile"; };
      sha2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".sha2."0.9.2" { inherit profileName; };
      solana_frozen_abi_macro = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".solana-frozen-abi-macro."1.6.4" { profileName = "__noProfile"; };
      ${ if !(hostPlatform.parsed.cpu.name == "bpf") then "solana_logger" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".solana-logger."1.6.4" { inherit profileName; };
      thiserror = rustPackages."registry+https://github.com/rust-lang/crates.io-index".thiserror."1.0.23" { inherit profileName; };
    };
    buildDependencies = {
      rustc_version = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".rustc_version."0.2.3" { profileName = "__noProfile"; };
    };
  });
  
  "unknown".solana-frozen-abi."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-frozen-abi";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/frozen-abi");
    dependencies = {
      bs58 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bs58."0.3.1" { inherit profileName; };
      bv = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bv."0.11.1" { inherit profileName; };
      ${ if !(hostPlatform.parsed.cpu.name == "bpf") then "generic_array" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".generic-array."0.14.3" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      ${ if !(hostPlatform.parsed.cpu.name == "bpf") then "memmap2" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".memmap2."0.1.0" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      serde_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_derive."1.0.122" { profileName = "__noProfile"; };
      sha2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".sha2."0.9.2" { inherit profileName; };
      solana_frozen_abi_macro = buildRustPackages."unknown".solana-frozen-abi-macro."1.6.6" { profileName = "__noProfile"; };
      ${ if !(hostPlatform.parsed.cpu.name == "bpf") then "solana_logger" else null } = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
      thiserror = rustPackages."registry+https://github.com/rust-lang/crates.io-index".thiserror."1.0.23" { inherit profileName; };
    };
    buildDependencies = {
      rustc_version = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".rustc_version."0.2.3" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".solana-frozen-abi-macro."1.6.4" = overridableMkRustCrate (profileName: rec {
    name = "solana-frozen-abi-macro";
    version = "1.6.4";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "ae448159038e470f3b2dd1ab0d219246211f940a10bf1e656a02cb5c2d897437"; };
    dependencies = {
      lazy_static = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
      proc_macro2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."1.0.24" { inherit profileName; };
      quote = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quote."1.0.6" { inherit profileName; };
      syn = rustPackages."registry+https://github.com/rust-lang/crates.io-index".syn."1.0.60" { inherit profileName; };
    };
    buildDependencies = {
      rustc_version = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".rustc_version."0.2.3" { profileName = "__noProfile"; };
    };
  });
  
  "unknown".solana-frozen-abi-macro."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-frozen-abi-macro";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/frozen-abi/macro");
    dependencies = {
      lazy_static = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
      proc_macro2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."1.0.24" { inherit profileName; };
      quote = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quote."1.0.6" { inherit profileName; };
      syn = rustPackages."registry+https://github.com/rust-lang/crates.io-index".syn."1.0.60" { inherit profileName; };
    };
    buildDependencies = {
      rustc_version = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".rustc_version."0.2.3" { profileName = "__noProfile"; };
    };
  });
  
  "unknown".solana-genesis."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-genesis";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/genesis");
    dependencies = {
      base64 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".base64."0.12.3" { inherit profileName; };
      chrono = rustPackages."registry+https://github.com/rust-lang/crates.io-index".chrono."0.4.19" { inherit profileName; };
      clap = rustPackages."registry+https://github.com/rust-lang/crates.io-index".clap."2.33.3" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      serde_json = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_json."1.0.62" { inherit profileName; };
      serde_yaml = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_yaml."0.8.13" { inherit profileName; };
      solana_budget_program = rustPackages."unknown".solana-budget-program."1.6.6" { inherit profileName; };
      solana_clap_utils = rustPackages."unknown".solana-clap-utils."1.6.6" { inherit profileName; };
      solana_cli_config = rustPackages."unknown".solana-cli-config."1.6.6" { inherit profileName; };
      solana_exchange_program = rustPackages."unknown".solana-exchange-program."1.6.6" { inherit profileName; };
      solana_ledger = rustPackages."unknown".solana-ledger."1.6.6" { inherit profileName; };
      solana_logger = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
      solana_runtime = rustPackages."unknown".solana-runtime."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      solana_stake_program = rustPackages."unknown".solana-stake-program."1.6.6" { inherit profileName; };
      solana_version = rustPackages."unknown".solana-version."1.6.6" { inherit profileName; };
      solana_vest_program = rustPackages."unknown".solana-vest-program."1.6.6" { inherit profileName; };
      solana_vote_program = rustPackages."unknown".solana-vote-program."1.6.6" { inherit profileName; };
      tempfile = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tempfile."3.1.0" { inherit profileName; };
    };
  });
  
  "unknown".solana-gossip."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-gossip";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/gossip");
    dependencies = {
      clap = rustPackages."registry+https://github.com/rust-lang/crates.io-index".clap."2.33.3" { inherit profileName; };
      solana_clap_utils = rustPackages."unknown".solana-clap-utils."1.6.6" { inherit profileName; };
      solana_core = rustPackages."unknown".solana-core."1.6.6" { inherit profileName; };
      solana_logger = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
      solana_net_utils = rustPackages."unknown".solana-net-utils."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      solana_version = rustPackages."unknown".solana-version."1.6.6" { inherit profileName; };
    };
  });
  
  "unknown".solana-install."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-install";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/install");
    dependencies = {
      atty = rustPackages."registry+https://github.com/rust-lang/crates.io-index".atty."0.2.14" { inherit profileName; };
      bincode = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bincode."1.3.1" { inherit profileName; };
      bzip2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bzip2."0.3.3" { inherit profileName; };
      chrono = rustPackages."registry+https://github.com/rust-lang/crates.io-index".chrono."0.4.19" { inherit profileName; };
      clap = rustPackages."registry+https://github.com/rust-lang/crates.io-index".clap."2.33.3" { inherit profileName; };
      console = rustPackages."registry+https://github.com/rust-lang/crates.io-index".console."0.11.3" { inherit profileName; };
      ctrlc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".ctrlc."3.1.5" { inherit profileName; };
      dirs_next = rustPackages."registry+https://github.com/rust-lang/crates.io-index".dirs-next."2.0.0" { inherit profileName; };
      indicatif = rustPackages."registry+https://github.com/rust-lang/crates.io-index".indicatif."0.15.0" { inherit profileName; };
      lazy_static = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
      nix = rustPackages."registry+https://github.com/rust-lang/crates.io-index".nix."0.19.0" { inherit profileName; };
      reqwest = rustPackages."registry+https://github.com/rust-lang/crates.io-index".reqwest."0.11.2" { inherit profileName; };
      semver = rustPackages."registry+https://github.com/rust-lang/crates.io-index".semver."0.9.0" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      serde_json = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_json."1.0.62" { inherit profileName; };
      serde_yaml = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_yaml."0.8.13" { inherit profileName; };
      solana_clap_utils = rustPackages."unknown".solana-clap-utils."1.6.6" { inherit profileName; };
      solana_client = rustPackages."unknown".solana-client."1.6.6" { inherit profileName; };
      solana_config_program = rustPackages."unknown".solana-config-program."1.6.6" { inherit profileName; };
      solana_logger = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      solana_version = rustPackages."unknown".solana-version."1.6.6" { inherit profileName; };
      tar = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tar."0.4.28" { inherit profileName; };
      tempfile = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tempfile."3.1.0" { inherit profileName; };
      url = rustPackages."registry+https://github.com/rust-lang/crates.io-index".url."2.2.0" { inherit profileName; };
      ${ if hostPlatform.isWindows then "winapi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.3.8" { inherit profileName; };
      ${ if hostPlatform.isWindows then "winreg" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winreg."0.7.0" { inherit profileName; };
    };
  });
  
  "unknown".solana-keygen."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-keygen";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/keygen");
    dependencies = {
      bs58 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bs58."0.3.1" { inherit profileName; };
      clap = rustPackages."registry+https://github.com/rust-lang/crates.io-index".clap."2.33.3" { inherit profileName; };
      dirs_next = rustPackages."registry+https://github.com/rust-lang/crates.io-index".dirs-next."2.0.0" { inherit profileName; };
      num_cpus = rustPackages."registry+https://github.com/rust-lang/crates.io-index".num_cpus."1.13.0" { inherit profileName; };
      solana_clap_utils = rustPackages."unknown".solana-clap-utils."1.6.6" { inherit profileName; };
      solana_cli_config = rustPackages."unknown".solana-cli-config."1.6.6" { inherit profileName; };
      solana_remote_wallet = rustPackages."unknown".solana-remote-wallet."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      solana_version = rustPackages."unknown".solana-version."1.6.6" { inherit profileName; };
      bip39 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tiny-bip39."0.7.3" { inherit profileName; };
    };
  });
  
  "unknown".solana-ledger."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-ledger";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/ledger");
    dependencies = {
      bincode = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bincode."1.3.1" { inherit profileName; };
      byteorder = rustPackages."registry+https://github.com/rust-lang/crates.io-index".byteorder."1.3.4" { inherit profileName; };
      chrono = rustPackages."registry+https://github.com/rust-lang/crates.io-index".chrono."0.4.19" { inherit profileName; };
      chrono_humanize = rustPackages."registry+https://github.com/rust-lang/crates.io-index".chrono-humanize."0.1.1" { inherit profileName; };
      crossbeam_channel = rustPackages."registry+https://github.com/rust-lang/crates.io-index".crossbeam-channel."0.4.4" { inherit profileName; };
      dlopen = rustPackages."registry+https://github.com/rust-lang/crates.io-index".dlopen."0.1.8" { inherit profileName; };
      dlopen_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".dlopen_derive."0.1.4" { profileName = "__noProfile"; };
      ed25519_dalek = rustPackages."registry+https://github.com/rust-lang/crates.io-index".ed25519-dalek."1.0.1" { inherit profileName; };
      fs_extra = rustPackages."registry+https://github.com/rust-lang/crates.io-index".fs_extra."1.2.0" { inherit profileName; };
      futures = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures."0.3.8" { inherit profileName; };
      futures_util = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-util."0.3.8" { inherit profileName; };
      itertools = rustPackages."registry+https://github.com/rust-lang/crates.io-index".itertools."0.9.0" { inherit profileName; };
      lazy_static = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      num_cpus = rustPackages."registry+https://github.com/rust-lang/crates.io-index".num_cpus."1.13.0" { inherit profileName; };
      prost = rustPackages."registry+https://github.com/rust-lang/crates.io-index".prost."0.7.0" { inherit profileName; };
      rand = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand."0.7.3" { inherit profileName; };
      rand_chacha = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand_chacha."0.2.2" { inherit profileName; };
      rayon = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rayon."1.5.0" { inherit profileName; };
      reed_solomon_erasure = rustPackages."registry+https://github.com/rust-lang/crates.io-index".reed-solomon-erasure."4.0.2" { inherit profileName; };
      rocksdb = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rocksdb."0.15.0" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      serde_bytes = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_bytes."0.11.4" { inherit profileName; };
      sha2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".sha2."0.9.2" { inherit profileName; };
      solana_bpf_loader_program = rustPackages."unknown".solana-bpf-loader-program."1.6.6" { inherit profileName; };
      solana_frozen_abi = rustPackages."unknown".solana-frozen-abi."1.6.6" { inherit profileName; };
      solana_frozen_abi_macro = buildRustPackages."unknown".solana-frozen-abi-macro."1.6.6" { profileName = "__noProfile"; };
      solana_logger = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
      solana_measure = rustPackages."unknown".solana-measure."1.6.6" { inherit profileName; };
      solana_merkle_tree = rustPackages."unknown".solana-merkle-tree."1.6.6" { inherit profileName; };
      solana_metrics = rustPackages."unknown".solana-metrics."1.6.6" { inherit profileName; };
      solana_perf = rustPackages."unknown".solana-perf."1.6.6" { inherit profileName; };
      solana_rayon_threadlimit = rustPackages."unknown".solana-rayon-threadlimit."1.6.6" { inherit profileName; };
      solana_runtime = rustPackages."unknown".solana-runtime."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      solana_stake_program = rustPackages."unknown".solana-stake-program."1.6.6" { inherit profileName; };
      solana_storage_bigtable = rustPackages."unknown".solana-storage-bigtable."1.6.6" { inherit profileName; };
      solana_storage_proto = rustPackages."unknown".solana-storage-proto."1.6.6" { inherit profileName; };
      solana_transaction_status = rustPackages."unknown".solana-transaction-status."1.6.6" { inherit profileName; };
      solana_vote_program = rustPackages."unknown".solana-vote-program."1.6.6" { inherit profileName; };
      tempfile = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tempfile."3.1.0" { inherit profileName; };
      thiserror = rustPackages."registry+https://github.com/rust-lang/crates.io-index".thiserror."1.0.23" { inherit profileName; };
      tokio = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio."1.1.1" { inherit profileName; };
      tokio_stream = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-stream."0.1.2" { inherit profileName; };
      trees = rustPackages."registry+https://github.com/rust-lang/crates.io-index".trees."0.2.1" { inherit profileName; };
    };
    devDependencies = {
      assert_matches = rustPackages."registry+https://github.com/rust-lang/crates.io-index".assert_matches."1.3.0" { inherit profileName; };
      matches = rustPackages."registry+https://github.com/rust-lang/crates.io-index".matches."0.1.8" { inherit profileName; };
      solana_account_decoder = rustPackages."unknown".solana-account-decoder."1.6.6" { inherit profileName; };
      solana_budget_program = rustPackages."unknown".solana-budget-program."1.6.6" { inherit profileName; };
    };
    buildDependencies = {
      rustc_version = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".rustc_version."0.2.3" { profileName = "__noProfile"; };
    };
  });
  
  "unknown".solana-ledger-tool."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-ledger-tool";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/ledger-tool");
    dependencies = {
      bs58 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bs58."0.3.1" { inherit profileName; };
      bytecount = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bytecount."0.6.0" { inherit profileName; };
      clap = rustPackages."registry+https://github.com/rust-lang/crates.io-index".clap."2.33.3" { inherit profileName; };
      csv = rustPackages."registry+https://github.com/rust-lang/crates.io-index".csv."1.1.3" { inherit profileName; };
      futures = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures."0.3.8" { inherit profileName; };
      futures_util = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-util."0.3.8" { inherit profileName; };
      histogram = rustPackages."registry+https://github.com/rust-lang/crates.io-index".histogram."0.6.9" { inherit profileName; };
      itertools = rustPackages."registry+https://github.com/rust-lang/crates.io-index".itertools."0.9.0" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      regex = rustPackages."registry+https://github.com/rust-lang/crates.io-index".regex."1.3.9" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      serde_json = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_json."1.0.62" { inherit profileName; };
      serde_yaml = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_yaml."0.8.13" { inherit profileName; };
      ${ if hostPlatform.isUnix then "signal_hook" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".signal-hook."0.1.15" { inherit profileName; };
      solana_clap_utils = rustPackages."unknown".solana-clap-utils."1.6.6" { inherit profileName; };
      solana_cli_output = rustPackages."unknown".solana-cli-output."1.6.6" { inherit profileName; };
      solana_ledger = rustPackages."unknown".solana-ledger."1.6.6" { inherit profileName; };
      solana_logger = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
      solana_measure = rustPackages."unknown".solana-measure."1.6.6" { inherit profileName; };
      solana_runtime = rustPackages."unknown".solana-runtime."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      solana_stake_program = rustPackages."unknown".solana-stake-program."1.6.6" { inherit profileName; };
      solana_storage_bigtable = rustPackages."unknown".solana-storage-bigtable."1.6.6" { inherit profileName; };
      solana_transaction_status = rustPackages."unknown".solana-transaction-status."1.6.6" { inherit profileName; };
      solana_version = rustPackages."unknown".solana-version."1.6.6" { inherit profileName; };
      solana_vote_program = rustPackages."unknown".solana-vote-program."1.6.6" { inherit profileName; };
      tempfile = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tempfile."3.1.0" { inherit profileName; };
      tokio = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio."1.1.1" { inherit profileName; };
    };
    devDependencies = {
      assert_cmd = rustPackages."registry+https://github.com/rust-lang/crates.io-index".assert_cmd."1.0.2" { inherit profileName; };
    };
  });
  
  "unknown".solana-local-cluster."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-local-cluster";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/local-cluster");
    dependencies = {
      crossbeam_channel = rustPackages."registry+https://github.com/rust-lang/crates.io-index".crossbeam-channel."0.4.4" { inherit profileName; };
      fs_extra = rustPackages."registry+https://github.com/rust-lang/crates.io-index".fs_extra."1.2.0" { inherit profileName; };
      gag = rustPackages."registry+https://github.com/rust-lang/crates.io-index".gag."0.1.10" { inherit profileName; };
      itertools = rustPackages."registry+https://github.com/rust-lang/crates.io-index".itertools."0.9.0" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      rand = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand."0.7.3" { inherit profileName; };
      rayon = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rayon."1.5.0" { inherit profileName; };
      solana_client = rustPackages."unknown".solana-client."1.6.6" { inherit profileName; };
      solana_config_program = rustPackages."unknown".solana-config-program."1.6.6" { inherit profileName; };
      solana_core = rustPackages."unknown".solana-core."1.6.6" { inherit profileName; };
      solana_download_utils = rustPackages."unknown".solana-download-utils."1.6.6" { inherit profileName; };
      solana_exchange_program = rustPackages."unknown".solana-exchange-program."1.6.6" { inherit profileName; };
      solana_faucet = rustPackages."unknown".solana-faucet."1.6.6" { inherit profileName; };
      solana_ledger = rustPackages."unknown".solana-ledger."1.6.6" { inherit profileName; };
      solana_logger = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
      solana_rayon_threadlimit = rustPackages."unknown".solana-rayon-threadlimit."1.6.6" { inherit profileName; };
      solana_runtime = rustPackages."unknown".solana-runtime."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      solana_stake_program = rustPackages."unknown".solana-stake-program."1.6.6" { inherit profileName; };
      solana_vest_program = rustPackages."unknown".solana-vest-program."1.6.6" { inherit profileName; };
      solana_vote_program = rustPackages."unknown".solana-vote-program."1.6.6" { inherit profileName; };
      tempfile = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tempfile."3.1.0" { inherit profileName; };
    };
    devDependencies = {
      assert_matches = rustPackages."registry+https://github.com/rust-lang/crates.io-index".assert_matches."1.3.0" { inherit profileName; };
      serial_test = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serial_test."0.4.0" { inherit profileName; };
    };
  });
  
  "unknown".solana-log-analyzer."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-log-analyzer";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/log-analyzer");
    dependencies = {
      byte_unit = rustPackages."registry+https://github.com/rust-lang/crates.io-index".byte-unit."4.0.9" { inherit profileName; };
      clap = rustPackages."registry+https://github.com/rust-lang/crates.io-index".clap."2.33.3" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      serde_json = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_json."1.0.62" { inherit profileName; };
      solana_clap_utils = rustPackages."unknown".solana-clap-utils."1.6.6" { inherit profileName; };
      solana_logger = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
      solana_version = rustPackages."unknown".solana-version."1.6.6" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".solana-logger."1.6.4" = overridableMkRustCrate (profileName: rec {
    name = "solana-logger";
    version = "1.6.4";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "7fa7bddd7b89c26c6e3ef4af9b47d6bc8d60888559affb5160f5ade18c0cd058"; };
    dependencies = {
      env_logger = rustPackages."registry+https://github.com/rust-lang/crates.io-index".env_logger."0.8.3" { inherit profileName; };
      lazy_static = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
    };
  });
  
  "unknown".solana-logger."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-logger";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/logger");
    dependencies = {
      env_logger = rustPackages."registry+https://github.com/rust-lang/crates.io-index".env_logger."0.8.3" { inherit profileName; };
      lazy_static = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
    };
  });
  
  "unknown".solana-measure."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-measure";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/measure");
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-measure/no-jemalloc") "no-jemalloc")
    ];
    dependencies = {
      ${ if (rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-watchtower") && hostPlatform.isUnix then "jemalloc_ctl" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".jemalloc-ctl."0.3.3" { inherit profileName; };
      ${ if (rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-watchtower") && hostPlatform.isUnix then "jemallocator" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".jemallocator."0.3.2" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-watchtower" then "log" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-watchtower" then "solana_metrics" else null } = rustPackages."unknown".solana-metrics."1.6.6" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-watchtower" then "solana_sdk" else null } = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
    };
  });
  
  "unknown".solana-merkle-root-bench."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-merkle-root-bench";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/merkle-root-bench");
    dependencies = {
      clap = rustPackages."registry+https://github.com/rust-lang/crates.io-index".clap."2.33.3" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      solana_logger = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
      solana_measure = rustPackages."unknown".solana-measure."1.6.6" { inherit profileName; };
      solana_runtime = rustPackages."unknown".solana-runtime."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      solana_version = rustPackages."unknown".solana-version."1.6.6" { inherit profileName; };
    };
  });
  
  "unknown".solana-merkle-tree."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-merkle-tree";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/merkle-tree");
    dependencies = {
      fast_math = rustPackages."registry+https://github.com/rust-lang/crates.io-index".fast-math."0.1.1" { inherit profileName; };
      ${ if hostPlatform.config == "bpfel-unknown-unknown" then "matches" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".matches."0.1.8" { inherit profileName; };
      solana_program = rustPackages."unknown".solana-program."1.6.6" { inherit profileName; };
    };
    devDependencies = {
      hex = rustPackages."registry+https://github.com/rust-lang/crates.io-index".hex."0.4.2" { inherit profileName; };
    };
  });
  
  "unknown".solana-metrics."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-metrics";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/metrics");
    dependencies = {
      env_logger = rustPackages."registry+https://github.com/rust-lang/crates.io-index".env_logger."0.8.3" { inherit profileName; };
      gethostname = rustPackages."registry+https://github.com/rust-lang/crates.io-index".gethostname."0.2.1" { inherit profileName; };
      lazy_static = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      reqwest = rustPackages."registry+https://github.com/rust-lang/crates.io-index".reqwest."0.11.2" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
    };
    devDependencies = {
      rand = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand."0.7.3" { inherit profileName; };
      serial_test = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serial_test."0.4.0" { inherit profileName; };
    };
  });
  
  "unknown".solana-net-shaper."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-net-shaper";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/net-shaper");
    dependencies = {
      clap = rustPackages."registry+https://github.com/rust-lang/crates.io-index".clap."2.33.3" { inherit profileName; };
      rand = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand."0.7.3" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      serde_json = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_json."1.0.62" { inherit profileName; };
      solana_clap_utils = rustPackages."unknown".solana-clap-utils."1.6.6" { inherit profileName; };
      solana_logger = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
    };
  });
  
  "unknown".solana-net-utils."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-net-utils";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/net-utils");
    dependencies = {
      bincode = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bincode."1.3.1" { inherit profileName; };
      clap = rustPackages."registry+https://github.com/rust-lang/crates.io-index".clap."2.33.3" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      nix = rustPackages."registry+https://github.com/rust-lang/crates.io-index".nix."0.19.0" { inherit profileName; };
      rand = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand."0.7.3" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      serde_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_derive."1.0.122" { profileName = "__noProfile"; };
      socket2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".socket2."0.3.17" { inherit profileName; };
      solana_clap_utils = rustPackages."unknown".solana-clap-utils."1.6.6" { inherit profileName; };
      solana_logger = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
      solana_version = rustPackages."unknown".solana-version."1.6.6" { inherit profileName; };
      tokio = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio."1.1.1" { inherit profileName; };
      url = rustPackages."registry+https://github.com/rust-lang/crates.io-index".url."2.2.0" { inherit profileName; };
    };
  });
  
  "unknown".solana-noop-program."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-noop-program";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/programs/noop");
    dependencies = {
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      solana_logger = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
    };
  });
  
  "unknown".solana-notifier."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-notifier";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/notifier");
    dependencies = {
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      reqwest = rustPackages."registry+https://github.com/rust-lang/crates.io-index".reqwest."0.11.2" { inherit profileName; };
      serde_json = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_json."1.0.62" { inherit profileName; };
    };
  });
  
  "unknown".solana-ownable."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-ownable";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/programs/ownable");
    dependencies = {
      bincode = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bincode."1.3.1" { inherit profileName; };
      num_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".num-derive."0.3.2" { profileName = "__noProfile"; };
      num_traits = rustPackages."registry+https://github.com/rust-lang/crates.io-index".num-traits."0.2.11" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      thiserror = rustPackages."registry+https://github.com/rust-lang/crates.io-index".thiserror."1.0.23" { inherit profileName; };
    };
    devDependencies = {
      solana_runtime = rustPackages."unknown".solana-runtime."1.6.6" { inherit profileName; };
    };
  });
  
  "unknown".solana-perf."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-perf";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/perf");
    dependencies = {
      bincode = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bincode."1.3.1" { inherit profileName; };
      curve25519_dalek = rustPackages."registry+https://github.com/rust-lang/crates.io-index".curve25519-dalek."2.1.0" { inherit profileName; };
      dlopen = rustPackages."registry+https://github.com/rust-lang/crates.io-index".dlopen."0.1.8" { inherit profileName; };
      dlopen_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".dlopen_derive."0.1.4" { profileName = "__noProfile"; };
      lazy_static = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      rand = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand."0.7.3" { inherit profileName; };
      rayon = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rayon."1.5.0" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      solana_budget_program = rustPackages."unknown".solana-budget-program."1.6.6" { inherit profileName; };
      solana_logger = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
      solana_measure = rustPackages."unknown".solana-measure."1.6.6" { inherit profileName; };
      solana_metrics = rustPackages."unknown".solana-metrics."1.6.6" { inherit profileName; };
      solana_rayon_threadlimit = rustPackages."unknown".solana-rayon-threadlimit."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
    };
    devDependencies = {
      matches = rustPackages."registry+https://github.com/rust-lang/crates.io-index".matches."0.1.8" { inherit profileName; };
    };
  });
  
  "unknown".solana-poh-bench."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-poh-bench";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/poh-bench");
    dependencies = {
      clap = rustPackages."registry+https://github.com/rust-lang/crates.io-index".clap."2.33.3" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      rand = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand."0.7.3" { inherit profileName; };
      rayon = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rayon."1.5.0" { inherit profileName; };
      solana_clap_utils = rustPackages."unknown".solana-clap-utils."1.6.6" { inherit profileName; };
      solana_ledger = rustPackages."unknown".solana-ledger."1.6.6" { inherit profileName; };
      solana_logger = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
      solana_measure = rustPackages."unknown".solana-measure."1.6.6" { inherit profileName; };
      solana_perf = rustPackages."unknown".solana-perf."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      solana_version = rustPackages."unknown".solana-version."1.6.6" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".solana-program."1.6.4" = overridableMkRustCrate (profileName: rec {
    name = "solana-program";
    version = "1.6.4";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "231bf4cd42997b6f34e7c74a1449e8a1e8816fd83662fc3a5a0922da48fc667b"; };
    dependencies = {
      bincode = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bincode."1.3.1" { inherit profileName; };
      borsh = rustPackages."registry+https://github.com/rust-lang/crates.io-index".borsh."0.8.1" { inherit profileName; };
      borsh_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".borsh-derive."0.8.1" { profileName = "__noProfile"; };
      bs58 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bs58."0.3.1" { inherit profileName; };
      bv = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bv."0.11.1" { inherit profileName; };
      ${ if !(hostPlatform.parsed.cpu.name == "bpf") then "curve25519_dalek" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".curve25519-dalek."2.1.0" { inherit profileName; };
      hex = rustPackages."registry+https://github.com/rust-lang/crates.io-index".hex."0.4.2" { inherit profileName; };
      itertools = rustPackages."registry+https://github.com/rust-lang/crates.io-index".itertools."0.9.0" { inherit profileName; };
      lazy_static = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      num_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".num-derive."0.3.2" { profileName = "__noProfile"; };
      num_traits = rustPackages."registry+https://github.com/rust-lang/crates.io-index".num-traits."0.2.11" { inherit profileName; };
      ${ if !(hostPlatform.parsed.cpu.name == "bpf") then "rand" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand."0.7.3" { inherit profileName; };
      rustversion = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".rustversion."1.0.4" { profileName = "__noProfile"; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      serde_bytes = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_bytes."0.11.4" { inherit profileName; };
      serde_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_derive."1.0.122" { profileName = "__noProfile"; };
      sha2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".sha2."0.9.2" { inherit profileName; };
      solana_frozen_abi = rustPackages."registry+https://github.com/rust-lang/crates.io-index".solana-frozen-abi."1.6.4" { inherit profileName; };
      solana_frozen_abi_macro = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".solana-frozen-abi-macro."1.6.4" { profileName = "__noProfile"; };
      ${ if !(hostPlatform.parsed.cpu.name == "bpf") then "solana_logger" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".solana-logger."1.6.4" { inherit profileName; };
      solana_sdk_macro = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".solana-sdk-macro."1.6.4" { profileName = "__noProfile"; };
      thiserror = rustPackages."registry+https://github.com/rust-lang/crates.io-index".thiserror."1.0.23" { inherit profileName; };
    };
    buildDependencies = {
      rustc_version = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".rustc_version."0.2.3" { profileName = "__noProfile"; };
    };
  });
  
  "unknown".solana-program."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-program";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/sdk/program");
    dependencies = {
      bincode = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bincode."1.3.1" { inherit profileName; };
      ${ if !(hostPlatform.parsed.cpu.name == "bpf") then "blake3" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".blake3."0.3.7" { inherit profileName; };
      borsh = rustPackages."registry+https://github.com/rust-lang/crates.io-index".borsh."0.8.1" { inherit profileName; };
      borsh_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".borsh-derive."0.8.1" { profileName = "__noProfile"; };
      bs58 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bs58."0.3.1" { inherit profileName; };
      bv = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bv."0.11.1" { inherit profileName; };
      ${ if !(hostPlatform.parsed.cpu.name == "bpf") then "curve25519_dalek" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".curve25519-dalek."2.1.0" { inherit profileName; };
      hex = rustPackages."registry+https://github.com/rust-lang/crates.io-index".hex."0.4.2" { inherit profileName; };
      itertools = rustPackages."registry+https://github.com/rust-lang/crates.io-index".itertools."0.9.0" { inherit profileName; };
      lazy_static = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      num_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".num-derive."0.3.2" { profileName = "__noProfile"; };
      num_traits = rustPackages."registry+https://github.com/rust-lang/crates.io-index".num-traits."0.2.11" { inherit profileName; };
      ${ if !(hostPlatform.parsed.cpu.name == "bpf") then "rand" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand."0.7.3" { inherit profileName; };
      rustversion = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".rustversion."1.0.4" { profileName = "__noProfile"; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      serde_bytes = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_bytes."0.11.4" { inherit profileName; };
      serde_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_derive."1.0.122" { profileName = "__noProfile"; };
      sha2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".sha2."0.9.2" { inherit profileName; };
      solana_frozen_abi = rustPackages."unknown".solana-frozen-abi."1.6.6" { inherit profileName; };
      solana_frozen_abi_macro = buildRustPackages."unknown".solana-frozen-abi-macro."1.6.6" { profileName = "__noProfile"; };
      ${ if !(hostPlatform.parsed.cpu.name == "bpf") then "solana_logger" else null } = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
      solana_sdk_macro = buildRustPackages."unknown".solana-sdk-macro."1.6.6" { profileName = "__noProfile"; };
      thiserror = rustPackages."registry+https://github.com/rust-lang/crates.io-index".thiserror."1.0.23" { inherit profileName; };
    };
    devDependencies = {
      assert_matches = rustPackages."registry+https://github.com/rust-lang/crates.io-index".assert_matches."1.3.0" { inherit profileName; };
      bincode = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bincode."1.3.1" { inherit profileName; };
      serde_json = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_json."1.0.62" { inherit profileName; };
    };
    buildDependencies = {
      rustc_version = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".rustc_version."0.2.3" { profileName = "__noProfile"; };
    };
  });
  
  "unknown".solana-program-test."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-program-test";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/program-test");
    dependencies = {
      async_trait = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".async-trait."0.1.42" { profileName = "__noProfile"; };
      base64 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".base64."0.12.3" { inherit profileName; };
      bincode = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bincode."1.3.1" { inherit profileName; };
      chrono = rustPackages."registry+https://github.com/rust-lang/crates.io-index".chrono."0.4.19" { inherit profileName; };
      chrono_humanize = rustPackages."registry+https://github.com/rust-lang/crates.io-index".chrono-humanize."0.1.1" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      mio = rustPackages."registry+https://github.com/rust-lang/crates.io-index".mio."0.7.6" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      serde_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_derive."1.0.122" { profileName = "__noProfile"; };
      solana_banks_client = rustPackages."unknown".solana-banks-client."1.6.6" { inherit profileName; };
      solana_banks_server = rustPackages."unknown".solana-banks-server."1.6.6" { inherit profileName; };
      solana_bpf_loader_program = rustPackages."unknown".solana-bpf-loader-program."1.6.6" { inherit profileName; };
      solana_logger = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
      solana_runtime = rustPackages."unknown".solana-runtime."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      solana_vote_program = rustPackages."unknown".solana-vote-program."1.6.6" { inherit profileName; };
      thiserror = rustPackages."registry+https://github.com/rust-lang/crates.io-index".thiserror."1.0.23" { inherit profileName; };
      tokio = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio."1.1.1" { inherit profileName; };
    };
    devDependencies = {
      solana_stake_program = rustPackages."unknown".solana-stake-program."1.6.6" { inherit profileName; };
    };
  });
  
  "unknown".solana-ramp-tps."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-ramp-tps";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/ramp-tps");
    dependencies = {
      bzip2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bzip2."0.3.3" { inherit profileName; };
      clap = rustPackages."registry+https://github.com/rust-lang/crates.io-index".clap."2.33.3" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      reqwest = rustPackages."registry+https://github.com/rust-lang/crates.io-index".reqwest."0.11.2" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      serde_json = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_json."1.0.62" { inherit profileName; };
      serde_yaml = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_yaml."0.8.13" { inherit profileName; };
      solana_client = rustPackages."unknown".solana-client."1.6.6" { inherit profileName; };
      solana_core = rustPackages."unknown".solana-core."1.6.6" { inherit profileName; };
      solana_logger = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
      solana_metrics = rustPackages."unknown".solana-metrics."1.6.6" { inherit profileName; };
      solana_net_utils = rustPackages."unknown".solana-net-utils."1.6.6" { inherit profileName; };
      solana_notifier = rustPackages."unknown".solana-notifier."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      solana_stake_program = rustPackages."unknown".solana-stake-program."1.6.6" { inherit profileName; };
      tar = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tar."0.4.28" { inherit profileName; };
    };
  });
  
  "unknown".solana-rayon-threadlimit."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-rayon-threadlimit";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/rayon-threadlimit");
    dependencies = {
      lazy_static = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
      num_cpus = rustPackages."registry+https://github.com/rust-lang/crates.io-index".num_cpus."1.13.0" { inherit profileName; };
    };
  });
  
  "unknown".solana-remote-wallet."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-remote-wallet";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/remote-wallet");
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-remote-wallet/default" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower") "default")
      (lib.optional (rootFeatures' ? "solana-remote-wallet/linux-shared-hidraw") "linux-shared-hidraw")
      (lib.optional (rootFeatures' ? "solana-remote-wallet/linux-shared-libusb") "linux-shared-libusb")
      (lib.optional (rootFeatures' ? "solana-remote-wallet/default" || rootFeatures' ? "solana-remote-wallet/linux-static-hidraw" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower") "linux-static-hidraw")
      (lib.optional (rootFeatures' ? "solana-remote-wallet/linux-static-libusb") "linux-static-libusb")
    ];
    dependencies = {
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower" then "base32" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".base32."0.4.0" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower" then "console" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".console."0.11.3" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower" then "dialoguer" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".dialoguer."0.6.2" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower" then "hidapi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".hidapi."1.2.5" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower" then "log" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower" then "num_derive" else null } = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".num-derive."0.3.2" { profileName = "__noProfile"; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower" then "num_traits" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".num-traits."0.2.11" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower" then "parking_lot" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".parking_lot."0.10.2" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower" then "semver" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".semver."0.9.0" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower" then "solana_sdk" else null } = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower" then "thiserror" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".thiserror."1.0.23" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower" then "url" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".url."2.2.0" { inherit profileName; };
    };
  });
  
  "unknown".solana-runtime."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-runtime";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/runtime");
    dependencies = {
      arrayref = rustPackages."registry+https://github.com/rust-lang/crates.io-index".arrayref."0.3.6" { inherit profileName; };
      bincode = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bincode."1.3.1" { inherit profileName; };
      blake3 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".blake3."0.3.7" { inherit profileName; };
      bv = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bv."0.11.1" { inherit profileName; };
      byteorder = rustPackages."registry+https://github.com/rust-lang/crates.io-index".byteorder."1.3.4" { inherit profileName; };
      bzip2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bzip2."0.3.3" { inherit profileName; };
      crossbeam_channel = rustPackages."registry+https://github.com/rust-lang/crates.io-index".crossbeam-channel."0.4.4" { inherit profileName; };
      dashmap = rustPackages."registry+https://github.com/rust-lang/crates.io-index".dashmap."4.0.2" { inherit profileName; };
      dir_diff = rustPackages."registry+https://github.com/rust-lang/crates.io-index".dir-diff."0.3.2" { inherit profileName; };
      flate2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".flate2."1.0.14" { inherit profileName; };
      fnv = rustPackages."registry+https://github.com/rust-lang/crates.io-index".fnv."1.0.7" { inherit profileName; };
      itertools = rustPackages."registry+https://github.com/rust-lang/crates.io-index".itertools."0.9.0" { inherit profileName; };
      lazy_static = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      libloading = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libloading."0.6.2" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      memmap2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".memmap2."0.1.0" { inherit profileName; };
      num_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".num-derive."0.3.2" { profileName = "__noProfile"; };
      num_traits = rustPackages."registry+https://github.com/rust-lang/crates.io-index".num-traits."0.2.11" { inherit profileName; };
      num_cpus = rustPackages."registry+https://github.com/rust-lang/crates.io-index".num_cpus."1.13.0" { inherit profileName; };
      ouroboros = rustPackages."registry+https://github.com/rust-lang/crates.io-index".ouroboros."0.5.1" { inherit profileName; };
      rand = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand."0.7.3" { inherit profileName; };
      rayon = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rayon."1.5.0" { inherit profileName; };
      regex = rustPackages."registry+https://github.com/rust-lang/crates.io-index".regex."1.3.9" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      serde_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_derive."1.0.122" { profileName = "__noProfile"; };
      solana_config_program = rustPackages."unknown".solana-config-program."1.6.6" { inherit profileName; };
      solana_frozen_abi = rustPackages."unknown".solana-frozen-abi."1.6.6" { inherit profileName; };
      solana_frozen_abi_macro = buildRustPackages."unknown".solana-frozen-abi-macro."1.6.6" { profileName = "__noProfile"; };
      solana_logger = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
      solana_measure = rustPackages."unknown".solana-measure."1.6.6" { inherit profileName; };
      solana_metrics = rustPackages."unknown".solana-metrics."1.6.6" { inherit profileName; };
      solana_rayon_threadlimit = rustPackages."unknown".solana-rayon-threadlimit."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      solana_secp256k1_program = rustPackages."unknown".solana-secp256k1-program."1.6.6" { inherit profileName; };
      solana_stake_program = rustPackages."unknown".solana-stake-program."1.6.6" { inherit profileName; };
      solana_vote_program = rustPackages."unknown".solana-vote-program."1.6.6" { inherit profileName; };
      symlink = rustPackages."registry+https://github.com/rust-lang/crates.io-index".symlink."0.1.0" { inherit profileName; };
      tar = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tar."0.4.28" { inherit profileName; };
      tempfile = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tempfile."3.1.0" { inherit profileName; };
      thiserror = rustPackages."registry+https://github.com/rust-lang/crates.io-index".thiserror."1.0.23" { inherit profileName; };
      zstd = rustPackages."registry+https://github.com/rust-lang/crates.io-index".zstd."0.5.3+zstd.1.4.5" { inherit profileName; };
    };
    devDependencies = {
      assert_matches = rustPackages."registry+https://github.com/rust-lang/crates.io-index".assert_matches."1.3.0" { inherit profileName; };
      solana_noop_program = rustPackages."unknown".solana-noop-program."1.6.6" { inherit profileName; };
    };
    buildDependencies = {
      rustc_version = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".rustc_version."0.2.3" { profileName = "__noProfile"; };
    };
  });
  
  "unknown".solana-scripts."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-scripts";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/scripts");
    dependencies = {
      csv = rustPackages."registry+https://github.com/rust-lang/crates.io-index".csv."1.1.3" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
    };
  });
  
  "unknown".solana-sdk."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-sdk";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/sdk");
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-sdk/assert_matches" || rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "assert_matches")
      (lib.optional (rootFeatures' ? "solana-sdk/byteorder" || rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "byteorder")
      (lib.optional (rootFeatures' ? "solana-sdk/chrono" || rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "chrono")
      (lib.optional (rootFeatures' ? "solana-sdk/curve25519-dalek") "curve25519-dalek")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "default")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/digest" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "digest")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/ed25519-dalek" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "ed25519-dalek")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "full")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/generic-array" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "generic-array")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/libsecp256k1" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "libsecp256k1")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/memmap2" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "memmap2")
      (lib.optional (rootFeatures' ? "solana-sdk/program") "program")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/rand" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "rand")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/rand_chacha" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "rand_chacha")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/serde_json" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "serde_json")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/sha3" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "sha3")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "solana-crate-features")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-logger" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "solana-logger")
    ];
    dependencies = {
      ${ if rootFeatures' ? "solana-sdk/assert_matches" || rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "assert_matches" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".assert_matches."1.3.0" { inherit profileName; };
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "bincode" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bincode."1.3.1" { inherit profileName; };
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "bs58" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bs58."0.3.1" { inherit profileName; };
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "bv" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bv."0.11.1" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/byteorder" || rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "byteorder" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".byteorder."1.3.4" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/chrono" || rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "chrono" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".chrono."0.4.19" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk" then "curve25519_dalek" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".curve25519-dalek."2.1.0" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/digest" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "digest" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".digest."0.9.0" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/ed25519-dalek" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "ed25519_dalek" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".ed25519-dalek."1.0.1" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/generic-array" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "generic_array" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".generic-array."0.14.3" { inherit profileName; };
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "hex" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".hex."0.4.2" { inherit profileName; };
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "hmac" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".hmac."0.10.1" { inherit profileName; };
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "itertools" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".itertools."0.9.0" { inherit profileName; };
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "lazy_static" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/libsecp256k1" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "secp256k1" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libsecp256k1."0.3.5" { inherit profileName; };
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "log" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/memmap2" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "memmap2" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".memmap2."0.1.0" { inherit profileName; };
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "num_derive" else null } = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".num-derive."0.3.2" { profileName = "__noProfile"; };
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "num_traits" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".num-traits."0.2.11" { inherit profileName; };
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "pbkdf2" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".pbkdf2."0.6.0" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/rand" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "rand" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand."0.7.3" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/rand_chacha" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "rand_chacha" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand_chacha."0.2.2" { inherit profileName; };
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "rand_core" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand_core."0.6.2" { inherit profileName; };
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "rustversion" else null } = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".rustversion."1.0.4" { profileName = "__noProfile"; };
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "serde" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "serde_bytes" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_bytes."0.11.4" { inherit profileName; };
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "serde_derive" else null } = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_derive."1.0.122" { profileName = "__noProfile"; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/serde_json" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "serde_json" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_json."1.0.62" { inherit profileName; };
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "sha2" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".sha2."0.9.2" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/sha3" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "sha3" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".sha3."0.9.1" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "solana_crate_features" else null } = rustPackages."unknown".solana-crate-features."1.6.6" { inherit profileName; };
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "solana_frozen_abi" else null } = rustPackages."unknown".solana-frozen-abi."1.6.6" { inherit profileName; };
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "solana_frozen_abi_macro" else null } = buildRustPackages."unknown".solana-frozen-abi-macro."1.6.6" { profileName = "__noProfile"; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-logger" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "solana_logger" else null } = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "solana_program" else null } = rustPackages."unknown".solana-program."1.6.6" { inherit profileName; };
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "solana_sdk_macro" else null } = buildRustPackages."unknown".solana-sdk-macro."1.6.6" { profileName = "__noProfile"; };
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "thiserror" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".thiserror."1.0.23" { inherit profileName; };
    };
    devDependencies = {
      curve25519_dalek = rustPackages."registry+https://github.com/rust-lang/crates.io-index".curve25519-dalek."2.1.0" { inherit profileName; };
      bip39 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tiny-bip39."0.7.3" { inherit profileName; };
    };
    buildDependencies = {
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "rustc_version" else null } = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".rustc_version."0.2.3" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".solana-sdk-macro."1.6.4" = overridableMkRustCrate (profileName: rec {
    name = "solana-sdk-macro";
    version = "1.6.4";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "3c805f1e32677f8ec0cc0b2f470833a0d5ab0922f068e52be3a4a93b468c9c70"; };
    dependencies = {
      bs58 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bs58."0.3.1" { inherit profileName; };
      proc_macro2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."1.0.24" { inherit profileName; };
      quote = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quote."1.0.6" { inherit profileName; };
      rustversion = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".rustversion."1.0.4" { profileName = "__noProfile"; };
      syn = rustPackages."registry+https://github.com/rust-lang/crates.io-index".syn."1.0.60" { inherit profileName; };
    };
  });
  
  "unknown".solana-sdk-macro."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-sdk-macro";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/sdk/macro");
    dependencies = {
      bs58 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bs58."0.3.1" { inherit profileName; };
      proc_macro2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."1.0.24" { inherit profileName; };
      quote = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quote."1.0.6" { inherit profileName; };
      rustversion = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".rustversion."1.0.4" { profileName = "__noProfile"; };
      syn = rustPackages."registry+https://github.com/rust-lang/crates.io-index".syn."1.0.60" { inherit profileName; };
    };
  });
  
  "unknown".solana-secp256k1-program."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-secp256k1-program";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/programs/secp256k1");
    dependencies = {
      bincode = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bincode."1.3.1" { inherit profileName; };
      digest = rustPackages."registry+https://github.com/rust-lang/crates.io-index".digest."0.9.0" { inherit profileName; };
      secp256k1 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libsecp256k1."0.3.5" { inherit profileName; };
      rand = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand."0.7.3" { inherit profileName; };
      sha3 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".sha3."0.9.1" { inherit profileName; };
      solana_logger = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
    };
  });
  
  "unknown".solana-stake-accounts."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-stake-accounts";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/stake-accounts");
    dependencies = {
      clap = rustPackages."registry+https://github.com/rust-lang/crates.io-index".clap."2.33.3" { inherit profileName; };
      solana_clap_utils = rustPackages."unknown".solana-clap-utils."1.6.6" { inherit profileName; };
      solana_cli_config = rustPackages."unknown".solana-cli-config."1.6.6" { inherit profileName; };
      solana_client = rustPackages."unknown".solana-client."1.6.6" { inherit profileName; };
      solana_remote_wallet = rustPackages."unknown".solana-remote-wallet."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      solana_stake_program = rustPackages."unknown".solana-stake-program."1.6.6" { inherit profileName; };
    };
    devDependencies = {
      solana_runtime = rustPackages."unknown".solana-runtime."1.6.6" { inherit profileName; };
    };
  });
  
  "unknown".solana-stake-monitor."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-stake-monitor";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/stake-monitor");
    dependencies = {
      clap = rustPackages."registry+https://github.com/rust-lang/crates.io-index".clap."2.33.3" { inherit profileName; };
      console = rustPackages."registry+https://github.com/rust-lang/crates.io-index".console."0.11.3" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      serde_yaml = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_yaml."0.8.13" { inherit profileName; };
      solana_clap_utils = rustPackages."unknown".solana-clap-utils."1.6.6" { inherit profileName; };
      solana_cli_config = rustPackages."unknown".solana-cli-config."1.6.6" { inherit profileName; };
      solana_client = rustPackages."unknown".solana-client."1.6.6" { inherit profileName; };
      solana_logger = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
      solana_metrics = rustPackages."unknown".solana-metrics."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      solana_stake_program = rustPackages."unknown".solana-stake-program."1.6.6" { inherit profileName; };
      solana_transaction_status = rustPackages."unknown".solana-transaction-status."1.6.6" { inherit profileName; };
      solana_version = rustPackages."unknown".solana-version."1.6.6" { inherit profileName; };
    };
    devDependencies = {
      serial_test = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serial_test."0.4.0" { inherit profileName; };
      solana_core = rustPackages."unknown".solana-core."1.6.6" { inherit profileName; };
      solana_local_cluster = rustPackages."unknown".solana-local-cluster."1.6.6" { inherit profileName; };
      tempfile = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tempfile."3.1.0" { inherit profileName; };
    };
  });
  
  "unknown".solana-stake-o-matic."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-stake-o-matic";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/stake-o-matic");
    dependencies = {
      clap = rustPackages."registry+https://github.com/rust-lang/crates.io-index".clap."2.33.3" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      reqwest = rustPackages."registry+https://github.com/rust-lang/crates.io-index".reqwest."0.11.2" { inherit profileName; };
      semver = rustPackages."registry+https://github.com/rust-lang/crates.io-index".semver."0.9.0" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      serde_json = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_json."1.0.62" { inherit profileName; };
      serde_yaml = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_yaml."0.8.13" { inherit profileName; };
      solana_clap_utils = rustPackages."unknown".solana-clap-utils."1.6.6" { inherit profileName; };
      solana_cli_config = rustPackages."unknown".solana-cli-config."1.6.6" { inherit profileName; };
      solana_cli_output = rustPackages."unknown".solana-cli-output."1.6.6" { inherit profileName; };
      solana_client = rustPackages."unknown".solana-client."1.6.6" { inherit profileName; };
      solana_logger = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
      solana_metrics = rustPackages."unknown".solana-metrics."1.6.6" { inherit profileName; };
      solana_notifier = rustPackages."unknown".solana-notifier."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      solana_stake_program = rustPackages."unknown".solana-stake-program."1.6.6" { inherit profileName; };
      solana_transaction_status = rustPackages."unknown".solana-transaction-status."1.6.6" { inherit profileName; };
      thiserror = rustPackages."registry+https://github.com/rust-lang/crates.io-index".thiserror."1.0.23" { inherit profileName; };
    };
  });
  
  "unknown".solana-stake-program."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-stake-program";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/programs/stake");
    dependencies = {
      bincode = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bincode."1.3.1" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      num_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".num-derive."0.3.2" { profileName = "__noProfile"; };
      num_traits = rustPackages."registry+https://github.com/rust-lang/crates.io-index".num-traits."0.2.11" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      serde_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_derive."1.0.122" { profileName = "__noProfile"; };
      solana_config_program = rustPackages."unknown".solana-config-program."1.6.6" { inherit profileName; };
      solana_frozen_abi = rustPackages."unknown".solana-frozen-abi."1.6.6" { inherit profileName; };
      solana_frozen_abi_macro = buildRustPackages."unknown".solana-frozen-abi-macro."1.6.6" { profileName = "__noProfile"; };
      solana_metrics = rustPackages."unknown".solana-metrics."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      solana_vote_program = rustPackages."unknown".solana-vote-program."1.6.6" { inherit profileName; };
      thiserror = rustPackages."registry+https://github.com/rust-lang/crates.io-index".thiserror."1.0.23" { inherit profileName; };
    };
    devDependencies = {
      solana_logger = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
    };
    buildDependencies = {
      rustc_version = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".rustc_version."0.2.3" { profileName = "__noProfile"; };
    };
  });
  
  "unknown".solana-storage-bigtable."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-storage-bigtable";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/storage-bigtable");
    dependencies = {
      arc_swap = rustPackages."registry+https://github.com/rust-lang/crates.io-index".arc-swap."0.4.8" { inherit profileName; };
      backoff = rustPackages."registry+https://github.com/rust-lang/crates.io-index".backoff."0.3.0" { inherit profileName; };
      bincode = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bincode."1.3.1" { inherit profileName; };
      bzip2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bzip2."0.3.3" { inherit profileName; };
      enum_iterator = rustPackages."registry+https://github.com/rust-lang/crates.io-index".enum-iterator."0.6.0" { inherit profileName; };
      flate2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".flate2."1.0.14" { inherit profileName; };
      futures = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures."0.3.8" { inherit profileName; };
      goauth = rustPackages."registry+https://github.com/rust-lang/crates.io-index".goauth."0.9.0" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      prost = rustPackages."registry+https://github.com/rust-lang/crates.io-index".prost."0.7.0" { inherit profileName; };
      prost_types = rustPackages."registry+https://github.com/rust-lang/crates.io-index".prost-types."0.7.0" { inherit profileName; };
      rand_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand_core."0.6.2" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      serde_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_derive."1.0.122" { profileName = "__noProfile"; };
      smpl_jwt = rustPackages."registry+https://github.com/rust-lang/crates.io-index".smpl_jwt."0.6.0" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      solana_storage_proto = rustPackages."unknown".solana-storage-proto."1.6.6" { inherit profileName; };
      solana_transaction_status = rustPackages."unknown".solana-transaction-status."1.6.6" { inherit profileName; };
      thiserror = rustPackages."registry+https://github.com/rust-lang/crates.io-index".thiserror."1.0.23" { inherit profileName; };
      tonic = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tonic."0.4.0" { inherit profileName; };
      zstd = rustPackages."registry+https://github.com/rust-lang/crates.io-index".zstd."0.5.3+zstd.1.4.5" { inherit profileName; };
    };
  });
  
  "unknown".solana-storage-proto."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-storage-proto";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/storage-proto");
    dependencies = {
      bincode = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bincode."1.3.1" { inherit profileName; };
      bs58 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bs58."0.3.1" { inherit profileName; };
      prost = rustPackages."registry+https://github.com/rust-lang/crates.io-index".prost."0.7.0" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      serde_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_derive."1.0.122" { profileName = "__noProfile"; };
      solana_account_decoder = rustPackages."unknown".solana-account-decoder."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      solana_transaction_status = rustPackages."unknown".solana-transaction-status."1.6.6" { inherit profileName; };
    };
  });
  
  "unknown".solana-store-tool."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-store-tool";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/runtime/store-tool");
    dependencies = {
      clap = rustPackages."registry+https://github.com/rust-lang/crates.io-index".clap."2.33.3" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      solana_logger = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
      solana_measure = rustPackages."unknown".solana-measure."1.6.6" { inherit profileName; };
      solana_runtime = rustPackages."unknown".solana-runtime."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      solana_version = rustPackages."unknown".solana-version."1.6.6" { inherit profileName; };
    };
  });
  
  "unknown".solana-streamer."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-streamer";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/streamer");
    dependencies = {
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      nix = rustPackages."registry+https://github.com/rust-lang/crates.io-index".nix."0.19.0" { inherit profileName; };
      solana_logger = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
      solana_measure = rustPackages."unknown".solana-measure."1.6.6" { inherit profileName; };
      solana_metrics = rustPackages."unknown".solana-metrics."1.6.6" { inherit profileName; };
      solana_perf = rustPackages."unknown".solana-perf."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      thiserror = rustPackages."registry+https://github.com/rust-lang/crates.io-index".thiserror."1.0.23" { inherit profileName; };
    };
  });
  
  "unknown".solana-sys-tuner."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-sys-tuner";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/sys-tuner");
    dependencies = {
      clap = rustPackages."registry+https://github.com/rust-lang/crates.io-index".clap."2.33.3" { inherit profileName; };
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      ${ if hostPlatform.isUnix then "nix" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".nix."0.19.0" { inherit profileName; };
      solana_clap_utils = rustPackages."unknown".solana-clap-utils."1.6.6" { inherit profileName; };
      solana_logger = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
      solana_version = rustPackages."unknown".solana-version."1.6.6" { inherit profileName; };
      ${ if hostPlatform.isUnix then "sysctl" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".sysctl."0.4.0" { inherit profileName; };
      ${ if hostPlatform.isUnix then "unix_socket" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".unix_socket2."0.5.4" { inherit profileName; };
      ${ if hostPlatform.isUnix then "users" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".users."0.10.0" { inherit profileName; };
    };
  });
  
  "unknown".solana-tokens."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-tokens";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/tokens");
    dependencies = {
      chrono = rustPackages."registry+https://github.com/rust-lang/crates.io-index".chrono."0.4.19" { inherit profileName; };
      clap = rustPackages."registry+https://github.com/rust-lang/crates.io-index".clap."2.33.3" { inherit profileName; };
      console = rustPackages."registry+https://github.com/rust-lang/crates.io-index".console."0.11.3" { inherit profileName; };
      csv = rustPackages."registry+https://github.com/rust-lang/crates.io-index".csv."1.1.3" { inherit profileName; };
      ctrlc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".ctrlc."3.1.5" { inherit profileName; };
      dirs_next = rustPackages."registry+https://github.com/rust-lang/crates.io-index".dirs-next."2.0.0" { inherit profileName; };
      indexmap = rustPackages."registry+https://github.com/rust-lang/crates.io-index".indexmap."1.5.1" { inherit profileName; };
      indicatif = rustPackages."registry+https://github.com/rust-lang/crates.io-index".indicatif."0.15.0" { inherit profileName; };
      pickledb = rustPackages."registry+https://github.com/rust-lang/crates.io-index".pickledb."0.4.1" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      solana_account_decoder = rustPackages."unknown".solana-account-decoder."1.6.6" { inherit profileName; };
      solana_clap_utils = rustPackages."unknown".solana-clap-utils."1.6.6" { inherit profileName; };
      solana_cli_config = rustPackages."unknown".solana-cli-config."1.6.6" { inherit profileName; };
      solana_client = rustPackages."unknown".solana-client."1.6.6" { inherit profileName; };
      solana_remote_wallet = rustPackages."unknown".solana-remote-wallet."1.6.6" { inherit profileName; };
      solana_runtime = rustPackages."unknown".solana-runtime."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      solana_stake_program = rustPackages."unknown".solana-stake-program."1.6.6" { inherit profileName; };
      solana_transaction_status = rustPackages."unknown".solana-transaction-status."1.6.6" { inherit profileName; };
      solana_version = rustPackages."unknown".solana-version."1.6.6" { inherit profileName; };
      spl_associated_token_account_v1_0 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".spl-associated-token-account."1.0.2" { inherit profileName; };
      spl_token_v2_0 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".spl-token."3.1.0" { inherit profileName; };
      tempfile = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tempfile."3.1.0" { inherit profileName; };
      thiserror = rustPackages."registry+https://github.com/rust-lang/crates.io-index".thiserror."1.0.23" { inherit profileName; };
    };
    devDependencies = {
      bincode = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bincode."1.3.1" { inherit profileName; };
      solana_core = rustPackages."unknown".solana-core."1.6.6" { inherit profileName; };
      solana_logger = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
      solana_program_test = rustPackages."unknown".solana-program-test."1.6.6" { inherit profileName; };
    };
  });
  
  "unknown".solana-transaction-status."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-transaction-status";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/transaction-status");
    dependencies = {
      inflector = rustPackages."registry+https://github.com/rust-lang/crates.io-index".Inflector."0.11.4" { inherit profileName; };
      base64 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".base64."0.12.3" { inherit profileName; };
      bincode = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bincode."1.3.1" { inherit profileName; };
      bs58 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bs58."0.3.1" { inherit profileName; };
      lazy_static = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      serde_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_derive."1.0.122" { profileName = "__noProfile"; };
      serde_json = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_json."1.0.62" { inherit profileName; };
      solana_account_decoder = rustPackages."unknown".solana-account-decoder."1.6.6" { inherit profileName; };
      solana_runtime = rustPackages."unknown".solana-runtime."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      solana_stake_program = rustPackages."unknown".solana-stake-program."1.6.6" { inherit profileName; };
      solana_vote_program = rustPackages."unknown".solana-vote-program."1.6.6" { inherit profileName; };
      spl_associated_token_account_v1_0 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".spl-associated-token-account."1.0.2" { inherit profileName; };
      spl_memo = rustPackages."registry+https://github.com/rust-lang/crates.io-index".spl-memo."3.0.1" { inherit profileName; };
      spl_token_v2_0 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".spl-token."3.1.0" { inherit profileName; };
      thiserror = rustPackages."registry+https://github.com/rust-lang/crates.io-index".thiserror."1.0.23" { inherit profileName; };
    };
  });
  
  "unknown".solana-upload-perf."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-upload-perf";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/upload-perf");
    dependencies = {
      serde_json = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_json."1.0.62" { inherit profileName; };
      solana_metrics = rustPackages."unknown".solana-metrics."1.6.6" { inherit profileName; };
    };
  });
  
  "unknown".solana-validator."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-validator";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/validator");
    dependencies = {
      base64 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".base64."0.12.3" { inherit profileName; };
      bincode = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bincode."1.3.1" { inherit profileName; };
      chrono = rustPackages."registry+https://github.com/rust-lang/crates.io-index".chrono."0.4.19" { inherit profileName; };
      clap = rustPackages."registry+https://github.com/rust-lang/crates.io-index".clap."2.33.3" { inherit profileName; };
      console = rustPackages."registry+https://github.com/rust-lang/crates.io-index".console."0.11.3" { inherit profileName; };
      core_affinity = rustPackages."registry+https://github.com/rust-lang/crates.io-index".core_affinity."0.5.10" { inherit profileName; };
      fd_lock = rustPackages."registry+https://github.com/rust-lang/crates.io-index".fd-lock."2.0.0" { inherit profileName; };
      indicatif = rustPackages."registry+https://github.com/rust-lang/crates.io-index".indicatif."0.15.0" { inherit profileName; };
      jsonrpc_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".jsonrpc-core."17.0.0" { inherit profileName; };
      jsonrpc_core_client = rustPackages."registry+https://github.com/rust-lang/crates.io-index".jsonrpc-core-client."17.0.0" { inherit profileName; };
      jsonrpc_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".jsonrpc-derive."17.0.0" { profileName = "__noProfile"; };
      jsonrpc_ipc_server = rustPackages."registry+https://github.com/rust-lang/crates.io-index".jsonrpc-ipc-server."17.0.1" { inherit profileName; };
      jsonrpc_server_utils = rustPackages."registry+https://github.com/rust-lang/crates.io-index".jsonrpc-server-utils."17.0.0" { inherit profileName; };
      ${ if hostPlatform.isUnix then "libc" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      num_cpus = rustPackages."registry+https://github.com/rust-lang/crates.io-index".num_cpus."1.13.0" { inherit profileName; };
      rand = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand."0.7.3" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      ${ if hostPlatform.isUnix then "signal_hook" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".signal-hook."0.1.15" { inherit profileName; };
      solana_clap_utils = rustPackages."unknown".solana-clap-utils."1.6.6" { inherit profileName; };
      solana_cli_config = rustPackages."unknown".solana-cli-config."1.6.6" { inherit profileName; };
      solana_client = rustPackages."unknown".solana-client."1.6.6" { inherit profileName; };
      solana_core = rustPackages."unknown".solana-core."1.6.6" { inherit profileName; };
      solana_download_utils = rustPackages."unknown".solana-download-utils."1.6.6" { inherit profileName; };
      solana_faucet = rustPackages."unknown".solana-faucet."1.6.6" { inherit profileName; };
      solana_ledger = rustPackages."unknown".solana-ledger."1.6.6" { inherit profileName; };
      solana_logger = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
      solana_metrics = rustPackages."unknown".solana-metrics."1.6.6" { inherit profileName; };
      solana_net_utils = rustPackages."unknown".solana-net-utils."1.6.6" { inherit profileName; };
      solana_perf = rustPackages."unknown".solana-perf."1.6.6" { inherit profileName; };
      solana_runtime = rustPackages."unknown".solana-runtime."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      solana_version = rustPackages."unknown".solana-version."1.6.6" { inherit profileName; };
      solana_vote_program = rustPackages."unknown".solana-vote-program."1.6.6" { inherit profileName; };
      symlink = rustPackages."registry+https://github.com/rust-lang/crates.io-index".symlink."0.1.0" { inherit profileName; };
    };
  });
  
  "unknown".solana-version."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-version";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/version");
    dependencies = {
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      serde_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_derive."1.0.122" { profileName = "__noProfile"; };
      solana_frozen_abi = rustPackages."unknown".solana-frozen-abi."1.6.6" { inherit profileName; };
      solana_frozen_abi_macro = buildRustPackages."unknown".solana-frozen-abi-macro."1.6.6" { profileName = "__noProfile"; };
      solana_logger = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
    };
    buildDependencies = {
      rustc_version = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".rustc_version."0.2.3" { profileName = "__noProfile"; };
    };
  });
  
  "unknown".solana-vest-program."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-vest-program";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/programs/vest");
    dependencies = {
      bincode = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bincode."1.3.1" { inherit profileName; };
      chrono = rustPackages."registry+https://github.com/rust-lang/crates.io-index".chrono."0.4.19" { inherit profileName; };
      num_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".num-derive."0.3.2" { profileName = "__noProfile"; };
      num_traits = rustPackages."registry+https://github.com/rust-lang/crates.io-index".num-traits."0.2.11" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      serde_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_derive."1.0.122" { profileName = "__noProfile"; };
      solana_config_program = rustPackages."unknown".solana-config-program."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      thiserror = rustPackages."registry+https://github.com/rust-lang/crates.io-index".thiserror."1.0.23" { inherit profileName; };
    };
    devDependencies = {
      solana_runtime = rustPackages."unknown".solana-runtime."1.6.6" { inherit profileName; };
    };
  });
  
  "unknown".solana-vote-program."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-vote-program";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/programs/vote");
    dependencies = {
      bincode = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bincode."1.3.1" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      num_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".num-derive."0.3.2" { profileName = "__noProfile"; };
      num_traits = rustPackages."registry+https://github.com/rust-lang/crates.io-index".num-traits."0.2.11" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      serde_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_derive."1.0.122" { profileName = "__noProfile"; };
      solana_frozen_abi = rustPackages."unknown".solana-frozen-abi."1.6.6" { inherit profileName; };
      solana_frozen_abi_macro = buildRustPackages."unknown".solana-frozen-abi-macro."1.6.6" { profileName = "__noProfile"; };
      solana_logger = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
      solana_metrics = rustPackages."unknown".solana-metrics."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      thiserror = rustPackages."registry+https://github.com/rust-lang/crates.io-index".thiserror."1.0.23" { inherit profileName; };
    };
    buildDependencies = {
      rustc_version = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".rustc_version."0.2.3" { profileName = "__noProfile"; };
    };
  });
  
  "unknown".solana-watchtower."1.6.6" = overridableMkRustCrate (profileName: rec {
    name = "solana-watchtower";
    version = "1.6.6";
    registry = "unknown";
    src = fetchCrateLocal (workspaceSrc + "/watchtower");
    dependencies = {
      clap = rustPackages."registry+https://github.com/rust-lang/crates.io-index".clap."2.33.3" { inherit profileName; };
      humantime = rustPackages."registry+https://github.com/rust-lang/crates.io-index".humantime."2.0.1" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      solana_clap_utils = rustPackages."unknown".solana-clap-utils."1.6.6" { inherit profileName; };
      solana_cli_config = rustPackages."unknown".solana-cli-config."1.6.6" { inherit profileName; };
      solana_cli_output = rustPackages."unknown".solana-cli-output."1.6.6" { inherit profileName; };
      solana_client = rustPackages."unknown".solana-client."1.6.6" { inherit profileName; };
      solana_logger = rustPackages."unknown".solana-logger."1.6.6" { inherit profileName; };
      solana_metrics = rustPackages."unknown".solana-metrics."1.6.6" { inherit profileName; };
      solana_notifier = rustPackages."unknown".solana-notifier."1.6.6" { inherit profileName; };
      solana_sdk = rustPackages."unknown".solana-sdk."1.6.6" { inherit profileName; };
      solana_version = rustPackages."unknown".solana-version."1.6.6" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".solana_rbpf."0.2.7" = overridableMkRustCrate (profileName: rec {
    name = "solana_rbpf";
    version = "0.2.7";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "14e36c51d5aa290416c5dea3c43ac467cb57c0b643184af23e6bdab7434710fb"; };
    dependencies = {
      byteorder = rustPackages."registry+https://github.com/rust-lang/crates.io-index".byteorder."1.3.4" { inherit profileName; };
      combine = rustPackages."registry+https://github.com/rust-lang/crates.io-index".combine."3.8.1" { inherit profileName; };
      goblin = rustPackages."registry+https://github.com/rust-lang/crates.io-index".goblin."0.3.0" { inherit profileName; };
      hash32 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".hash32."0.1.1" { inherit profileName; };
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      rand = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand."0.7.3" { inherit profileName; };
      scroll = rustPackages."registry+https://github.com/rust-lang/crates.io-index".scroll."0.10.1" { inherit profileName; };
      thiserror = rustPackages."registry+https://github.com/rust-lang/crates.io-index".thiserror."1.0.23" { inherit profileName; };
      time = rustPackages."registry+https://github.com/rust-lang/crates.io-index".time."0.1.43" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".spin."0.5.2" = overridableMkRustCrate (profileName: rec {
    name = "spin";
    version = "0.5.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "6e63cff320ae2c57904679ba7cb63280a3dc4613885beafb148ee7bf9aa9042d"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".spl-associated-token-account."1.0.2" = overridableMkRustCrate (profileName: rec {
    name = "spl-associated-token-account";
    version = "1.0.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "4adc47eebe5d2b662cbaaba1843719c28a67e5ec5d0460bc3ca60900a51f74e2"; };
    features = builtins.concatLists [
      [ "no-entrypoint" ]
    ];
    dependencies = {
      solana_program = rustPackages."registry+https://github.com/rust-lang/crates.io-index".solana-program."1.6.4" { inherit profileName; };
      spl_token = rustPackages."registry+https://github.com/rust-lang/crates.io-index".spl-token."3.1.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".spl-memo."3.0.1" = overridableMkRustCrate (profileName: rec {
    name = "spl-memo";
    version = "3.0.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "bd0dc6f70db6bacea7ff25870b016a65ba1d1b6013536f08e4fd79a8f9005325"; };
    features = builtins.concatLists [
      [ "no-entrypoint" ]
    ];
    dependencies = {
      solana_program = rustPackages."registry+https://github.com/rust-lang/crates.io-index".solana-program."1.6.4" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".spl-token."3.1.0" = overridableMkRustCrate (profileName: rec {
    name = "spl-token";
    version = "3.1.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "b795e50d15dfd35aa5460b80a16414503a322be115a417a43db987c5824c6798"; };
    features = builtins.concatLists [
      [ "no-entrypoint" ]
    ];
    dependencies = {
      arrayref = rustPackages."registry+https://github.com/rust-lang/crates.io-index".arrayref."0.3.6" { inherit profileName; };
      num_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".num-derive."0.3.2" { profileName = "__noProfile"; };
      num_traits = rustPackages."registry+https://github.com/rust-lang/crates.io-index".num-traits."0.2.11" { inherit profileName; };
      num_enum = rustPackages."registry+https://github.com/rust-lang/crates.io-index".num_enum."0.5.1" { inherit profileName; };
      solana_program = rustPackages."registry+https://github.com/rust-lang/crates.io-index".solana-program."1.6.4" { inherit profileName; };
      thiserror = rustPackages."registry+https://github.com/rust-lang/crates.io-index".thiserror."1.0.23" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".stable_deref_trait."1.2.0" = overridableMkRustCrate (profileName: rec {
    name = "stable_deref_trait";
    version = "1.2.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "a8f112729512f8e442d81f95a8a7ddf2b7c6b8a1a6f509a95864142b30cab2d3"; };
    features = builtins.concatLists [
      [ "alloc" ]
      [ "default" ]
      [ "std" ]
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".standback."0.2.9" = overridableMkRustCrate (profileName: rec {
    name = "standback";
    version = "0.2.9";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "b0437cfb83762844799a60e1e3b489d5ceb6a650fbacb86437badc1b6d87b246"; };
    features = builtins.concatLists [
      [ "std" ]
    ];
    buildDependencies = {
      version_check = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".version_check."0.9.2" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".static_assertions."1.1.0" = overridableMkRustCrate (profileName: rec {
    name = "static_assertions";
    version = "1.1.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "a2eb9349b6444b326872e140eb1cf5e7c522154d69e7a0ffb0fb81c06b37543f"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".stdweb."0.4.20" = overridableMkRustCrate (profileName: rec {
    name = "stdweb";
    version = "0.4.20";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "d022496b16281348b52d0e30ae99e01a73d737b2f45d38fed4edf79f9325a1d5"; };
    dependencies = {
      discard = rustPackages."registry+https://github.com/rust-lang/crates.io-index".discard."1.0.4" { inherit profileName; };
      stdweb_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".stdweb-derive."0.5.3" { profileName = "__noProfile"; };
      stdweb_internal_macros = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".stdweb-internal-macros."0.2.9" { profileName = "__noProfile"; };
      stdweb_internal_runtime = rustPackages."registry+https://github.com/rust-lang/crates.io-index".stdweb-internal-runtime."0.1.5" { inherit profileName; };
      ${ if hostPlatform.parsed.cpu.name == "wasm32" && hostPlatform.parsed.vendor.name == "unknown" && hostPlatform.parsed.kernel.name == "unknown" then "wasm_bindgen" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".wasm-bindgen."0.2.70" { inherit profileName; };
    };
    buildDependencies = {
      rustc_version = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".rustc_version."0.2.3" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".stdweb-derive."0.5.3" = overridableMkRustCrate (profileName: rec {
    name = "stdweb-derive";
    version = "0.5.3";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "c87a60a40fccc84bef0652345bbbbbe20a605bf5d0ce81719fc476f5c03b50ef"; };
    dependencies = {
      proc_macro2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."1.0.24" { inherit profileName; };
      quote = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quote."1.0.6" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      serde_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_derive."1.0.122" { profileName = "__noProfile"; };
      syn = rustPackages."registry+https://github.com/rust-lang/crates.io-index".syn."1.0.60" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".stdweb-internal-macros."0.2.9" = overridableMkRustCrate (profileName: rec {
    name = "stdweb-internal-macros";
    version = "0.2.9";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "58fa5ff6ad0d98d1ffa8cb115892b6e69d67799f6763e162a1c9db421dc22e11"; };
    dependencies = {
      base_x = rustPackages."registry+https://github.com/rust-lang/crates.io-index".base-x."0.2.6" { inherit profileName; };
      proc_macro2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."1.0.24" { inherit profileName; };
      quote = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quote."1.0.6" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      serde_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_derive."1.0.122" { profileName = "__noProfile"; };
      serde_json = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_json."1.0.62" { inherit profileName; };
      sha1 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".sha1."0.6.0" { inherit profileName; };
      syn = rustPackages."registry+https://github.com/rust-lang/crates.io-index".syn."1.0.60" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".stdweb-internal-runtime."0.1.5" = overridableMkRustCrate (profileName: rec {
    name = "stdweb-internal-runtime";
    version = "0.1.5";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "213701ba3370744dcd1a12960caa4843b3d68b4d1c0a5d575e0d65b2ee9d16c0"; };
    features = builtins.concatLists [
      [ "default" ]
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".strsim."0.8.0" = overridableMkRustCrate (profileName: rec {
    name = "strsim";
    version = "0.8.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "8ea5119cdb4c55b55d432abb513a0429384878c15dde60cc77b1c99de1a95a6a"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".subtle."1.0.0" = overridableMkRustCrate (profileName: rec {
    name = "subtle";
    version = "1.0.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "2d67a5a62ba6e01cb2192ff309324cb4875d0c451d55fe2319433abe7a05a8ee"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".subtle."2.2.2" = overridableMkRustCrate (profileName: rec {
    name = "subtle";
    version = "2.2.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "7c65d530b10ccaeac294f349038a597e435b18fb456aadd0840a623f83b9e941"; };
    features = builtins.concatLists [
      [ "std" ]
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".symlink."0.1.0" = overridableMkRustCrate (profileName: rec {
    name = "symlink";
    version = "0.1.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "a7973cce6668464ea31f176d85b13c7ab3bba2cb3b77a2ed26abd7801688010a"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".syn."0.15.44" = overridableMkRustCrate (profileName: rec {
    name = "syn";
    version = "0.15.44";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "9ca4b3b69a77cbe1ffc9e198781b7acb0c7365a883670e8f1c1bc66fba79a5c5"; };
    features = builtins.concatLists [
      [ "clone-impls" ]
      [ "default" ]
      [ "derive" ]
      [ "extra-traits" ]
      [ "fold" ]
      [ "full" ]
      [ "parsing" ]
      [ "printing" ]
      [ "proc-macro" ]
      [ "quote" ]
    ];
    dependencies = {
      proc_macro2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."0.4.30" { inherit profileName; };
      quote = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quote."0.6.13" { inherit profileName; };
      unicode_xid = rustPackages."registry+https://github.com/rust-lang/crates.io-index".unicode-xid."0.1.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".syn."1.0.60" = overridableMkRustCrate (profileName: rec {
    name = "syn";
    version = "1.0.60";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "c700597eca8a5a762beb35753ef6b94df201c81cca676604f547495a0d7f0081"; };
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cargo-test-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-config" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-frozen-abi-macro" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-scripts" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-sdk-macro" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "clone-impls")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cargo-test-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-config" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-frozen-abi-macro" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-scripts" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-sdk-macro" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "default")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cargo-test-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-config" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-frozen-abi-macro" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-scripts" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-sdk-macro" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "derive")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-frozen-abi-macro" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-sdk-macro" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "extra-traits")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "fold")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-frozen-abi-macro" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-sdk-macro" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "full")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cargo-test-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-config" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-frozen-abi-macro" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-scripts" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-sdk-macro" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "parsing")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cargo-test-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-config" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-frozen-abi-macro" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-scripts" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-sdk-macro" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "printing")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cargo-test-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-config" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-frozen-abi-macro" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-scripts" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-sdk-macro" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "proc-macro")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cargo-test-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-config" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-frozen-abi-macro" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-scripts" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-sdk-macro" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "quote")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "visit")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "visit-mut")
    ];
    dependencies = {
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cargo-test-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-config" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-frozen-abi-macro" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-scripts" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-sdk-macro" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "proc_macro2" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."1.0.24" { inherit profileName; };
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cargo-test-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-config" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-frozen-abi-macro" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-scripts" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-sdk-macro" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "quote" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quote."1.0.6" { inherit profileName; };
      ${ if rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cargo-test-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-config" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-frozen-abi-macro" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-scripts" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-sdk-macro" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "unicode_xid" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".unicode-xid."0.2.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".synstructure."0.12.3" = overridableMkRustCrate (profileName: rec {
    name = "synstructure";
    version = "0.12.3";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "67656ea1dc1b41b1451851562ea232ec2e5a80242139f7e679ceccfb5d61f545"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "proc-macro" ]
    ];
    dependencies = {
      proc_macro2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."1.0.24" { inherit profileName; };
      quote = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quote."1.0.6" { inherit profileName; };
      syn = rustPackages."registry+https://github.com/rust-lang/crates.io-index".syn."1.0.60" { inherit profileName; };
      unicode_xid = rustPackages."registry+https://github.com/rust-lang/crates.io-index".unicode-xid."0.2.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".sysctl."0.4.0" = overridableMkRustCrate (profileName: rec {
    name = "sysctl";
    version = "0.4.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "0501f0d0c2aa64b419abff97c209f4b82c4e67caa63e8dc5b222ecc1b574cb5c"; };
    dependencies = {
      bitflags = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bitflags."1.2.1" { inherit profileName; };
      byteorder = rustPackages."registry+https://github.com/rust-lang/crates.io-index".byteorder."1.3.4" { inherit profileName; };
      failure = rustPackages."registry+https://github.com/rust-lang/crates.io-index".failure."0.1.8" { inherit profileName; };
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      ${ if hostPlatform.parsed.kernel.name == "android" || hostPlatform.parsed.kernel.name == "linux" then "walkdir" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".walkdir."2.3.1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".systemstat."0.1.5" = overridableMkRustCrate (profileName: rec {
    name = "systemstat";
    version = "0.1.5";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "2078da8d09c6202bffd5e075946e65bfad5ce2cfa161edb15c5f014a8440adee"; };
    dependencies = {
      bytesize = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bytesize."1.0.1" { inherit profileName; };
      chrono = rustPackages."registry+https://github.com/rust-lang/crates.io-index".chrono."0.4.19" { inherit profileName; };
      lazy_static = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      ${ if hostPlatform.parsed.kernel.name == "linux" || hostPlatform.parsed.kernel.name == "android" then "nom" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".nom."3.2.1" { inherit profileName; };
      time = rustPackages."registry+https://github.com/rust-lang/crates.io-index".time."0.1.43" { inherit profileName; };
      ${ if hostPlatform.isWindows then "winapi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.3.8" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".tar."0.4.28" = overridableMkRustCrate (profileName: rec {
    name = "tar";
    version = "0.4.28";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "5c058ad0bd6ccb84faa24cc44d4fc99bee8a5d7ba9ff33aa4d993122d1aeeac2"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "xattr" ]
    ];
    dependencies = {
      filetime = rustPackages."registry+https://github.com/rust-lang/crates.io-index".filetime."0.2.10" { inherit profileName; };
      ${ if hostPlatform.isUnix then "libc" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      ${ if hostPlatform.parsed.kernel.name == "redox" then "syscall" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".redox_syscall."0.1.56" { inherit profileName; };
      ${ if hostPlatform.isUnix then "xattr" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".xattr."0.2.2" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".tarpc."0.24.1" = overridableMkRustCrate (profileName: rec {
    name = "tarpc";
    version = "0.24.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "e325774dd5b35d979e9f4db2b0f0d7d85dc2ff2b676a3150af56c09eafc14b07"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "full" ]
      [ "serde" ]
      [ "serde-transport" ]
      [ "serde1" ]
      [ "tcp" ]
      [ "tokio-serde" ]
      [ "tokio-util" ]
      [ "tokio1" ]
    ];
    dependencies = {
      anyhow = rustPackages."registry+https://github.com/rust-lang/crates.io-index".anyhow."1.0.38" { inherit profileName; };
      fnv = rustPackages."registry+https://github.com/rust-lang/crates.io-index".fnv."1.0.7" { inherit profileName; };
      futures = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures."0.3.8" { inherit profileName; };
      humantime = rustPackages."registry+https://github.com/rust-lang/crates.io-index".humantime."2.0.1" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      pin_project = rustPackages."registry+https://github.com/rust-lang/crates.io-index".pin-project."1.0.1" { inherit profileName; };
      rand = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand."0.7.3" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      static_assertions = rustPackages."registry+https://github.com/rust-lang/crates.io-index".static_assertions."1.1.0" { inherit profileName; };
      tarpc_plugins = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".tarpc-plugins."0.9.0" { profileName = "__noProfile"; };
      tokio = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio."1.1.1" { inherit profileName; };
      tokio_serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-serde."0.8.0" { inherit profileName; };
      tokio_util = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-util."0.6.3" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".tarpc-plugins."0.9.0" = overridableMkRustCrate (profileName: rec {
    name = "tarpc-plugins";
    version = "0.9.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "f3240378a22b1195734e085ba71d1d4188d50f034aea82635acc430b7005afb5"; };
    features = builtins.concatLists [
      [ "serde1" ]
    ];
    dependencies = {
      proc_macro2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."1.0.24" { inherit profileName; };
      quote = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quote."1.0.6" { inherit profileName; };
      syn = rustPackages."registry+https://github.com/rust-lang/crates.io-index".syn."1.0.60" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".tempfile."3.1.0" = overridableMkRustCrate (profileName: rec {
    name = "tempfile";
    version = "3.1.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "7a6e24d9338a0a5be79593e2fa15a648add6138caa803e2d5bc782c371732ca9"; };
    dependencies = {
      cfg_if = rustPackages."registry+https://github.com/rust-lang/crates.io-index".cfg-if."0.1.10" { inherit profileName; };
      ${ if hostPlatform.isUnix then "libc" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      rand = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand."0.7.3" { inherit profileName; };
      ${ if hostPlatform.parsed.kernel.name == "redox" then "syscall" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".redox_syscall."0.1.56" { inherit profileName; };
      remove_dir_all = rustPackages."registry+https://github.com/rust-lang/crates.io-index".remove_dir_all."0.5.0" { inherit profileName; };
      ${ if hostPlatform.isWindows then "winapi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.3.8" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".termcolor."1.1.0" = overridableMkRustCrate (profileName: rec {
    name = "termcolor";
    version = "1.1.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "bb6bfa289a4d7c5766392812c0a1f4c1ba45afa1ad47803c11e1f407d846d75f"; };
    dependencies = {
      ${ if hostPlatform.isWindows then "winapi_util" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi-util."0.1.5" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".terminal_size."0.1.12" = overridableMkRustCrate (profileName: rec {
    name = "terminal_size";
    version = "0.1.12";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "8038f95fc7a6f351163f4b964af631bd26c9e828f7db085f2a84aca56f70d13b"; };
    dependencies = {
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      ${ if hostPlatform.isWindows then "winapi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.3.8" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".termios."0.3.2" = overridableMkRustCrate (profileName: rec {
    name = "termios";
    version = "0.3.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "6f0fcee7b24a25675de40d5bb4de6e41b0df07bc9856295e7e2b3a3600c400c2"; };
    dependencies = {
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".textwrap."0.11.0" = overridableMkRustCrate (profileName: rec {
    name = "textwrap";
    version = "0.11.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "d326610f408c7a4eb6f51c37c330e496b08506c9457c9d34287ecc38809fb060"; };
    dependencies = {
      unicode_width = rustPackages."registry+https://github.com/rust-lang/crates.io-index".unicode-width."0.1.7" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".thiserror."1.0.23" = overridableMkRustCrate (profileName: rec {
    name = "thiserror";
    version = "1.0.23";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "76cc616c6abf8c8928e2fdcc0dbfab37175edd8fb49a4641066ad1364fdab146"; };
    dependencies = {
      thiserror_impl = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".thiserror-impl."1.0.23" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".thiserror-impl."1.0.23" = overridableMkRustCrate (profileName: rec {
    name = "thiserror-impl";
    version = "1.0.23";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "9be73a2caec27583d0046ef3796c3794f868a5bc813db689eed00c7631275cd1"; };
    dependencies = {
      proc_macro2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."1.0.24" { inherit profileName; };
      quote = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quote."1.0.6" { inherit profileName; };
      syn = rustPackages."registry+https://github.com/rust-lang/crates.io-index".syn."1.0.60" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".thread-scoped."1.0.2" = overridableMkRustCrate (profileName: rec {
    name = "thread-scoped";
    version = "1.0.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "bcbb6aa301e5d3b0b5ef639c9a9c7e2f1c944f177b460c04dc24c69b1fa2bd99"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".thread_local."1.0.1" = overridableMkRustCrate (profileName: rec {
    name = "thread_local";
    version = "1.0.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "d40c6d1b69745a6ec6fb1ca717914848da4b44ae29d9b3080cbee91d72a69b14"; };
    dependencies = {
      lazy_static = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".time."0.1.43" = overridableMkRustCrate (profileName: rec {
    name = "time";
    version = "0.1.43";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "ca8a50ef2360fbd1eeb0ecd46795a87a19024eb4b53c5dc916ca1fd95fe62438"; };
    dependencies = {
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      ${ if hostPlatform.isWindows then "winapi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.3.8" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".time."0.2.25" = overridableMkRustCrate (profileName: rec {
    name = "time";
    version = "0.2.25";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "1195b046942c221454c2539395f85413b33383a067449d78aab2b7b052a142f7"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "deprecated" ]
      [ "libc" ]
      [ "std" ]
      [ "stdweb" ]
      [ "winapi" ]
    ];
    dependencies = {
      const_fn = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".const_fn."0.4.5" { profileName = "__noProfile"; };
      ${ if hostPlatform.isUnix then "libc" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      standback = rustPackages."registry+https://github.com/rust-lang/crates.io-index".standback."0.2.9" { inherit profileName; };
      ${ if hostPlatform.parsed.cpu.name == "wasm32" then "stdweb" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".stdweb."0.4.20" { inherit profileName; };
      time_macros = rustPackages."registry+https://github.com/rust-lang/crates.io-index".time-macros."0.1.0" { inherit profileName; };
      ${ if hostPlatform.isWindows then "winapi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.3.8" { inherit profileName; };
    };
    buildDependencies = {
      version_check = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".version_check."0.9.2" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".time-macros."0.1.0" = overridableMkRustCrate (profileName: rec {
    name = "time-macros";
    version = "0.1.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "9ae9b6e9f095bc105e183e3cd493d72579be3181ad4004fceb01adbe9eecab2d"; };
    dependencies = {
      proc_macro_hack = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro-hack."0.5.19" { profileName = "__noProfile"; };
      time_macros_impl = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".time-macros-impl."0.1.1" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".time-macros-impl."0.1.1" = overridableMkRustCrate (profileName: rec {
    name = "time-macros-impl";
    version = "0.1.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "e5c3be1edfad6027c69f5491cf4cb310d1a71ecd6af742788c6ff8bced86b8fa"; };
    dependencies = {
      proc_macro_hack = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro-hack."0.5.19" { profileName = "__noProfile"; };
      proc_macro2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."1.0.24" { inherit profileName; };
      quote = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quote."1.0.6" { inherit profileName; };
      standback = rustPackages."registry+https://github.com/rust-lang/crates.io-index".standback."0.2.9" { inherit profileName; };
      syn = rustPackages."registry+https://github.com/rust-lang/crates.io-index".syn."1.0.60" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".tiny-bip39."0.7.3" = overridableMkRustCrate (profileName: rec {
    name = "tiny-bip39";
    version = "0.7.3";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "b0165e045cc2ae1660270ca65e1676dbaab60feb0f91b10f7d0665e9b47e31f2"; };
    features = builtins.concatLists [
      [ "chinese-simplified" ]
      [ "chinese-traditional" ]
      [ "default" ]
      [ "french" ]
      [ "italian" ]
      [ "japanese" ]
      [ "korean" ]
      [ "spanish" ]
    ];
    dependencies = {
      failure = rustPackages."registry+https://github.com/rust-lang/crates.io-index".failure."0.1.8" { inherit profileName; };
      hmac = rustPackages."registry+https://github.com/rust-lang/crates.io-index".hmac."0.7.1" { inherit profileName; };
      once_cell = rustPackages."registry+https://github.com/rust-lang/crates.io-index".once_cell."1.5.2" { inherit profileName; };
      pbkdf2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".pbkdf2."0.3.0" { inherit profileName; };
      rand = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand."0.7.3" { inherit profileName; };
      rustc_hash = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rustc-hash."1.1.0" { inherit profileName; };
      sha2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".sha2."0.8.2" { inherit profileName; };
      unicode_normalization = rustPackages."registry+https://github.com/rust-lang/crates.io-index".unicode-normalization."0.1.16" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".tiny-bip39."0.8.0" = overridableMkRustCrate (profileName: rec {
    name = "tiny-bip39";
    version = "0.8.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "d9e44c4759bae7f1032e286a7ef990bd9ed23fe831b7eeba0beb97484c2e59b8"; };
    features = builtins.concatLists [
      [ "chinese-simplified" ]
      [ "chinese-traditional" ]
      [ "default" ]
      [ "french" ]
      [ "italian" ]
      [ "japanese" ]
      [ "korean" ]
      [ "spanish" ]
    ];
    dependencies = {
      anyhow = rustPackages."registry+https://github.com/rust-lang/crates.io-index".anyhow."1.0.38" { inherit profileName; };
      hmac = rustPackages."registry+https://github.com/rust-lang/crates.io-index".hmac."0.8.1" { inherit profileName; };
      once_cell = rustPackages."registry+https://github.com/rust-lang/crates.io-index".once_cell."1.5.2" { inherit profileName; };
      pbkdf2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".pbkdf2."0.4.0" { inherit profileName; };
      rand = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand."0.7.3" { inherit profileName; };
      rustc_hash = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rustc-hash."1.1.0" { inherit profileName; };
      sha2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".sha2."0.9.2" { inherit profileName; };
      thiserror = rustPackages."registry+https://github.com/rust-lang/crates.io-index".thiserror."1.0.23" { inherit profileName; };
      unicode_normalization = rustPackages."registry+https://github.com/rust-lang/crates.io-index".unicode-normalization."0.1.16" { inherit profileName; };
      zeroize = rustPackages."registry+https://github.com/rust-lang/crates.io-index".zeroize."1.2.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".tinyvec."1.1.1" = overridableMkRustCrate (profileName: rec {
    name = "tinyvec";
    version = "1.1.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "317cca572a0e89c3ce0ca1f1bdc9369547fe318a683418e42ac8f59d14701023"; };
    features = builtins.concatLists [
      [ "alloc" ]
      [ "default" ]
      [ "tinyvec_macros" ]
    ];
    dependencies = {
      tinyvec_macros = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tinyvec_macros."0.1.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".tinyvec_macros."0.1.0" = overridableMkRustCrate (profileName: rec {
    name = "tinyvec_macros";
    version = "0.1.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "cda74da7e1a664f795bb1f8a87ec406fb89a02522cf6e50620d016add6dbbf5c"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".tokio."0.1.22" = overridableMkRustCrate (profileName: rec {
    name = "tokio";
    version = "0.1.22";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "5a09c0b5bb588872ab2f09afa13ee6e9dac11e10a0ec9e8e3ba39a5a5d530af6"; };
    features = builtins.concatLists [
      [ "bytes" ]
      [ "codec" ]
      [ "default" ]
      [ "fs" ]
      [ "io" ]
      [ "mio" ]
      [ "num_cpus" ]
      [ "reactor" ]
      [ "rt-full" ]
      [ "sync" ]
      [ "tcp" ]
      [ "timer" ]
      [ "tokio-codec" ]
      [ "tokio-current-thread" ]
      [ "tokio-executor" ]
      [ "tokio-fs" ]
      [ "tokio-io" ]
      [ "tokio-reactor" ]
      [ "tokio-sync" ]
      [ "tokio-tcp" ]
      [ "tokio-threadpool" ]
      [ "tokio-timer" ]
      [ "tokio-udp" ]
      [ "tokio-uds" ]
      [ "udp" ]
      [ "uds" ]
    ];
    dependencies = {
      bytes = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bytes."0.4.12" { inherit profileName; };
      futures = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures."0.1.29" { inherit profileName; };
      mio = rustPackages."registry+https://github.com/rust-lang/crates.io-index".mio."0.6.22" { inherit profileName; };
      num_cpus = rustPackages."registry+https://github.com/rust-lang/crates.io-index".num_cpus."1.13.0" { inherit profileName; };
      tokio_codec = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-codec."0.1.2" { inherit profileName; };
      tokio_current_thread = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-current-thread."0.1.7" { inherit profileName; };
      tokio_executor = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-executor."0.1.10" { inherit profileName; };
      tokio_fs = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-fs."0.1.7" { inherit profileName; };
      tokio_io = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-io."0.1.13" { inherit profileName; };
      tokio_reactor = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-reactor."0.1.12" { inherit profileName; };
      tokio_sync = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-sync."0.1.8" { inherit profileName; };
      tokio_tcp = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-tcp."0.1.4" { inherit profileName; };
      tokio_threadpool = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-threadpool."0.1.18" { inherit profileName; };
      tokio_timer = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-timer."0.2.13" { inherit profileName; };
      tokio_udp = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-udp."0.1.6" { inherit profileName; };
      ${ if hostPlatform.isUnix then "tokio_uds" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-uds."0.2.6" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".tokio."0.2.22" = overridableMkRustCrate (profileName: rec {
    name = "tokio";
    version = "0.2.22";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "5d34ca54d84bf2b5b4d7d31e901a8464f7b60ac145a284fba25ceb801f2ddccd"; };
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "blocking")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "default")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "dns")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "fnv")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "fs")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "full")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "futures-core")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "io-driver")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "io-std")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "io-util")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "iovec")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "lazy_static")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "libc")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "macros")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "memchr")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "mio")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "mio-named-pipes")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "mio-uds")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "net")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "num_cpus")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "process")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "rt-core")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "rt-threaded")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "rt-util")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "signal")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "signal-hook-registry")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "slab")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "stream")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "sync")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "tcp")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "time")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "tokio-macros")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "udp")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "uds")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "winapi")
    ];
    dependencies = {
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" then "bytes" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bytes."0.5.4" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" then "fnv" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".fnv."1.0.7" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" then "futures_core" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-core."0.3.8" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" then "iovec" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".iovec."0.1.4" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" then "lazy_static" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
      ${ if (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") && hostPlatform.isUnix then "libc" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" then "memchr" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".memchr."2.3.3" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" then "mio" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".mio."0.6.22" { inherit profileName; };
      ${ if (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") && hostPlatform.isWindows then "mio_named_pipes" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".mio-named-pipes."0.1.7" { inherit profileName; };
      ${ if (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") && hostPlatform.isUnix then "mio_uds" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".mio-uds."0.6.8" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" then "num_cpus" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".num_cpus."1.13.0" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" then "pin_project_lite" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".pin-project-lite."0.1.5" { inherit profileName; };
      ${ if (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") && hostPlatform.isUnix then "signal_hook_registry" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".signal-hook-registry."1.2.0" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" then "slab" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".slab."0.4.2" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" then "tokio_macros" else null } = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-macros."0.2.6" { profileName = "__noProfile"; };
      ${ if (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") && hostPlatform.isWindows then "winapi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.3.8" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".tokio."1.1.1" = overridableMkRustCrate (profileName: rec {
    name = "tokio";
    version = "1.1.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "6714d663090b6b0acb0fa85841c6d66233d150cdb2602c8f9b8abb03370beb3f"; };
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "bytes")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "default")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower") "fs")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower") "full")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower") "io-std")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "io-util")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "libc")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower") "macros")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "memchr")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "mio")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "net")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "num_cpus")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower") "once_cell")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower") "parking_lot")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower") "process")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "rt")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "rt-multi-thread")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower") "signal")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower") "signal-hook-registry")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "sync")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "time")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower") "tokio-macros")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower") "winapi")
    ];
    dependencies = {
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "bytes" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bytes."1.0.1" { inherit profileName; };
      ${ if (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") && hostPlatform.isUnix then "libc" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "memchr" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".memchr."2.3.3" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "mio" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".mio."0.7.6" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "num_cpus" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".num_cpus."1.13.0" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower" then "once_cell" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".once_cell."1.5.2" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower" then "parking_lot" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".parking_lot."0.11.0" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "pin_project_lite" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".pin-project-lite."0.2.4" { inherit profileName; };
      ${ if (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower") && hostPlatform.isUnix then "signal_hook_registry" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".signal-hook-registry."1.2.0" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower" then "tokio_macros" else null } = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-macros."1.0.0" { profileName = "__noProfile"; };
      ${ if (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower") && hostPlatform.isWindows then "winapi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.3.8" { inherit profileName; };
    };
    buildDependencies = {
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "autocfg" else null } = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".autocfg."1.0.0" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".tokio-codec."0.1.2" = overridableMkRustCrate (profileName: rec {
    name = "tokio-codec";
    version = "0.1.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "25b2998660ba0e70d18684de5d06b70b70a3a747469af9dea7618cc59e75976b"; };
    dependencies = {
      bytes = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bytes."0.4.12" { inherit profileName; };
      futures = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures."0.1.29" { inherit profileName; };
      tokio_io = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-io."0.1.13" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".tokio-current-thread."0.1.7" = overridableMkRustCrate (profileName: rec {
    name = "tokio-current-thread";
    version = "0.1.7";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "b1de0e32a83f131e002238d7ccde18211c0a5397f60cbfffcb112868c2e0e20e"; };
    dependencies = {
      futures = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures."0.1.29" { inherit profileName; };
      tokio_executor = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-executor."0.1.10" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".tokio-executor."0.1.10" = overridableMkRustCrate (profileName: rec {
    name = "tokio-executor";
    version = "0.1.10";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "fb2d1b8f4548dbf5e1f7818512e9c406860678f29c300cdf0ebac72d1a3a1671"; };
    dependencies = {
      crossbeam_utils = rustPackages."registry+https://github.com/rust-lang/crates.io-index".crossbeam-utils."0.7.2" { inherit profileName; };
      futures = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures."0.1.29" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".tokio-fs."0.1.7" = overridableMkRustCrate (profileName: rec {
    name = "tokio-fs";
    version = "0.1.7";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "297a1206e0ca6302a0eed35b700d292b275256f596e2f3fea7729d5e629b6ff4"; };
    dependencies = {
      futures = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures."0.1.29" { inherit profileName; };
      tokio_io = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-io."0.1.13" { inherit profileName; };
      tokio_threadpool = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-threadpool."0.1.18" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".tokio-io."0.1.13" = overridableMkRustCrate (profileName: rec {
    name = "tokio-io";
    version = "0.1.13";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "57fc868aae093479e3131e3d165c93b1c7474109d13c90ec0dda2a1bbfff0674"; };
    dependencies = {
      bytes = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bytes."0.4.12" { inherit profileName; };
      futures = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures."0.1.29" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".tokio-macros."0.2.6" = overridableMkRustCrate (profileName: rec {
    name = "tokio-macros";
    version = "0.2.6";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "e44da00bfc73a25f814cd8d7e57a68a5c31b74b3152a0a1d1f590c97ed06265a"; };
    dependencies = {
      proc_macro2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."1.0.24" { inherit profileName; };
      quote = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quote."1.0.6" { inherit profileName; };
      syn = rustPackages."registry+https://github.com/rust-lang/crates.io-index".syn."1.0.60" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".tokio-macros."1.0.0" = overridableMkRustCrate (profileName: rec {
    name = "tokio-macros";
    version = "1.0.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "42517d2975ca3114b22a16192634e8241dc5cc1f130be194645970cc1c371494"; };
    dependencies = {
      proc_macro2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."1.0.24" { inherit profileName; };
      quote = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quote."1.0.6" { inherit profileName; };
      syn = rustPackages."registry+https://github.com/rust-lang/crates.io-index".syn."1.0.60" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".tokio-native-tls."0.3.0" = overridableMkRustCrate (profileName: rec {
    name = "tokio-native-tls";
    version = "0.3.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "f7d995660bd2b7f8c1568414c1126076c13fbb725c40112dc0120b78eb9b717b"; };
    dependencies = {
      native_tls = rustPackages."registry+https://github.com/rust-lang/crates.io-index".native-tls."0.2.7" { inherit profileName; };
      tokio = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio."1.1.1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".tokio-reactor."0.1.12" = overridableMkRustCrate (profileName: rec {
    name = "tokio-reactor";
    version = "0.1.12";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "09bc590ec4ba8ba87652da2068d150dcada2cfa2e07faae270a5e0409aa51351"; };
    dependencies = {
      crossbeam_utils = rustPackages."registry+https://github.com/rust-lang/crates.io-index".crossbeam-utils."0.7.2" { inherit profileName; };
      futures = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures."0.1.29" { inherit profileName; };
      lazy_static = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      mio = rustPackages."registry+https://github.com/rust-lang/crates.io-index".mio."0.6.22" { inherit profileName; };
      num_cpus = rustPackages."registry+https://github.com/rust-lang/crates.io-index".num_cpus."1.13.0" { inherit profileName; };
      parking_lot = rustPackages."registry+https://github.com/rust-lang/crates.io-index".parking_lot."0.9.0" { inherit profileName; };
      slab = rustPackages."registry+https://github.com/rust-lang/crates.io-index".slab."0.4.2" { inherit profileName; };
      tokio_executor = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-executor."0.1.10" { inherit profileName; };
      tokio_io = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-io."0.1.13" { inherit profileName; };
      tokio_sync = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-sync."0.1.8" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".tokio-rustls."0.22.0" = overridableMkRustCrate (profileName: rec {
    name = "tokio-rustls";
    version = "0.22.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "bc6844de72e57df1980054b38be3a9f4702aba4858be64dd700181a8a6d0e1b6"; };
    dependencies = {
      rustls = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rustls."0.19.0" { inherit profileName; };
      tokio = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio."1.1.1" { inherit profileName; };
      webpki = rustPackages."registry+https://github.com/rust-lang/crates.io-index".webpki."0.21.2" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".tokio-serde."0.8.0" = overridableMkRustCrate (profileName: rec {
    name = "tokio-serde";
    version = "0.8.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "911a61637386b789af998ee23f50aa30d5fd7edcec8d6d3dedae5e5815205466"; };
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "bincode")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "bincode-crate")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "educe")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "json")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "serde")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "serde_json")
    ];
    dependencies = {
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" then "bincode_crate" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bincode."1.3.1" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" then "bytes" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bytes."1.0.1" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" then "educe" else null } = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".educe."0.4.13" { profileName = "__noProfile"; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" then "futures_core" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-core."0.3.8" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" then "futures_sink" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-sink."0.3.8" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" then "pin_project" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".pin-project."1.0.1" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" then "serde" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" then "serde_json" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_json."1.0.62" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".tokio-stream."0.1.2" = overridableMkRustCrate (profileName: rec {
    name = "tokio-stream";
    version = "0.1.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "76066865172052eb8796c686f0b441a93df8b08d40a950b062ffb9a426f00edd"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "time" ]
    ];
    dependencies = {
      futures_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-core."0.3.8" { inherit profileName; };
      pin_project_lite = rustPackages."registry+https://github.com/rust-lang/crates.io-index".pin-project-lite."0.2.4" { inherit profileName; };
      tokio = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio."1.1.1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".tokio-sync."0.1.8" = overridableMkRustCrate (profileName: rec {
    name = "tokio-sync";
    version = "0.1.8";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "edfe50152bc8164fcc456dab7891fa9bf8beaf01c5ee7e1dd43a397c3cf87dee"; };
    dependencies = {
      fnv = rustPackages."registry+https://github.com/rust-lang/crates.io-index".fnv."1.0.7" { inherit profileName; };
      futures = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures."0.1.29" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".tokio-tcp."0.1.4" = overridableMkRustCrate (profileName: rec {
    name = "tokio-tcp";
    version = "0.1.4";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "98df18ed66e3b72e742f185882a9e201892407957e45fbff8da17ae7a7c51f72"; };
    dependencies = {
      bytes = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bytes."0.4.12" { inherit profileName; };
      futures = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures."0.1.29" { inherit profileName; };
      iovec = rustPackages."registry+https://github.com/rust-lang/crates.io-index".iovec."0.1.4" { inherit profileName; };
      mio = rustPackages."registry+https://github.com/rust-lang/crates.io-index".mio."0.6.22" { inherit profileName; };
      tokio_io = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-io."0.1.13" { inherit profileName; };
      tokio_reactor = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-reactor."0.1.12" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".tokio-threadpool."0.1.18" = overridableMkRustCrate (profileName: rec {
    name = "tokio-threadpool";
    version = "0.1.18";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "df720b6581784c118f0eb4310796b12b1d242a7eb95f716a8367855325c25f89"; };
    dependencies = {
      crossbeam_deque = rustPackages."registry+https://github.com/rust-lang/crates.io-index".crossbeam-deque."0.7.3" { inherit profileName; };
      crossbeam_queue = rustPackages."registry+https://github.com/rust-lang/crates.io-index".crossbeam-queue."0.2.3" { inherit profileName; };
      crossbeam_utils = rustPackages."registry+https://github.com/rust-lang/crates.io-index".crossbeam-utils."0.7.2" { inherit profileName; };
      futures = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures."0.1.29" { inherit profileName; };
      lazy_static = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      num_cpus = rustPackages."registry+https://github.com/rust-lang/crates.io-index".num_cpus."1.13.0" { inherit profileName; };
      slab = rustPackages."registry+https://github.com/rust-lang/crates.io-index".slab."0.4.2" { inherit profileName; };
      tokio_executor = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-executor."0.1.10" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".tokio-timer."0.2.13" = overridableMkRustCrate (profileName: rec {
    name = "tokio-timer";
    version = "0.2.13";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "93044f2d313c95ff1cb7809ce9a7a05735b012288a888b62d4434fd58c94f296"; };
    dependencies = {
      crossbeam_utils = rustPackages."registry+https://github.com/rust-lang/crates.io-index".crossbeam-utils."0.7.2" { inherit profileName; };
      futures = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures."0.1.29" { inherit profileName; };
      slab = rustPackages."registry+https://github.com/rust-lang/crates.io-index".slab."0.4.2" { inherit profileName; };
      tokio_executor = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-executor."0.1.10" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".tokio-tls."0.2.1" = overridableMkRustCrate (profileName: rec {
    name = "tokio-tls";
    version = "0.2.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "354b8cd83825b3c20217a9dc174d6a0c67441a2fae5c41bcb1ea6679f6ae0f7c"; };
    dependencies = {
      futures = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures."0.1.29" { inherit profileName; };
      native_tls = rustPackages."registry+https://github.com/rust-lang/crates.io-index".native-tls."0.2.7" { inherit profileName; };
      tokio_io = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-io."0.1.13" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".tokio-udp."0.1.6" = overridableMkRustCrate (profileName: rec {
    name = "tokio-udp";
    version = "0.1.6";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "e2a0b10e610b39c38b031a2fcab08e4b82f16ece36504988dcbd81dbba650d82"; };
    dependencies = {
      bytes = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bytes."0.4.12" { inherit profileName; };
      futures = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures."0.1.29" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      mio = rustPackages."registry+https://github.com/rust-lang/crates.io-index".mio."0.6.22" { inherit profileName; };
      tokio_codec = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-codec."0.1.2" { inherit profileName; };
      tokio_io = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-io."0.1.13" { inherit profileName; };
      tokio_reactor = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-reactor."0.1.12" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".tokio-uds."0.2.6" = overridableMkRustCrate (profileName: rec {
    name = "tokio-uds";
    version = "0.2.6";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "5076db410d6fdc6523df7595447629099a1fdc47b3d9f896220780fa48faf798"; };
    dependencies = {
      bytes = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bytes."0.4.12" { inherit profileName; };
      futures = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures."0.1.29" { inherit profileName; };
      iovec = rustPackages."registry+https://github.com/rust-lang/crates.io-index".iovec."0.1.4" { inherit profileName; };
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      mio = rustPackages."registry+https://github.com/rust-lang/crates.io-index".mio."0.6.22" { inherit profileName; };
      mio_uds = rustPackages."registry+https://github.com/rust-lang/crates.io-index".mio-uds."0.6.8" { inherit profileName; };
      tokio_codec = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-codec."0.1.2" { inherit profileName; };
      tokio_io = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-io."0.1.13" { inherit profileName; };
      tokio_reactor = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-reactor."0.1.12" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".tokio-util."0.3.1" = overridableMkRustCrate (profileName: rec {
    name = "tokio-util";
    version = "0.3.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "be8242891f2b6cbef26a2d7e8605133c2c554cd35b3e4948ea892d6d68436499"; };
    features = builtins.concatLists [
      [ "codec" ]
      [ "default" ]
    ];
    dependencies = {
      bytes = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bytes."0.5.4" { inherit profileName; };
      futures_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-core."0.3.8" { inherit profileName; };
      futures_sink = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-sink."0.3.8" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      pin_project_lite = rustPackages."registry+https://github.com/rust-lang/crates.io-index".pin-project-lite."0.1.5" { inherit profileName; };
      tokio = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio."0.2.22" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".tokio-util."0.6.3" = overridableMkRustCrate (profileName: rec {
    name = "tokio-util";
    version = "0.6.3";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "ebb7cb2f00c5ae8df755b252306272cd1790d39728363936e01827e11f0b017b"; };
    features = builtins.concatLists [
      [ "codec" ]
      [ "default" ]
    ];
    dependencies = {
      bytes = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bytes."1.0.1" { inherit profileName; };
      futures_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-core."0.3.8" { inherit profileName; };
      futures_sink = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-sink."0.3.8" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      pin_project_lite = rustPackages."registry+https://github.com/rust-lang/crates.io-index".pin-project-lite."0.2.4" { inherit profileName; };
      tokio = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio."1.1.1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".toml."0.5.6" = overridableMkRustCrate (profileName: rec {
    name = "toml";
    version = "0.5.6";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "ffc92d160b1eef40665be3a05630d003936a3bc7da7421277846c2613e92c71a"; };
    features = builtins.concatLists [
      [ "default" ]
    ];
    dependencies = {
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".tonic."0.4.0" = overridableMkRustCrate (profileName: rec {
    name = "tonic";
    version = "0.4.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "3ba8f479158947373b6df40cf48f4779bb25c99ca3c661bd95e0ab1963ad8b0e"; };
    features = builtins.concatLists [
      [ "async-trait" ]
      [ "codegen" ]
      [ "default" ]
      [ "h2" ]
      [ "hyper" ]
      [ "prost" ]
      [ "prost-derive" ]
      [ "prost1" ]
      [ "tls" ]
      [ "tokio" ]
      [ "tokio-rustls" ]
      [ "tower" ]
      [ "tracing-futures" ]
      [ "transport" ]
    ];
    dependencies = {
      async_stream = rustPackages."registry+https://github.com/rust-lang/crates.io-index".async-stream."0.3.0" { inherit profileName; };
      async_trait = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".async-trait."0.1.42" { profileName = "__noProfile"; };
      base64 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".base64."0.13.0" { inherit profileName; };
      bytes = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bytes."1.0.1" { inherit profileName; };
      futures_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-core."0.3.8" { inherit profileName; };
      futures_util = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-util."0.3.8" { inherit profileName; };
      h2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".h2."0.3.0" { inherit profileName; };
      http = rustPackages."registry+https://github.com/rust-lang/crates.io-index".http."0.2.1" { inherit profileName; };
      http_body = rustPackages."registry+https://github.com/rust-lang/crates.io-index".http-body."0.4.0" { inherit profileName; };
      hyper = rustPackages."registry+https://github.com/rust-lang/crates.io-index".hyper."0.14.3" { inherit profileName; };
      percent_encoding = rustPackages."registry+https://github.com/rust-lang/crates.io-index".percent-encoding."2.1.0" { inherit profileName; };
      pin_project = rustPackages."registry+https://github.com/rust-lang/crates.io-index".pin-project."1.0.1" { inherit profileName; };
      prost1 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".prost."0.7.0" { inherit profileName; };
      prost_derive = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".prost-derive."0.7.0" { profileName = "__noProfile"; };
      tokio = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio."1.1.1" { inherit profileName; };
      tokio_rustls = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-rustls."0.22.0" { inherit profileName; };
      tokio_stream = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-stream."0.1.2" { inherit profileName; };
      tokio_util = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-util."0.6.3" { inherit profileName; };
      tower = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tower."0.4.4" { inherit profileName; };
      tower_service = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tower-service."0.3.0" { inherit profileName; };
      tracing = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tracing."0.1.18" { inherit profileName; };
      tracing_futures = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tracing-futures."0.2.4" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".tower."0.4.4" = overridableMkRustCrate (profileName: rec {
    name = "tower";
    version = "0.4.4";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "5fd7b451959622e21de79261673d658a0944b835012c58c51878ea55957fb51a"; };
    features = builtins.concatLists [
      [ "balance" ]
      [ "buffer" ]
      [ "default" ]
      [ "discover" ]
      [ "futures-util" ]
      [ "indexmap" ]
      [ "limit" ]
      [ "load" ]
      [ "log" ]
      [ "make" ]
      [ "rand" ]
      [ "ready-cache" ]
      [ "slab" ]
      [ "timeout" ]
      [ "tokio" ]
      [ "tokio-stream" ]
      [ "util" ]
    ];
    dependencies = {
      futures_core = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-core."0.3.8" { inherit profileName; };
      futures_util = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures-util."0.3.8" { inherit profileName; };
      indexmap = rustPackages."registry+https://github.com/rust-lang/crates.io-index".indexmap."1.5.1" { inherit profileName; };
      pin_project = rustPackages."registry+https://github.com/rust-lang/crates.io-index".pin-project."1.0.1" { inherit profileName; };
      rand = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand."0.8.3" { inherit profileName; };
      slab = rustPackages."registry+https://github.com/rust-lang/crates.io-index".slab."0.4.2" { inherit profileName; };
      tokio = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio."1.1.1" { inherit profileName; };
      tokio_stream = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-stream."0.1.2" { inherit profileName; };
      tower_layer = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tower-layer."0.3.1" { inherit profileName; };
      tower_service = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tower-service."0.3.0" { inherit profileName; };
      tracing = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tracing."0.1.18" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".tower-layer."0.3.1" = overridableMkRustCrate (profileName: rec {
    name = "tower-layer";
    version = "0.3.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "343bc9466d3fe6b0f960ef45960509f84480bf4fd96f92901afe7ff3df9d3a62"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".tower-service."0.3.0" = overridableMkRustCrate (profileName: rec {
    name = "tower-service";
    version = "0.3.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "e987b6bf443f4b5b3b6f38704195592cca41c5bb7aedd3c3693c7081f8289860"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".tracing."0.1.18" = overridableMkRustCrate (profileName: rec {
    name = "tracing";
    version = "0.1.18";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "f0aae59226cf195d8e74d4b34beae1859257efb4e5fed3f147d2dc2c7d372178"; };
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "attributes")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "default")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "log")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "std")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "tracing-attributes")
    ];
    dependencies = {
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "cfg_if" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".cfg-if."0.1.10" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" then "log" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      ${ if rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" then "tracing_attributes" else null } = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".tracing-attributes."0.1.9" { profileName = "__noProfile"; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "tracing_core" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tracing-core."0.1.13" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".tracing-attributes."0.1.9" = overridableMkRustCrate (profileName: rec {
    name = "tracing-attributes";
    version = "0.1.9";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "f0693bf8d6f2bf22c690fc61a9d21ac69efdbb894a17ed596b9af0f01e64b84b"; };
    dependencies = {
      proc_macro2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."1.0.24" { inherit profileName; };
      quote = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quote."1.0.6" { inherit profileName; };
      syn = rustPackages."registry+https://github.com/rust-lang/crates.io-index".syn."1.0.60" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".tracing-core."0.1.13" = overridableMkRustCrate (profileName: rec {
    name = "tracing-core";
    version = "0.1.13";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "d593f98af59ebc017c0648f0117525db358745a8894a8d684e185ba3f45954f9"; };
    features = builtins.concatLists [
      [ "lazy_static" ]
      [ "std" ]
    ];
    dependencies = {
      lazy_static = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".tracing-futures."0.2.4" = overridableMkRustCrate (profileName: rec {
    name = "tracing-futures";
    version = "0.2.4";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "ab7bb6f14721aa00656086e9335d363c5c8747bae02ebe32ea2c7dece5689b4c"; };
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "default")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "pin-project")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "std")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "std-future")
    ];
    dependencies = {
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "pin_project" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".pin-project."0.4.23" { inherit profileName; };
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "tracing" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tracing."0.1.18" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".traitobject."0.1.0" = overridableMkRustCrate (profileName: rec {
    name = "traitobject";
    version = "0.1.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "efd1f82c56340fdf16f2a953d7bda4f8fdffba13d93b00844c25572110b26079"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".treeline."0.1.0" = overridableMkRustCrate (profileName: rec {
    name = "treeline";
    version = "0.1.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "a7f741b240f1a48843f9b8e0444fb55fb2a4ff67293b50a9179dfd5ea67f8d41"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".trees."0.2.1" = overridableMkRustCrate (profileName: rec {
    name = "trees";
    version = "0.2.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "afa1821e85be4f56cc5bd08bdbc32c0e26d105c90bed9c637992f6c7f747c180"; };
    dependencies = {
      indexed = rustPackages."registry+https://github.com/rust-lang/crates.io-index".indexed."0.1.1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".try-lock."0.2.2" = overridableMkRustCrate (profileName: rec {
    name = "try-lock";
    version = "0.2.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "e604eb7b43c06650e854be16a2a03155743d3752dd1c943f6829e26b7a36e382"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".tungstenite."0.10.1" = overridableMkRustCrate (profileName: rec {
    name = "tungstenite";
    version = "0.10.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "cfea31758bf674f990918962e8e5f07071a3161bd7c4138ed23e416e1ac4264e"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "native-tls" ]
      [ "tls" ]
    ];
    dependencies = {
      base64 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".base64."0.11.0" { inherit profileName; };
      byteorder = rustPackages."registry+https://github.com/rust-lang/crates.io-index".byteorder."1.3.4" { inherit profileName; };
      bytes = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bytes."0.5.4" { inherit profileName; };
      http = rustPackages."registry+https://github.com/rust-lang/crates.io-index".http."0.2.1" { inherit profileName; };
      httparse = rustPackages."registry+https://github.com/rust-lang/crates.io-index".httparse."1.3.4" { inherit profileName; };
      input_buffer = rustPackages."registry+https://github.com/rust-lang/crates.io-index".input_buffer."0.3.1" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      native_tls = rustPackages."registry+https://github.com/rust-lang/crates.io-index".native-tls."0.2.7" { inherit profileName; };
      rand = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand."0.7.3" { inherit profileName; };
      sha1 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".sha-1."0.8.2" { inherit profileName; };
      url = rustPackages."registry+https://github.com/rust-lang/crates.io-index".url."2.2.0" { inherit profileName; };
      utf8 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".utf-8."0.7.5" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".typeable."0.1.2" = overridableMkRustCrate (profileName: rec {
    name = "typeable";
    version = "0.1.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "1410f6f91f21d1612654e7cc69193b0334f909dcf2c790c4826254fbb86f8887"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".typenum."1.12.0" = overridableMkRustCrate (profileName: rec {
    name = "typenum";
    version = "1.12.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "373c8a200f9e67a0c95e62a4f52fbf80c23b4381c05a17845531982fa99e6b33"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".ucd-trie."0.1.3" = overridableMkRustCrate (profileName: rec {
    name = "ucd-trie";
    version = "0.1.3";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "56dee185309b50d1f11bfedef0fe6d036842e3fb77413abef29f8f8d1c5d4c1c"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "std" ]
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".unicase."1.4.2" = overridableMkRustCrate (profileName: rec {
    name = "unicase";
    version = "1.4.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "7f4765f83163b74f957c797ad9253caf97f103fb064d3999aea9568d09fc8a33"; };
    buildDependencies = {
      version_check = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".version_check."0.1.5" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".unicase."2.6.0" = overridableMkRustCrate (profileName: rec {
    name = "unicase";
    version = "2.6.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "50f37be617794602aabbeee0be4f259dc1778fabe05e2d67ee8f79326d5cb4f6"; };
    buildDependencies = {
      version_check = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".version_check."0.9.2" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".unicode-bidi."0.3.4" = overridableMkRustCrate (profileName: rec {
    name = "unicode-bidi";
    version = "0.3.4";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "49f2bd0c6468a8230e1db229cff8029217cf623c767ea5d60bfbd42729ea54d5"; };
    features = builtins.concatLists [
      [ "default" ]
    ];
    dependencies = {
      matches = rustPackages."registry+https://github.com/rust-lang/crates.io-index".matches."0.1.8" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".unicode-normalization."0.1.16" = overridableMkRustCrate (profileName: rec {
    name = "unicode-normalization";
    version = "0.1.16";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "a13e63ab62dbe32aeee58d1c5408d35c36c392bba5d9d3142287219721afe606"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "std" ]
    ];
    dependencies = {
      tinyvec = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tinyvec."1.1.1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".unicode-width."0.1.7" = overridableMkRustCrate (profileName: rec {
    name = "unicode-width";
    version = "0.1.7";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "caaa9d531767d1ff2150b9332433f32a24622147e5ebb1f26409d5da67afd479"; };
    features = builtins.concatLists [
      [ "default" ]
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".unicode-xid."0.1.0" = overridableMkRustCrate (profileName: rec {
    name = "unicode-xid";
    version = "0.1.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "fc72304796d0818e357ead4e000d19c9c174ab23dc11093ac919054d20a6a7fc"; };
    features = builtins.concatLists [
      [ "default" ]
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".unicode-xid."0.2.0" = overridableMkRustCrate (profileName: rec {
    name = "unicode-xid";
    version = "0.2.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "826e7639553986605ec5979c7dd957c7895e93eabed50ab2ffa7f6128a75097c"; };
    features = builtins.concatLists [
      [ "default" ]
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".unix_socket2."0.5.4" = overridableMkRustCrate (profileName: rec {
    name = "unix_socket2";
    version = "0.5.4";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "b57c6eace16c00eccb98a28e85db3370eab0685bdd5e13831d59e2bcb49a1d8a"; };
    dependencies = {
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".unreachable."1.0.0" = overridableMkRustCrate (profileName: rec {
    name = "unreachable";
    version = "1.0.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "382810877fe448991dfc7f0dd6e3ae5d58088fd0ea5e35189655f84e6814fa56"; };
    dependencies = {
      void = rustPackages."registry+https://github.com/rust-lang/crates.io-index".void."1.0.2" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".untrusted."0.7.1" = overridableMkRustCrate (profileName: rec {
    name = "untrusted";
    version = "0.7.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "a156c684c91ea7d62626509bce3cb4e1d9ed5c4d978f7b4352658f96a4c26b4a"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".uriparse."0.6.3" = overridableMkRustCrate (profileName: rec {
    name = "uriparse";
    version = "0.6.3";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "e515b1ada404168e145ac55afba3c42f04cf972201a8552d42e2abb17c1b7221"; };
    features = builtins.concatLists [
      [ "default" ]
    ];
    dependencies = {
      fnv = rustPackages."registry+https://github.com/rust-lang/crates.io-index".fnv."1.0.7" { inherit profileName; };
      lazy_static = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".url."1.7.2" = overridableMkRustCrate (profileName: rec {
    name = "url";
    version = "1.7.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "dd4e7c0d531266369519a4aa4f399d748bd37043b00bde1e4ff1f60a120b355a"; };
    dependencies = {
      idna = rustPackages."registry+https://github.com/rust-lang/crates.io-index".idna."0.1.5" { inherit profileName; };
      matches = rustPackages."registry+https://github.com/rust-lang/crates.io-index".matches."0.1.8" { inherit profileName; };
      percent_encoding = rustPackages."registry+https://github.com/rust-lang/crates.io-index".percent-encoding."1.0.1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".url."2.2.0" = overridableMkRustCrate (profileName: rec {
    name = "url";
    version = "2.2.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "5909f2b0817350449ed73e8bcd81c8c3c8d9a7a5d8acba4b27db277f1868976e"; };
    dependencies = {
      form_urlencoded = rustPackages."registry+https://github.com/rust-lang/crates.io-index".form_urlencoded."1.0.0" { inherit profileName; };
      idna = rustPackages."registry+https://github.com/rust-lang/crates.io-index".idna."0.2.0" { inherit profileName; };
      matches = rustPackages."registry+https://github.com/rust-lang/crates.io-index".matches."0.1.8" { inherit profileName; };
      percent_encoding = rustPackages."registry+https://github.com/rust-lang/crates.io-index".percent-encoding."2.1.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".users."0.10.0" = overridableMkRustCrate (profileName: rec {
    name = "users";
    version = "0.10.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "aa4227e95324a443c9fcb06e03d4d85e91aabe9a5a02aa818688b6918b6af486"; };
    features = builtins.concatLists [
      [ "cache" ]
      [ "default" ]
      [ "log" ]
      [ "logging" ]
      [ "mock" ]
    ];
    dependencies = {
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".utf-8."0.7.5" = overridableMkRustCrate (profileName: rec {
    name = "utf-8";
    version = "0.7.5";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "05e42f7c18b8f902290b009cde6d651262f956c98bc51bca4cd1d511c9cd85c7"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".utf8-width."0.1.3" = overridableMkRustCrate (profileName: rec {
    name = "utf8-width";
    version = "0.1.3";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "6f2c54fe5e8d6907c60dc6fba532cc8529245d97ff4e26cb490cb462de114ba4"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".vcpkg."0.2.8" = overridableMkRustCrate (profileName: rec {
    name = "vcpkg";
    version = "0.2.8";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "3fc439f2794e98976c88a2a2dafce96b930fe8010b0a256b3c2199a773933168"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".vec_map."0.8.2" = overridableMkRustCrate (profileName: rec {
    name = "vec_map";
    version = "0.8.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "f1bddf1187be692e79c5ffeab891132dfb0f236ed36a43c7ed39f1165ee20191"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".version_check."0.1.5" = overridableMkRustCrate (profileName: rec {
    name = "version_check";
    version = "0.1.5";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "914b1a6776c4c929a602fafd8bc742e06365d4bcbe48c30f9cca5824f70dc9dd"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".version_check."0.9.2" = overridableMkRustCrate (profileName: rec {
    name = "version_check";
    version = "0.9.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "b5a972e5669d67ba988ce3dc826706fb0a8b01471c088cb0b6110b805cc36aed"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".void."1.0.2" = overridableMkRustCrate (profileName: rec {
    name = "void";
    version = "1.0.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "6a02e4885ed3bc0f2de90ea6dd45ebcbb66dacffe03547fadbb0eeae2770887d"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "std" ]
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".wait-timeout."0.2.0" = overridableMkRustCrate (profileName: rec {
    name = "wait-timeout";
    version = "0.2.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "9f200f5b12eb75f8c1ed65abd4b2db8a6e1b138a20de009dacee265a2498f3f6"; };
    dependencies = {
      ${ if hostPlatform.isUnix then "libc" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".walkdir."2.3.1" = overridableMkRustCrate (profileName: rec {
    name = "walkdir";
    version = "2.3.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "777182bc735b6424e1a57516d35ed72cb8019d85c8c9bf536dccb3445c1a2f7d"; };
    dependencies = {
      same_file = rustPackages."registry+https://github.com/rust-lang/crates.io-index".same-file."1.0.6" { inherit profileName; };
      ${ if hostPlatform.isWindows then "winapi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.3.8" { inherit profileName; };
      ${ if hostPlatform.isWindows then "winapi_util" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi-util."0.1.5" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".want."0.3.0" = overridableMkRustCrate (profileName: rec {
    name = "want";
    version = "0.3.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "1ce8a968cb1cd110d136ff8b819a556d6fb6d919363c61534f6860c7eb172ba0"; };
    dependencies = {
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      try_lock = rustPackages."registry+https://github.com/rust-lang/crates.io-index".try-lock."0.2.2" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".wasi."0.9.0+wasi-snapshot-preview1" = overridableMkRustCrate (profileName: rec {
    name = "wasi";
    version = "0.9.0+wasi-snapshot-preview1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "cccddf32554fecc6acb585f82a32a72e28b48f8c4c1883ddfeeeaa96f7d8e519"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "std" ]
    ];
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".wasm-bindgen."0.2.70" = overridableMkRustCrate (profileName: rec {
    name = "wasm-bindgen";
    version = "0.2.70";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "55c0f7123de74f0dab9b7d00fd614e7b19349cd1e2f5252bbe9b1754b59433be"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "serde" ]
      [ "serde-serialize" ]
      [ "serde_json" ]
      [ "spans" ]
      [ "std" ]
    ];
    dependencies = {
      cfg_if = rustPackages."registry+https://github.com/rust-lang/crates.io-index".cfg-if."1.0.0" { inherit profileName; };
      serde = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde."1.0.122" { inherit profileName; };
      serde_json = rustPackages."registry+https://github.com/rust-lang/crates.io-index".serde_json."1.0.62" { inherit profileName; };
      wasm_bindgen_macro = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".wasm-bindgen-macro."0.2.70" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".wasm-bindgen-backend."0.2.70" = overridableMkRustCrate (profileName: rec {
    name = "wasm-bindgen-backend";
    version = "0.2.70";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "7bc45447f0d4573f3d65720f636bbcc3dd6ce920ed704670118650bcd47764c7"; };
    features = builtins.concatLists [
      [ "spans" ]
    ];
    dependencies = {
      bumpalo = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bumpalo."3.3.0" { inherit profileName; };
      lazy_static = rustPackages."registry+https://github.com/rust-lang/crates.io-index".lazy_static."1.4.0" { inherit profileName; };
      log = rustPackages."registry+https://github.com/rust-lang/crates.io-index".log."0.4.11" { inherit profileName; };
      proc_macro2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."1.0.24" { inherit profileName; };
      quote = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quote."1.0.6" { inherit profileName; };
      syn = rustPackages."registry+https://github.com/rust-lang/crates.io-index".syn."1.0.60" { inherit profileName; };
      wasm_bindgen_shared = rustPackages."registry+https://github.com/rust-lang/crates.io-index".wasm-bindgen-shared."0.2.70" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".wasm-bindgen-futures."0.4.20" = overridableMkRustCrate (profileName: rec {
    name = "wasm-bindgen-futures";
    version = "0.4.20";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "3de431a2910c86679c34283a33f66f4e4abd7e0aec27b6669060148872aadf94"; };
    dependencies = {
      cfg_if = rustPackages."registry+https://github.com/rust-lang/crates.io-index".cfg-if."1.0.0" { inherit profileName; };
      js_sys = rustPackages."registry+https://github.com/rust-lang/crates.io-index".js-sys."0.3.47" { inherit profileName; };
      wasm_bindgen = rustPackages."registry+https://github.com/rust-lang/crates.io-index".wasm-bindgen."0.2.70" { inherit profileName; };
      ${ if builtins.elem "atomics" hostPlatformFeatures then "web_sys" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".web-sys."0.3.40" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".wasm-bindgen-macro."0.2.70" = overridableMkRustCrate (profileName: rec {
    name = "wasm-bindgen-macro";
    version = "0.2.70";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "3b8853882eef39593ad4174dd26fc9865a64e84026d223f63bb2c42affcbba2c"; };
    features = builtins.concatLists [
      [ "spans" ]
    ];
    dependencies = {
      quote = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quote."1.0.6" { inherit profileName; };
      wasm_bindgen_macro_support = rustPackages."registry+https://github.com/rust-lang/crates.io-index".wasm-bindgen-macro-support."0.2.70" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".wasm-bindgen-macro-support."0.2.70" = overridableMkRustCrate (profileName: rec {
    name = "wasm-bindgen-macro-support";
    version = "0.2.70";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "4133b5e7f2a531fa413b3a1695e925038a05a71cf67e87dafa295cb645a01385"; };
    features = builtins.concatLists [
      [ "spans" ]
    ];
    dependencies = {
      proc_macro2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."1.0.24" { inherit profileName; };
      quote = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quote."1.0.6" { inherit profileName; };
      syn = rustPackages."registry+https://github.com/rust-lang/crates.io-index".syn."1.0.60" { inherit profileName; };
      wasm_bindgen_backend = rustPackages."registry+https://github.com/rust-lang/crates.io-index".wasm-bindgen-backend."0.2.70" { inherit profileName; };
      wasm_bindgen_shared = rustPackages."registry+https://github.com/rust-lang/crates.io-index".wasm-bindgen-shared."0.2.70" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".wasm-bindgen-shared."0.2.70" = overridableMkRustCrate (profileName: rec {
    name = "wasm-bindgen-shared";
    version = "0.2.70";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "dd4945e4943ae02d15c13962b38a5b1e81eadd4b71214eee75af64a4d6a4fd64"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".web-sys."0.3.40" = overridableMkRustCrate (profileName: rec {
    name = "web-sys";
    version = "0.3.40";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "7b72fe77fd39e4bd3eaa4412fd299a0be6b3dfe9d2597e2f1c20beb968f41d17"; };
    features = builtins.concatLists [
      [ "Blob" ]
      [ "BlobPropertyBag" ]
      [ "Crypto" ]
      [ "Event" ]
      [ "EventTarget" ]
      [ "FormData" ]
      [ "Headers" ]
      [ "MessageEvent" ]
      [ "Request" ]
      [ "RequestInit" ]
      [ "RequestMode" ]
      [ "Response" ]
      [ "ServiceWorkerGlobalScope" ]
      [ "Window" ]
      [ "Worker" ]
      [ "WorkerGlobalScope" ]
    ];
    dependencies = {
      js_sys = rustPackages."registry+https://github.com/rust-lang/crates.io-index".js-sys."0.3.47" { inherit profileName; };
      wasm_bindgen = rustPackages."registry+https://github.com/rust-lang/crates.io-index".wasm-bindgen."0.2.70" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".webpki."0.21.2" = overridableMkRustCrate (profileName: rec {
    name = "webpki";
    version = "0.21.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "f1f50e1972865d6b1adb54167d1c8ed48606004c2c9d0ea5f1eeb34d95e863ef"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "std" ]
      [ "trust_anchor_util" ]
    ];
    dependencies = {
      ring = rustPackages."registry+https://github.com/rust-lang/crates.io-index".ring."0.16.12" { inherit profileName; };
      untrusted = rustPackages."registry+https://github.com/rust-lang/crates.io-index".untrusted."0.7.1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".webpki-roots."0.21.0" = overridableMkRustCrate (profileName: rec {
    name = "webpki-roots";
    version = "0.21.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "82015b7e0b8bad8185994674a13a93306bea76cf5a16c5a181382fd3a5ec2376"; };
    dependencies = {
      webpki = rustPackages."registry+https://github.com/rust-lang/crates.io-index".webpki."0.21.2" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".websocket."0.24.0" = overridableMkRustCrate (profileName: rec {
    name = "websocket";
    version = "0.24.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "413b37840b9e27b340ce91b319ede10731de8c72f5bc4cb0206ec1ca4ce581d0"; };
    features = builtins.concatLists [
      [ "async" ]
      [ "async-ssl" ]
      [ "bytes" ]
      [ "default" ]
      [ "futures" ]
      [ "native-tls" ]
      [ "sync" ]
      [ "sync-ssl" ]
      [ "tokio-codec" ]
      [ "tokio-io" ]
      [ "tokio-reactor" ]
      [ "tokio-tcp" ]
      [ "tokio-tls" ]
    ];
    dependencies = {
      bytes = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bytes."0.4.12" { inherit profileName; };
      futures = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures."0.1.29" { inherit profileName; };
      hyper = rustPackages."registry+https://github.com/rust-lang/crates.io-index".hyper."0.10.16" { inherit profileName; };
      native_tls = rustPackages."registry+https://github.com/rust-lang/crates.io-index".native-tls."0.2.7" { inherit profileName; };
      rand = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand."0.6.5" { inherit profileName; };
      tokio_codec = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-codec."0.1.2" { inherit profileName; };
      tokio_io = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-io."0.1.13" { inherit profileName; };
      tokio_reactor = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-reactor."0.1.12" { inherit profileName; };
      tokio_tcp = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-tcp."0.1.4" { inherit profileName; };
      tokio_tls = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-tls."0.2.1" { inherit profileName; };
      unicase = rustPackages."registry+https://github.com/rust-lang/crates.io-index".unicase."1.4.2" { inherit profileName; };
      url = rustPackages."registry+https://github.com/rust-lang/crates.io-index".url."1.7.2" { inherit profileName; };
      websocket_base = rustPackages."registry+https://github.com/rust-lang/crates.io-index".websocket-base."0.24.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".websocket-base."0.24.0" = overridableMkRustCrate (profileName: rec {
    name = "websocket-base";
    version = "0.24.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "5e3810f0d00c4dccb54c30a4eee815e703232819dec7b007db115791c42aa374"; };
    features = builtins.concatLists [
      [ "async" ]
      [ "async-ssl" ]
      [ "bytes" ]
      [ "futures" ]
      [ "native-tls" ]
      [ "sync" ]
      [ "sync-ssl" ]
      [ "tokio-codec" ]
      [ "tokio-io" ]
      [ "tokio-tcp" ]
      [ "tokio-tls" ]
    ];
    dependencies = {
      base64 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".base64."0.10.1" { inherit profileName; };
      bitflags = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bitflags."1.2.1" { inherit profileName; };
      byteorder = rustPackages."registry+https://github.com/rust-lang/crates.io-index".byteorder."1.3.4" { inherit profileName; };
      bytes = rustPackages."registry+https://github.com/rust-lang/crates.io-index".bytes."0.4.12" { inherit profileName; };
      futures = rustPackages."registry+https://github.com/rust-lang/crates.io-index".futures."0.1.29" { inherit profileName; };
      native_tls = rustPackages."registry+https://github.com/rust-lang/crates.io-index".native-tls."0.2.7" { inherit profileName; };
      rand = rustPackages."registry+https://github.com/rust-lang/crates.io-index".rand."0.6.5" { inherit profileName; };
      sha1 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".sha1."0.6.0" { inherit profileName; };
      tokio_codec = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-codec."0.1.2" { inherit profileName; };
      tokio_io = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-io."0.1.13" { inherit profileName; };
      tokio_tcp = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-tcp."0.1.4" { inherit profileName; };
      tokio_tls = rustPackages."registry+https://github.com/rust-lang/crates.io-index".tokio-tls."0.2.1" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".which."3.1.1" = overridableMkRustCrate (profileName: rec {
    name = "which";
    version = "3.1.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "d011071ae14a2f6671d0b74080ae0cd8ebf3a6f8c9589a2cd45f23126fe29724"; };
    dependencies = {
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".winapi."0.2.8" = overridableMkRustCrate (profileName: rec {
    name = "winapi";
    version = "0.2.8";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "167dc9d6949a9b857f3451275e911c3f44255842c1f7a76f33c55103a909087a"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".winapi."0.3.8" = overridableMkRustCrate (profileName: rec {
    name = "winapi";
    version = "0.3.8";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "8093091eeb260906a183e6ae1abdba2ef5ef2257a21801128899c3fc699229c6"; };
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "accctrl")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "aclapi")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "basetsd")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "cfg")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cargo-test-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-logger" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "consoleapi")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator") "dbghelp")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cargo-test-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-logger" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "errhandlingapi")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "evntrace")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-logger" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "fileapi")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "handleapi")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "impl-debug")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "impl-default")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "in6addr")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "inaddr")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "ioapiset")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-config" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "knownfolders")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "libloaderapi")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower") "lmcons")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "memoryapi")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower") "minschannel")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cargo-test-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-logger" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "minwinbase")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cargo-test-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-logger" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "minwindef")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "mswsock")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "namedpipeapi")
      (lib.optional (rootFeatures' ? "solana-sdk/chrono" || rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "ntdef")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "ntsecapi")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "ntstatus")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-config" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "objbase")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cargo-test-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-logger" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "processenv")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "processthreadsapi")
      (lib.optional (rootFeatures' ? "solana-sdk/chrono" || rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "profileapi")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower") "schannel")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower") "securitybaseapi")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-config" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "shlobj")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower") "sspi")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-logger" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "std")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "synchapi")
      (lib.optional (rootFeatures' ? "solana-sdk/chrono" || rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "sysinfoapi")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower") "threadpoollegacyapiset")
      (lib.optional (rootFeatures' ? "solana-sdk/chrono" || rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "timezoneapi")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "utilapiset")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cargo-test-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-config" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-logger" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "winbase")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-logger" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "wincon")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower") "wincrypt")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "windef")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-config" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-logger" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "winerror")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "winioctl")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "winnls")
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-logger" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "winnt")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "winreg")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "winsock2")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "winuser")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "ws2def")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "ws2ipdef")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "ws2tcpip")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "wtypesbase")
    ];
    dependencies = {
      ${ if (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cargo-test-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-config" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-logger" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") && hostPlatform.config == "i686-pc-windows-gnu" then "winapi_i686_pc_windows_gnu" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi-i686-pc-windows-gnu."0.4.0" { inherit profileName; };
      ${ if (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-cargo-test-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-config" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-frozen-abi" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-logger" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-notifier" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") && hostPlatform.config == "x86_64-pc-windows-gnu" then "winapi_x86_64_pc_windows_gnu" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi-x86_64-pc-windows-gnu."0.4.0" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".winapi-build."0.1.1" = overridableMkRustCrate (profileName: rec {
    name = "winapi-build";
    version = "0.1.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "2d315eee3b34aca4797b2da6b13ed88266e6d612562a0c46390af8299fc699bc"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".winapi-i686-pc-windows-gnu."0.4.0" = overridableMkRustCrate (profileName: rec {
    name = "winapi-i686-pc-windows-gnu";
    version = "0.4.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "ac3b87c63620426dd9b991e5ce0329eff545bccbbb34f3be09ff6fb6ab51b7b6"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".winapi-util."0.1.5" = overridableMkRustCrate (profileName: rec {
    name = "winapi-util";
    version = "0.1.5";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "70ec6ce85bb158151cae5e5c87f95a8e97d2c0c4b001223f33a334e3ce5de178"; };
    dependencies = {
      ${ if hostPlatform.isWindows then "winapi" else null } = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.3.8" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".winapi-x86_64-pc-windows-gnu."0.4.0" = overridableMkRustCrate (profileName: rec {
    name = "winapi-x86_64-pc-windows-gnu";
    version = "0.4.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "712e227841d057c1ee1cd2fb22fa7e5a5461ae8e48fa2ca79ec42cfc1931183f"; };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".winreg."0.7.0" = overridableMkRustCrate (profileName: rec {
    name = "winreg";
    version = "0.7.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "0120db82e8a1e0b9fb3345a539c478767c0048d842860994d96113d5b667bd69"; };
    dependencies = {
      winapi = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.3.8" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".ws2_32-sys."0.2.1" = overridableMkRustCrate (profileName: rec {
    name = "ws2_32-sys";
    version = "0.2.1";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "d59cefebd0c892fa2dd6de581e937301d8552cb44489cdff035c6187cb63fa5e"; };
    dependencies = {
      winapi = rustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi."0.2.8" { inherit profileName; };
    };
    buildDependencies = {
      build = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".winapi-build."0.1.1" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".xattr."0.2.2" = overridableMkRustCrate (profileName: rec {
    name = "xattr";
    version = "0.2.2";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "244c3741f4240ef46274860397c7c74e50eb23624996930e484c16679633a54c"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "unsupported" ]
    ];
    dependencies = {
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".yaml-rust."0.4.3" = overridableMkRustCrate (profileName: rec {
    name = "yaml-rust";
    version = "0.4.3";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "65923dd1784f44da1d2c3dbbc5e822045628c590ba72123e1c73d3c230c4434d"; };
    dependencies = {
      linked_hash_map = rustPackages."registry+https://github.com/rust-lang/crates.io-index".linked-hash-map."0.5.3" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".zeroize."1.2.0" = overridableMkRustCrate (profileName: rec {
    name = "zeroize";
    version = "1.2.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "81a974bcdd357f0dca4d41677db03436324d45a4c9ed2d0b873a5a360ce41c36"; };
    features = builtins.concatLists [
      (lib.optional (rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-merkle-tree" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-sdk" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "alloc")
      (lib.optional (rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-watchtower") "default")
      (lib.optional (rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/ed25519-dalek" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower") "zeroize_derive")
    ];
    dependencies = {
      ${ if rootFeatures' ? "solana-sdk/default" || rootFeatures' ? "solana-sdk/ed25519-dalek" || rootFeatures' ? "solana-sdk/full" || rootFeatures' ? "solana-sdk/solana-crate-features" || rootFeatures' ? "solana-account-decoder" || rootFeatures' ? "solana-accounts-bench" || rootFeatures' ? "solana-accounts-cluster-bench" || rootFeatures' ? "solana-banking-bench" || rootFeatures' ? "solana-banks-client" || rootFeatures' ? "solana-banks-interface" || rootFeatures' ? "solana-banks-server" || rootFeatures' ? "solana-bench-exchange" || rootFeatures' ? "solana-bench-streamer" || rootFeatures' ? "solana-bench-tps" || rootFeatures' ? "solana-bpf-loader-program" || rootFeatures' ? "solana-budget-program" || rootFeatures' ? "solana-cargo-build-bpf" || rootFeatures' ? "solana-clap-utils" || rootFeatures' ? "solana-cli" || rootFeatures' ? "solana-cli-output" || rootFeatures' ? "solana-client" || rootFeatures' ? "solana-config-program" || rootFeatures' ? "solana-core" || rootFeatures' ? "solana-crate-features" || rootFeatures' ? "solana-dos" || rootFeatures' ? "solana-download-utils" || rootFeatures' ? "solana-exchange-program" || rootFeatures' ? "solana-failure-program" || rootFeatures' ? "solana-faucet" || rootFeatures' ? "solana-genesis" || rootFeatures' ? "solana-gossip" || rootFeatures' ? "solana-install" || rootFeatures' ? "solana-keygen" || rootFeatures' ? "solana-ledger" || rootFeatures' ? "solana-ledger-tool" || rootFeatures' ? "solana-local-cluster" || rootFeatures' ? "solana-log-analyzer" || rootFeatures' ? "solana-measure" || rootFeatures' ? "solana-merkle-root-bench" || rootFeatures' ? "solana-metrics" || rootFeatures' ? "solana-net-shaper" || rootFeatures' ? "solana-net-utils" || rootFeatures' ? "solana-noop-program" || rootFeatures' ? "solana-ownable" || rootFeatures' ? "solana-perf" || rootFeatures' ? "solana-poh-bench" || rootFeatures' ? "solana-program-test" || rootFeatures' ? "solana-ramp-tps" || rootFeatures' ? "solana-remote-wallet" || rootFeatures' ? "solana-runtime" || rootFeatures' ? "solana-secp256k1-program" || rootFeatures' ? "solana-stake-accounts" || rootFeatures' ? "solana-stake-monitor" || rootFeatures' ? "solana-stake-o-matic" || rootFeatures' ? "solana-stake-program" || rootFeatures' ? "solana-storage-bigtable" || rootFeatures' ? "solana-storage-proto" || rootFeatures' ? "solana-store-tool" || rootFeatures' ? "solana-streamer" || rootFeatures' ? "solana-sys-tuner" || rootFeatures' ? "solana-tokens" || rootFeatures' ? "solana-transaction-status" || rootFeatures' ? "solana-upload-perf" || rootFeatures' ? "solana-validator" || rootFeatures' ? "solana-version" || rootFeatures' ? "solana-vest-program" || rootFeatures' ? "solana-vote-program" || rootFeatures' ? "solana-watchtower" then "zeroize_derive" else null } = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".zeroize_derive."1.0.0" { profileName = "__noProfile"; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".zeroize_derive."1.0.0" = overridableMkRustCrate (profileName: rec {
    name = "zeroize_derive";
    version = "1.0.0";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "de251eec69fc7c1bc3923403d18ececb929380e016afe103da75f396704f8ca2"; };
    dependencies = {
      proc_macro2 = rustPackages."registry+https://github.com/rust-lang/crates.io-index".proc-macro2."1.0.24" { inherit profileName; };
      quote = rustPackages."registry+https://github.com/rust-lang/crates.io-index".quote."1.0.6" { inherit profileName; };
      syn = rustPackages."registry+https://github.com/rust-lang/crates.io-index".syn."1.0.60" { inherit profileName; };
      synstructure = rustPackages."registry+https://github.com/rust-lang/crates.io-index".synstructure."0.12.3" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".zstd."0.5.3+zstd.1.4.5" = overridableMkRustCrate (profileName: rec {
    name = "zstd";
    version = "0.5.3+zstd.1.4.5";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "01b32eaf771efa709e8308605bbf9319bf485dc1503179ec0469b611937c0cd8"; };
    features = builtins.concatLists [
      [ "default" ]
      [ "legacy" ]
    ];
    dependencies = {
      zstd_safe = rustPackages."registry+https://github.com/rust-lang/crates.io-index".zstd-safe."2.0.5+zstd.1.4.5" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".zstd-safe."2.0.5+zstd.1.4.5" = overridableMkRustCrate (profileName: rec {
    name = "zstd-safe";
    version = "2.0.5+zstd.1.4.5";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "1cfb642e0d27f64729a639c52db457e0ae906e7bc6f5fe8f5c453230400f1055"; };
    features = builtins.concatLists [
      [ "experimental" ]
      [ "legacy" ]
    ];
    dependencies = {
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
      zstd_sys = rustPackages."registry+https://github.com/rust-lang/crates.io-index".zstd-sys."1.4.17+zstd.1.4.5" { inherit profileName; };
    };
  });
  
  "registry+https://github.com/rust-lang/crates.io-index".zstd-sys."1.4.17+zstd.1.4.5" = overridableMkRustCrate (profileName: rec {
    name = "zstd-sys";
    version = "1.4.17+zstd.1.4.5";
    registry = "registry+https://github.com/rust-lang/crates.io-index";
    src = fetchCratesIo { inherit name version; sha256 = "b89249644df056b522696b1bb9e7c18c87e8ffa3e2f0dc3b0155875d6498f01b"; };
    features = builtins.concatLists [
      [ "experimental" ]
      [ "legacy" ]
    ];
    dependencies = {
      libc = rustPackages."registry+https://github.com/rust-lang/crates.io-index".libc."0.2.81" { inherit profileName; };
    };
    buildDependencies = {
      cc = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".cc."1.0.49" { profileName = "__noProfile"; };
      glob = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".glob."0.3.0" { profileName = "__noProfile"; };
      itertools = buildRustPackages."registry+https://github.com/rust-lang/crates.io-index".itertools."0.9.0" { profileName = "__noProfile"; };
    };
  });
  
}
